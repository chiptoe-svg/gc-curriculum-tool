# Project State — GC Curriculum Tool

> **Audience:** developer / implementation detail. This file is the engineering snapshot — what's live, what's blocked, what's next at the code and deployment level. Stakeholder-facing surfaces (executive brief, vision, background) summarize the same state in non-implementation terms; if you're orienting non-technical reviewers, point them there first and use this file as the backing source they can drop into when they want detail. The executive brief links here intentionally — anyone following that link is opting into the operational view.
>
> **Last verified:** `d59b99a` · 2026-06-12 *(CourseCapture **Step 1 three-source redesign** shipped & deployed — built via an ultracode workflow (6 agents): the confirm screen is now three accordion source-boxes (Syllabus & course info · Canvas · Other materials), each honest about depth + managed inline; replaces the flat list + bolted-on MaterialsPanel reveal (panel kept, reuses the extracted CatalogOverview, reachable via a bottom "Manage all materials in detail" disclosure). Live-walkthrough polish since: dropped a stray SyllabusBox shadow; "Scan linked docs" grays once scanned and **moved to the Canvas box**; **"Index now"/readability** + the indexing-backlog backfill (ready 72→89). Also reconciled the operator's **`/procurement.html`** public explainer (sibling ask_procurement project; basic-auth allowlist) into one `dev==main==origin` lineage. 995 suite green, tsc clean; live capture page + procurement page HTTP 200. Prior `7c29626`: capture entry refinements. `f6ee29a`: Step-1 gate. `34703a3`: course categories + `builds_to_career` (migration `0033`). `6c8bba7`: wiki trust-hardening A–E + system hardening + CC-v2 finish.)* Round 8: A15 approve guard (approval locked until ≥1 edit, all worth-a-look reviewed, or a substantive departmental note — anti-rubber-stamp, per the audit's documented decay mode); Step-1 manager disclosure slimmed to 'Bulk material operations' (hideRows MaterialsPanel — per-row controls live in the boxes; FERPA include-anyway + why-ignored reasons moved into box rows; chat stage keeps the full panel).
>
> **What this is:** the single source of truth for "what's live, what's next, what's blocked." Read this before any feature work, schema change, AI function add, deployment change, or new spec/plan. Static framing (KUD+, vision, architecture rationale) lives in [`CLAUDE.md`](../CLAUDE.md) and [`docs/superpowers/README.md`](./superpowers/README.md); this file is the volatile snapshot that sits in front of them.
>
> **Update protocol:** if your commit touches anything in [§What this file tracks](#what-this-file-tracks), update it in the same commit. For a full reconciliation, run `/refresh-state`.

---

## ✅ Recently shipped + deployed (2026-06-11) — wiki trust-hardening, system hardening, CourseCapture-v2 finish, debt sweep

A large session, merged to `main` (`6c8bba7`) + deployed (launchd `com.gc.curriculum-tool` restarted, healthy). Suite **920** green, `tsc` clean. No new deps; no new migrations applied (the held `0031`/`0032` were deliberately NOT touched). Five sub-batches:

- **Wiki trust-hardening A–E** (myKG-inspired) — `gc-wiki-lint` (deterministic structural validator, `pnpm wiki:lint`) + `WIKI_SCHEMA` contract; typed-graph MCP/`/ask` tools `coverage_for_target` + `prereq_chain`; loud-not-silent batch reconcile + `input_hash` watermark in the compile loop; per-competency evidence band (`·claimed`/`·materials`/`·artifact`) on course **and** competency pages + an optional `search_wiki` `bandFloor`; snapshot-derived text fenced as untrusted in `wiki-update.md`. Design: [`2026-06-11-wiki-trust-hardening-design.md`](./superpowers/specs/2026-06-11-wiki-trust-hardening-design.md). **The lint surfaced 153 broken wikilinks** (a real two-namespace compile bug) — structural fix shipped (`competencyLinks` map); clear the count by re-running `pnpm wiki:lint` after the next regen.
- **System hardening** — `getProvider()` → `getProviderForFunction(id)` for the 4 live scorers (3 new functionIds: `parse-profile-fields`, `extract-course-kud`, `synthesize-target`); **9 dead/unreachable M-trial scorer modules deleted** (2 confirmed-dead chains + 7 superseded) with their prompts + tests; per-route daily-cap gate on the 7 paid AI routes (+ `recordSpend` on the 5 non-streaming); admin **dual-accept Bearer auth** (`lib/auth/admin-auth.ts:checkAdminAuth` — Bearer `ADMIN_TOKEN` or slug-in-header or legacy `?slug=`) across all 12 admin routes.
- **CourseCapture-v2 finish** — fixed the `transcript_session_id` link bug (no snapshot had ever linked its v2 session → wiki transcripts never rendered) + backfilled the 6 existing snapshots; **retired the dead v1 capture path** (v2 is the only path; `captureChatTurn`/`generateCaptureProfile`/2 prompts/the `if(v2)…else` branches removed); corrected the stale "Stages 2–5 pending" docs (all stages shipped + live).
- **Debt sweep (items 11–22, minus 15)** — wiki index forced to last batch + manifest; scaffolding `no_introduction` status; class-structure chips keyboard-accessible; Position-Capture Page-6 `completeness>='rated'` guard; 4 system-audit cosmetics (daily-cap local day key, spreadsheet base64 breadth, `getSessionInstructor` query, evidence-ladder `source='materials'`); `bulkCreateCourses` batched. Deferrals (DB CHECKs blocked by held migrations; TOCTOU; micro-perf) recorded in Deferred/debt.

Branches (`feat/wiki-trust-hardening`, `chore/system-hardening`, `feat/capture-v2-finish`, `chore/debt-lows-17-22`, `chore/debt-11-16`) all merged via `--no-ff`.

**Program memory for the capture agent (item #5) — built same session (`feat/program-memory-capture-agent`).** Brainstormed "cross-course memory" → **cross-instructor program memory**, and the brainstorm found no new data/store is warranted: the program-wide picture already exists (coverage matrix + wiki + typed-graph tools); the gap was that the course-local audit agent couldn't query it. Fix: `buildAuditTools` now also hands the agent the 4 existing read-only program-query tools (`search_wiki`, `read_wiki`, `coverage_for_target`, `prereq_chain`); `maxToolCalls` 2→4; `capture-chat-agent.md` gains a "Program memory" section with a **reference-never-evidence** binding rule (program coverage never raises this course's K/U/D). FERPA-safe (wiki excludes `raw/`; graph is aggregate). Spec: [`2026-06-11-program-memory-for-capture-agent-design.md`](./superpowers/specs/2026-06-11-program-memory-for-capture-agent-design.md).

---

## ✅ Recently merged + deployed (2026-06-09) — audit-remediation + demand-seam + projects-first-class

These three workstreams (developed on `projects-first-class-2026-06-08`, which was stacked on the `audit-remediation-2026-06-07` branch) were **merged into `main` (`03558b2`) and deployed on 2026-06-09.** Suite **843/843** green, `tsc` clean. The audit report: [`docs/superpowers/audits/2026-06-07-system-audit.md`](./superpowers/audits/2026-06-07-system-audit.md). **All of the FERPA/PII/cost/correctness fixes below are now live on `main`** — they were previously branch-only, which meant `main` (had it been deployed) lacked PII redaction on the public `/view`; that gap is closed.

**Shipped on the branch (all 11 confirmed high/critical + several mediums):**
- **FERPA enforcement (was the critical one):** high-risk *content* (CUIDs, gradebook/roster tables, multiple student emails) is now auto-set-aside in `runV2Pipeline` **before** any external digest/embedding call — the detector was previously advisory-only. Detector patterns broadened (`lib/capture/ferpa-detect.ts`). PII redaction added on the public `/view/<code>` render (`lib/capture/redact-pii.ts`).
- **Admin defense-in-depth:** `mark-invited`, `v2-reset`, `v2-backfill` now carry the `isValidSlug` second factor (consistency with the other 9 admin routes — NOT the held Bearer migration); regression test asserts every `app/api/admin/**/route.ts` gates.
- **Cost caps (now live-relevant — deployed `AI_PROVIDER=openai`, so these were real, not latent):** `checkDailyCap` + `recordSpend` added to the two biggest paid fan-outs — transcription (`/api/transcribe`, `/api/partners/transcribe`) and program coverage scoring (`/api/program/coverage/refresh` + per-pair). `provider.ts` now **fails closed** on unset `AI_PROVIDER` (no silent default to paid OpenAI). OpenAI tool-path cost is now metered (was 0).
- **Correctness:** scaffolding `brittle_scaffold` decided by `sequenceIndex` not array index (`lib/program/scaffolding.ts`); prereq-gap null-delivered K/U treated as no-data not a phantom gap; wiki git writes serialized (`writeAndPush` queue) and LLM-returned page paths allowlisted to the requested set; TOCTOU select-then-write upserts → `onConflictDoUpdate` (3 fns).
- **FERPA/cost mediums:** whisper OpenAI fallback is now opt-in (`WHISPER_OPENAI_FALLBACK`); partner transcribe rejects inactive partners; partner `supersedes` ownership validated; partner-chat stops leaking raw exception text; retired-aware position counts.

**Demand → coverage sufficiency seam — on `main` now, but still dormant (flag-gated; migration NOT applied).** The Q1-sufficiency seam (audit step 4, Q1 demand half) is built end-to-end behind `DEMAND_COVERAGE_SEAM` (off). It is merged + deployed in code, but **inert until activated** — `career_target_demand` migration `0032` is still unapplied and the flag is off. Spec: [`docs/superpowers/specs/2026-06-07-demand-coverage-sufficiency-seam-design.md`](./superpowers/specs/2026-06-07-demand-coverage-sufficiency-seam-design.md). Pieces: pure engine `lib/program/sufficiency.ts` (partner-weighted demand vs ordinal-MAX attainment, `no_demand`/`no_coverage` discipline) → new `career_target_demand` table (**migration `0032`, written but NOT applied**) → demand rollup `lib/ai/position-capture/demand-rollup.ts` (maps interviewed positions' `qualifying_competencies` via `sub_competency_id`, partner-weighted, unmapped excluded) → read-compose `lib/program/sufficiency-read.ts` → read-only `SufficiencyPanel` on the per-target synthesis page. The aggregate-regen route auto-populates demand when the flag is on.

**Actions still open (merge itself is DONE):**
1. ~~Review + merge the branch to `main`~~ **DONE 2026-06-09** (`03558b2`, deployed).
2. **HELD — auth migration:** admin `?slug=` → `Authorization: Bearer` + faculty-auth, across all admin routes + callers (could lock people out — see Deferred/debt). NOT started.
3. **To activate the demand seam (3 steps, reversible):** `pnpm db:migrate` (applies additive `0032`) → set `DEMAND_COVERAGE_SEAM=1` → hit "regenerate aggregate" on a target. Off until then.
4. **Deferred safe mediums (not yet done):** F11 broad cap+spend rollout across the ~10 other faculty AI routes (cleanest centralized in `provider.complete`, watch double-counting); legacy `getProvider()` scorers ignoring per-function model tiers (~13 sites); wiki batch-page reconciliation + index-staleness; private-repo startup assertion for the wiki push; `/refresh-state` for the STATE.md dead-prompt drift (kud-chat mislabeled; count 33→37) **and the residual branch-attribution drift this targeted edit didn't sweep.**

### Projects / class-structure / syllabus first-class — merged to `main` 2026-06-09 (deployed)

Built 2026-06-08 via subagent-driven execution of [spec](./superpowers/specs/2026-06-08-projects-first-class-design.md) + [plan](./superpowers/plans/2026-06-08-projects-first-class-plan.md). Adds `class_structure` + `major_projects` as first-class **nullable JSONB** fields on `CaptureProfile` (additive — no DB migration), extracted by the **v2 capture synthesis** (`capture-synthesis.md`), editable in `ProfileReviewPanel` (new `ClassStructureSection` / `MajorProjectsSection`), and rendered on the wiki course page (new **Class structure / Major projects / Syllabus** sections) with the syllabus pulled **live from the Google Sheet** (`fetchLiveCourseFromSheet`, DB-row fallback) via a new pure `mergeCourseInfo`. Strict OpenAI **v2** JSON schema extended (recursive walker test); legacy v1 schema frozen. Suite **843/843**, `tsc` clean; final holistic review (Opus) confirmed the field-name chain connects across all six layers. **Merged + deployed 2026-06-09** (it carried the audit + demand-seam work with it — they all landed together in `03558b2`). Population is still v2-flag-gated (`COURSECAPTURE_V2_INGESTION=1`) — see Deferred/debt.

---

## What's live

One Mac, one DB, one Next.js process. Faculty surfaces gated by HTTP Basic Auth (via the Tailscale Funnel); partner surfaces public via the same Funnel under `PUBLIC_PREFIXES`. Vercel + Neon + Resend retired 2026-06-04.

**Deploy is pinned to `main` (2026-06-09).** The launchd `com.gc.curriculum-tool` service serves a dedicated **deploy worktree** at `~/projects/curriculum_developer-deploy` checked out on `main` — *not* the dev checkout. Day-to-day branch switching in `~/projects/curriculum_developer` (the dev checkout) can no longer move the live server. **Deploy a new `main`:** `git -C ~/projects/curriculum_developer-deploy pull` (the `next dev` process hot-reloads). This replaced the prior fragile setup where production was whatever branch happened to be checked out in the dev dir.

### Faculty surfaces — local Mac (LAN, `0.0.0.0:3000`, Basic Auth)

| Route | Surface | Status | Shipped |
| ----- | ------- | ------ | ------- |
| `/capture/[code]` | **CourseCapture v1** — audit conversation → confirmed Course Outcome Profile + immutable snapshots | live | 2026-05-23 (prototype) → 2026-05-24 (v1 close-out) |
| `/explore/[code]` | **Explore v1** — custom-target authoring, downstream-target auto-detection, what-if scenarios. **(2026-06-02)** Third mode `?tab=ask` mounts `<AskTab>`, a streaming chat over the curriculum wiki anchored to the focused course but program-aware — agent ranges across courses / competencies / targets / concepts. When the course has no snapshot, the page renders a chat-only fallback so faculty can still ask program-level questions about it. | live | 2026-05-24 |
| `/program` | **Program Coverage Matrix (Phase 1A)** — snapshots × career-target sub-competencies, depth-aware heat map, on-demand AI scoring. **(2026-06-12, A8 decision)** One row per **(course, instructor)** — latest non-retired snapshot per instructor (`DISTINCT ON (course_code, instructor_name)` in `getMatrixData`/`listStalePairs`), rows labeled "by [instructor]" — replaces newest-capture-wins so instructor variance is visible side by side. Flags stay course-level (marker appears on every instructor row of the course; drift anchors to the newest snapshot across instructors). | live | 2026-05-25 |
| `/program/scaffolding` | **Scaffolding Strip (Phase 1B Stage 1)** — per-target depth × productive-failure × reflection heat map; deterministic scoring | live | 2026-05-28 |
| `/courses` | **Course catalog — typeset index** of every catalog course with computed capture status (`not-started / in-audit / ai-drafted / reviewed / captured`), last-captured date, click-through to `/capture/[code]`. Editorial design language (Fraunces serif + DM Sans + IBM Plex Mono via `next/font`). Status grouped by 1000/2000/3000/4000-level. **(2026-06-02)** Per-row `💬 Ask` link deep-links to `/explore/[code]?tab=ask`. **(2026-06-05)** Bulk "preload all" button + add-a-class flow; data-state badges (captured / in-audit / not-started). | live | 2026-05-31 |
| `/courses/new` | Focused add-a-course form → POSTs roster add → redirects into `/capture/[code]` Step 1 | live | 2026-06-12 |
| `POST /api/capture/[code]/reconcile` | **Guided faculty-reconciliation AI route** — `reconcile-feedback` AI function; faculty send per-section feedback, route returns proposed per-item edits; cost-capped. Part of the `reconcile` capture stage. | live | 2026-06-14 |
| `/courses/[code]` | **Per-course prerequisite view** — seed skill-tagged prerequisite edges from syllabus prerequisites (`prereq-edge-seed`), confirm/edit the resulting edge list, and read the computed gap view (`computePrereqGaps` — ordinal-MAX, no double-count, `basis: measured / mixed / intended / none`, where `mixed` flags a gap closed partly by an unverified syllabus-intended value). Data-state badge shows per-edge basis. `not yet captured` prerequisites shown explicitly as no-data (never inferred as gap-free). | live | 2026-06-05 |
| `/wiki` | **Curriculum Wiki index** — reads `index.md` from the local wiki clone. **Now populated** (not empty): `wiki-update` writes course + concept pages + `index.md` on snapshot creation (see Wiki-update below). `competencies/` + `targets/` now regenerate **after coverage scoring** — the coverage refresh routes (`/api/program/coverage/refresh` + the per-pair variant) re-fire `wiki-update` (deduped, fire-and-forget) once cells exist, since those pages derive from `snapshot_target_coverage`. **Back-populated 2026-06-05** — 30 `competencies/` + 5 `targets/` pages now live (cross-course rollups). **(2026-06-02)** Standalone curriculum chat (`<AskTab />` in no-anchor mode) mounted below the index so faculty can ask program-level questions in the same place they browse. | live | 2026-06-01 |
| `/ask` | **Standalone curriculum chat** — program-level Q&A over the wiki, no course anchor. Pre-loads `index.md` as orientation context so the agent saves one obvious `read_wiki` call. Same `<AskTab />` as the Explore "Ask" tab and the `/wiki` chat panel. | live | 2026-06-02 |
| `/wiki/[type]/[slug]` | **Wiki per-page** — reads `<type>/<slug>.md`; renders markdown with GFM + wikilink resolution; frontmatter-gated title + breadcrumb; 404 when file is absent; `type` ∈ `{courses,competencies,targets,concepts}` | live | 2026-06-01 |
| `/settings` | Per-function AI model tier + override | live | 2026-05-24 |
| `/admin/partners` | Partner roster — single-partner **Add** form (`POST /api/admin/partners/create`, slug-gated) + CSV import; Copy-link / Compose-email invites (one link per employer, reusable across multiple positions); per-partner status now incl. invited / opened / **draft & submitted position counts** (`countPositionsByPartner`) | live | 2026-05-19 (Plan 1); add-form + status counts 2026-06-06 |
| `/admin/synthesis?slug=…` | Per-target AI synthesis dashboard (themes, salaries, partner quotes, proposed KUD edits) | live | 2026-05-19 (Plan 3) |
| `/` | Home (slug-gated) | live | — |

### Partner / public surfaces — Mac via Tailscale Funnel (`admins-mac-studio-2.tailb723c1.ts.net`)

| Route | Surface | Status |
| ----- | ------- | ------ |
| `/partners/[token]` | Magic-link landing: welcome + career-target picker | live |
| `/partners/[token]/positions/new` | **Position Capture v1** — career-target picker (step 0 of the 6-page flow) | live |
| `/partners/[token]/positions/[id]/page/[step]` | **Position Capture v1** — 6-page wizard (JD ingest → uniqueness → interview Qs → trajectory → AI-rated experiences → agent interview) | live |
| `/view/[code]` | **Read-only public profile view** — HTTP, no auth, linked from the public landing. For captured courses: rich splash + per-competency K/U/D depth ratings (`CapturedView`). For un-captured courses: catalog fallback with a live Google Sheet pull (`CatalogFallbackView`, 2026-06-04). | live |
| `GET /view/[code]/okf` | **Portable OKF-v0.1 markdown download** — public; on-demand serialization of the latest captured snapshot as a self-contained OKF-v0.1 markdown file, PII-redacted (same `redactPiiDeep` as `/view`), served as a `.md` attachment. Generated deterministically by `profileToOkfMarkdown` (`lib/okf/profile-to-okf.ts`); Postgres is the source of truth (the file is an always-current projection). Returns 404 when no snapshot exists. | live |
| `/procurement.html` | **Static public explainer for the sibling "ask_procurement" project** (a rule-grounded procurement assistant) — `public/procurement.html`, 607-line standalone page; in `PUBLIC_PREFIXES` so it skips faculty Basic Auth (no faculty data on it). Operator-authored 2026-06-11. Unrelated to the curriculum tool beyond sharing this host. | live |
| `/sme-questions.html` | **Static public SME questionnaire for the sibling "ask_procurement" project** — `public/sme-questions.html`; in `PUBLIC_PREFIXES` (no faculty data). Operator-authored 2026-06-12 (`d370994`), iterated same day (Q2–Q8 revisions, voice dictation, grounded wording — `1ee0b90`…`e24a857`). Same shared-host-only relationship as `/procurement.html`. | live |
| `/api/procurement-intake` | **SME-intake endpoint for the sibling "ask_procurement" project** — `app/api/procurement-intake/route.ts` (stores questionnaire answers + uploads); in `PUBLIC_PREFIXES` (own validation; no faculty data). Operator-authored 2026-06-12 (`33082c4`). Shared-host-only relationship. | live |

The static GitHub-Pages preview at `chiptoe-svg.github.io/gc-curriculum-tool/` serves the docs (vision, specs, plans, deep-dives, faculty guide) plus the legacy interactive partner-interface preview. Submissions and feedback POST to a Google Apps Script Web App which appends to the shared Google Sheet's "Submissions" and "Feedback" tabs.

The `/preview/*` M-trial surface (Course Builder · Prereq Analyzer · Career-Target Alignment) was removed 2026-06-02; everything it did is now covered by `/capture`, `/explore`, and `/program`. Two orphaned components (`TargetChainForm`, `TargetKUDPreview`) and their tests were swept at the same time.

### Agent-facing surfaces — Clemson LAN, bearer-gated

| Route | Surface | Status | Shipped |
| ----- | ------- | ------ | ------- |
| `/api/mcp` | **Wiki MCP server** — exposes **5 read-only tools**: the 3 narrative tools `read_wiki` / `list_wiki` / `search_wiki` (the last with an optional evidence-`bandFloor`) **+ 2 typed-graph tools `coverage_for_target` + `prereq_chain`** (over `getMatrixData()` / `listEdgePairs()`; added 2026-06-11) — `buildCurriculumChatTools()` + `buildCurriculumGraphTools()`. Over MCP Streamable HTTP so any MCP-capable agent on the Clemson internal network can answer narrative AND structural class/curriculum questions. Read-only (`raw/` excluded). **Stateless** transport (`WebStandardStreamableHTTPServerTransport`, fresh server+transport per request). Auth: shared bearer token `WIKI_MCP_TOKEN`, **fail-closed** (unset ⇒ 401 on every request); `/api/mcp` is in `PUBLIC_PREFIXES` so it skips faculty Basic Auth and owns its own auth. Served on `0.0.0.0:3000` (LAN) — **NOT** mapped onto the public Funnel (wiki is private internal analysis). Agents connect with `{ url: "http://<mac-lan-ip>:3000/api/mcp", headers: { Authorization: "Bearer <token>" } }`. Design: [`2026-06-09-wiki-mcp-server-design.md`](./superpowers/specs/2026-06-09-wiki-mcp-server-design.md). Build: `lib/ai/wiki/mcp-server.ts`, `lib/ai/wiki/graph-tools.ts`, `lib/auth/bearer.ts`, `app/api/mcp/route.ts`. | live | 2026-06-09 (5 tools 2026-06-11) |

### Cross-cutting

| Surface | What it does | Status | Shipped |
| ------- | ------------ | ------ | ------- |
| **Faculty dispute flags** (2026-06-12) | ⚑ flag + named note on any AI reading: `/program` matrix cells (cell drawer "Flag this reading", per-cell ⚑ markers) + review-panel competencies (`CompetencyFlagButton`). One polymorphic `faculty_flags` table (migration `0034`) keyed by STABLE ids (course×target×sub-comp for cells; course×statement for competencies) so flags survive re-scores/re-captures; reading-as-disputed frozen in `flagged_context` jsonb → read-time drift ("was D=4 → now D=2") in the "⚑ N open" panel; explicit resolve only (name + required note + date, 409 on double-resolve). Routes `POST/GET /api/flags`, `PATCH /api/flags/[id]` (faculty Basic Auth + slug). Pure logic `lib/program/flags.ts`; queries `lib/db/flag-queries.ts`. Dead M-trial chain swept (ReasoningExpand/PrerequisiteGapPanel/CoverageHeatMap/TargetChainResults + prototype-flag query fns deleted; `FlagDialog` rewritten as the survivor with roster identity). Spec [`2026-06-12-faculty-flag-mechanism-design.md`](./superpowers/specs/2026-06-12-faculty-flag-mechanism-design.md) · plan [`2026-06-12-faculty-flag-mechanism.md`](./superpowers/plans/2026-06-12-faculty-flag-mechanism.md). | live | 2026-06-12 |
| **Prerequisite-edge admin routes** (2026-06-05) | `GET /api/admin/courses/roster` (returns all courses with capture-status and snapshot summary for the `/courses` roster UI); `GET /api/admin/courses/[code]/prereq-edges` (returns the focal course's `prerequisiteEdges` rows with per-edge gap computation). Slug-gated behind `isValidSlug` + faculty Basic Auth. | live | 2026-06-05 |
| **Intended-skills admin route** (2026-06-05) | `POST /api/admin/courses/intended-skills` — runs `intended-skills-extract` for one course (`mode:'one'`, body `{ courseCode }`) or all uncaptured courses (`mode:'all-uncaptured'`); daily-cap gated; upserts into `course_intended_coverage`. Slug-gated + faculty Basic Auth. | live | 2026-06-05 |
| **Dev-server watchdog** (`com.gc.dev-watchdog`, launchd, every 5 min) | Probes `http://127.0.0.1:3000/` for 5xx / connection failure. Three-tier recovery: kickstart → clear `.next` + kickstart → log "manual intervention needed". Logs only on unhealthy detection + recovery actions + once-daily heartbeat. Catches the failure mode where Next is running but returning 500s (Turbopack cache corruption etc.) — launchd's own KeepAlive only catches process crashes. Log at `~/.local/state/gc-curriculum-tool/watchdog.log`. | live | 2026-06-01 |
| **Feedback widget Phase 1 + 2** on every faculty page | Phase 1: header `Feedback` link → modal (name + freeform) → `POST /api/feedback` → GitHub Issue (`gc-feedback` label) with auto-captured route / course / UA / timestamp context. **(Trigger refactor 2026-06-02:** moved from floating bottom-right pill to header text link beside `Hub`/`Guide`/`Settings` so it reads as a real app affordance, not a third-party widget; `<FeedbackLink />` dispatches a `gc:open-feedback` DOM event the globally-mounted `<FeedbackWidget />` listens for.) **Phase 2 (2026-05-30):** launchd cron `com.gc.feedback-cron` (every 15 min) orchestrates `/triage-feedback <N> auto-post` for untriaged issues and `/implement-feedback <N>` for eligible triaged ones (effort-trivial/small without gates, or any effort with `approved`). Hard gates: `gate-prompts-or-schema`, `gate-auth`, `gate-cost`, `gate-anonymous` block auto-implement until operator removes the gate or adds `approved`. Cost interlock: `daily_cost` table lookup before each dispatch, run halts when today's spend ≥ `DAILY_COST_CAP_USD`. PRs prefixed `[bot-<effort>]` for inbox filtering; `@chiptoe-svg`-tagged; never auto-merged. | live | 2026-05-30 |
| **Canvas import — published filter + per-item ignore** (2026-06-02) | `lib/canvas/fetchCanvasCourse.ts` captures `published` on assignments, modules, module items, and quizzes (previously only on pages + discussions); importer tags `[unpublished]` inline in the markdown for every type. `CanvasImportZone` shows a `Skip unpublished items` checkbox (default ON) so faculty don't import the typical draft graveyard. Server-side filter in `/api/courses/[code]/canvas-import` returns an `unpublishedSkipped` count. For published items that turn out to be irrelevant, per-item ignore: `course_materials.ignored_items` JSONB (migration `0026`), `lib/canvas/parseCanvasBlob.ts` splits the concatenated `## Title` blob into items, `effectiveAuditText` drops ignored titles before the AI sees the text (and overrides `useDigest` for Canvas-list materials since the digest doesn't preserve per-item structure). UI: per-Canvas-row disclosure with item checklist. v2 chunker still indexes everything at ingest time → retrieval may surface ignored chunks during agent chat; scoring-time formatMaterials path is filtered correctly. | live | 2026-06-02 |
| **Capture-synthesis D-scoring discipline tightened** (2026-06-02) | Both `capture-scores.md` (v1) and `capture-synthesis.md` (v2) gained a default-low policy (when evidence supports two adjacent depths, pick the lower one), worked calibration examples at the D=2/D=3/D=4 boundary (collaboration % field = D=2, "summarized findings" presentation = D=3, etc.), and a dimensional-consistency pre-check (D≥3 with U≤1 must either downgrade or name the craft-without-articulation pattern in rationale). GC 4440's re-fired profile went 11/15 → 5/15 at D=4 with the new prompt; the remaining D=4s all carry explicit adaptation-to-novel-conditions evidence. | live | 2026-06-02 |
| **Public landing + read-only profile views; whole faculty app on HTTPS funnel** (2026-06-03) | Replaces the spec-broken iframe-bridge for mic. Public HTTP landing at `/` lists every course with View (HTTP, public, read-only `/view/[code]`) and Edit (HTTPS funnel, Basic Auth `/capture/[code]?slug=…`) buttons. Whole faculty app is now reachable via the Tailscale Funnel; Basic Auth in middleware gates everything except `/`, `/view/*`, `/partners/*`, `/preview/*`. Mic works natively because each faculty page is a top-level HTTPS secure context (no iframe). Faculty bookmark the LAN landing; one Basic Auth prompt per browser session when first clicking Edit. Removed: `components/VoiceBridgeProxy.tsx`, `app/voice-bridge/*`, `app/api/voice-session/*`, `lib/voice-session/*`, `lib/rate-limit/slug-rate-limit.ts`. Simplified: `app/api/transcribe/route.ts` (Basic Auth handles auth now), `next.config.ts` (no Permissions-Policy delegation), `lib/auth/basic-auth.ts` (PUBLIC_PREFIXES shuffled). Plan: [`2026-06-03-hybrid-http-https-mic-architecture.md`](./superpowers/plans/2026-06-03-hybrid-http-https-mic-architecture.md). Supersedes [`2026-06-03-microphone-bridge-via-tailscale.md`](./superpowers/plans/2026-06-03-microphone-bridge-via-tailscale.md). | live | 2026-06-03 |
| **MLX-Whisper for YouTube audio transcription** (2026-06-03) | Swapped `whisper.cpp + ggml-medium.en` for `mlx-whisper + whisper-large-v3-turbo` in `lib/youtube/transcribe-audio.ts`. ~2× faster on M4 Max Metal (13-min video: 25s → 12s) with slightly cleaner segmentation. Same shell-out pattern. Env overrides MLX_WHISPER_PATH + MLX_WHISPER_MODEL available. | live | 2026-06-03 |
| **Course emphasis ranking** (2026-06-02) | New `course_emphasis` field on the CaptureProfile schema (nullable array of `{ competency, points, share_pct, centrality }`). The agent walks every graded assignment + rubric criterion, attributes points to whichever competencies each piece evidences, sums, and ranks. Centrality auto-derived: ≥20% central, 5–19% supporting, <5% peripheral. Surfaces in the Review panel as a separate section with bar-chart visualization. Independent of K/U/D depth scoring — emphasis measures what the course weights, depth measures what students can do. Both v1 (capture-scores.md) + v2 (capture-synthesis.md) prompts gained an attribution rule + worked example. Pre-existing profiles render fine (field is nullable). | live | 2026-06-02 |
| **YouTube Whisper fallback** (2026-06-02) | `scan-linked-docs` now falls back to local whisper.cpp transcription when a referenced YouTube video has no English captions (or none at all). Pipeline: `yt-dlp` extracts audio → `ffmpeg` resamples to 16kHz mono → `whisper-cli` with `ggml-medium.en.bin` (Metal GPU on M4 Max, ~1/10 realtime). Free + local, no per-minute API cost. Default 30-min length cap (`WHISPER_MAX_DURATION_SEC` env override). Materials produced this way are tagged `(Whisper)` in the filename so faculty can distinguish caption-derived from audio-derived transcripts. Mac-only — partner Vercel deploy doesn't have yt-dlp or whisper.cpp installed and falls through to the existing "inaccessible" placeholder. Implementation: `lib/youtube/transcribe-audio.ts`. | live | 2026-06-02 |
| **Curriculum chat over the wiki** (2026-06-02) | New `curriculum-chat` AI function (`lib/ai/wiki/{chat,tools,response-schema}.ts` + `lib/ai/prompts/curriculum-chat.md`). Three navigation tools (`read_wiki` / `list_wiki` / `search_wiki`) over the gc-curriculum-wiki repo's narrative layer; agent emits `{ response, citations[] }` per turn (markdown reply + verbatim-excerpt page citations). Surfaced four ways: (1) third mode in `/explore/[code]` (`?tab=ask`) anchored to the focused course, (2) standalone `/ask?slug=…` for program-level Q&A with no anchor, (3) chat panel on `/wiki` below the index, (4) `💬 Ask` link in every faculty header + per-row `💬 Ask` on `/courses` (deep-links into the course's Explore ask tab). The shared `<AskTab>` component (`components/AskTab.tsx`) auto-selects course-anchored vs. standalone endpoints based on whether `courseCode` is set. Plan: [`2026-06-02-curriculum-chat-phase-b-revised.md`](./superpowers/plans/2026-06-02-curriculum-chat-phase-b-revised.md). | live | 2026-06-02 |
| **Adversarial stress-test reviewer** (2026-06-03) | On-demand "Stress-test this profile" button on `ProfileReviewPanel`. Runs a separate heavy-tier `capture-stress-test` agent over the produced profile + the synthesis context (transcript + materials + catalog) and returns per-competency annotations (confidence + concerns + optional suggested adjustments) + profile-level concerns (catalog_vs_evidence, consistency, coverage) + overall assessment. Results are ephemeral — never persisted to the working draft, never baked into snapshots, cleared on any profile edit. Per-competency badges render inline; high-confidence-no-concerns rows render nothing (clean by default). ~$0.05–0.20 per stress-test; counts against the daily cap. Files: `lib/ai/prompts/capture-stress-test.md`, `lib/ai/stress-test/{schema,run}.ts`, `app/api/capture/[code]/stress-test/route.ts`, `app/capture/[code]/{StressTestPanel,StressTestBadge}.tsx`. Plan: [`2026-06-03-adversarial-stress-test-agent.md`](./superpowers/plans/2026-06-03-adversarial-stress-test-agent.md). | live | 2026-06-03 |
| **Vercel/Neon/Resend phaseout — local-only deploy** (2026-06-04) | The hybrid Vercel-side stack retired in one push. (1) `lib/db/client.ts` swapped from `@neondatabase/serverless` → `pg` + `drizzle-orm/node-postgres`. Postgres 17 via Postgres.app on `127.0.0.1:5433`, launchd `com.gc.postgres`. Neon DB dumped + restored row-for-row; row counts matched on every spot-checked table. `middleware.ts` gains `runtime: 'nodejs'` for its partner-session DB lookup. (2) `PARTNERS_BASE_URL` flipped to the Tailscale Funnel — partner survey now served by the Mac. (3) Admin manual-send UI (commit `98c0632`) replaces Resend invites. `lib/email/*` + `resend` package + `@vercel/blob` + `vercel.json` all removed. (4) Backup script switched from libpq's `pg_dump` to Postgres.app's matched-version client; the two Neon-specific tiers (PITR + branch checkpoints) retired with the source. Partner-side voice transcribe at `/api/partners/transcribe?token=` validated via `findPartnerByToken`. Plan: [`2026-06-04-partner-handoff-vercel-phaseout.md`](./superpowers/plans/2026-06-04-partner-handoff-vercel-phaseout.md); supersedes [`2026-06-03-local-canonical-postgres-migration.md`](./superpowers/plans/2026-06-03-local-canonical-postgres-migration.md). Final user-side step: decommission Neon + Vercel projects via their respective dashboards. | live | 2026-06-04 |
| **Position Capture v1** (2026-06-05) | Replaces CareerCapture v1 (prototype, never shipped) + the old 3-step `/submit` wizard with a 6-page partner flow: JD ingest (Docling + LLM `jd-extract`) → uniqueness → interview questions → trajectory → AI-rated experiences (`position-rated-items`) → agent interview (`position-interview-agent`). Ends with synthesis (`position-synthesis`) → an immutable `position_captures` row. Multiple positions per target roll up into a `career_target_kud_aggregate` (v1 = deterministic Markdown stub; AI synthesis deferred). New schema **0029** (renames `career_captures` → `position_captures`, `career_capture_messages` → `position_capture_messages`). Output type: `PositionProfile`. Partner API routes under `/api/partners/[token]/positions/*` (CRUD, extract-jd, upload-doc, generate-items, chat). **Demand captured, not yet compared against course attainment:** sub-competency demand is keyed to `sub_competency_id` on qualifying competencies and Page-5 ratings, but the demand→`snapshot_target_coverage` seam is specified (not yet built) — see the A5 scope note in [`docs/superpowers/specs/2026-06-04-position-capture-v1-prebuild-amendments.md`](./superpowers/specs/2026-06-04-position-capture-v1-prebuild-amendments.md) (Amendment 5); numeric `k/u/d_demand` columns + sufficiency scoring belong to the unified demand/coverage layer (audit step 4). Plan: [`2026-06-04-position-capture-v1.md`](./superpowers/plans/2026-06-04-position-capture-v1.md). | live | 2026-06-05 |
| **CareerCapture v1 — employer interview pipeline** (2026-06-04, **retired**) | ⚠️ Prototype only — one employer tried it and the flow did not work. **Retired by Position Capture v1** (schema 0029 renames its tables; the `/partners/[token]/interview/[targetId]` route is removed). Recorded for history only. Plan: [`2026-06-04-careercapture-v1.md`](./superpowers/plans/2026-06-04-careercapture-v1.md). | retired | 2026-06-04 |
| **Per-instructor capture** (2026-06-03) | Same course code can be taught very differently by different faculty; depth scoring is a function of what students under THAT instructor actually do, not an average. Migration `0027` adds `course_capture_snapshots.instructor_name` + `capture_messages.instructor_name` (nullable text); backfilled all pre-2026-06-03 rows to `'Department canonical'`. Faculty roster in `lib/faculty.ts` (hardcoded; 20 names). Session-start chooser on `/capture/[code]` empty state: "I'm: [dropdown]" + "Start mode: Fresh capture / Build on prior" (only when a prior snapshot exists). Always-visible "Auditor: X · change" badge at the top of the audit panel (mid-session change supported; `getSessionInstructor` is most-recent-wins so the snapshot inherits the active identity). Snapshot creation inherits via `getSessionInstructor(getLatestSessionId(courseCode))`. Display: landing rows show "by [name]" next to date; `/view/[code]` header shows "Captured by [name] · [date]" + "N other captures on file" hint when multi-instructor variance exists. Files: `lib/faculty.ts`, schema + queries + agent (instructor passthrough), `app/capture/[code]/{page,CaptureClient,CaptureChatPanel}.tsx`, `app/{page,view/[code]/page}.tsx`. | live | 2026-06-03 |
| **Citation provenance tightened** (2026-06-03) | Synthesis prompt + Zod schema now reject excerpt-only citations. Every `chunk` citation requires a real `chunkId`; every `instructor` citation requires a UUID or 8-char-hex `messageId` matching the form the transcript exposes via `id=<prefix>`. Synthetic positional ids like `user_3` / `turn_5` fail validation. Closes a class of hallucinated provenance where the synthesizer paraphrased "what it remembered" instead of pointing at a real source. Existing snapshots with synthetic citations remain readable (immutable; not re-validated on load) — `CitationDrawer` gracefully degrades to an "excerpt only" display for those. Files: `lib/ai/capture/schema.ts` (superRefine), `lib/ai/prompts/capture-synthesis.md` (hard provenance rule), `app/capture/[code]/CitationDrawer.tsx` (404 → graceful fallback). | live | 2026-06-03 |
| **Faculty navigation refresh + URL cleanup** (2026-06-03) | Deleted 9 dead API routes from the M-trial era (`/api/flag`, `/api/analyze` + `/analyze/target-chain`, `/api/partners/me` + `/partners/target-options`, `/api/courses/[code]/builder`, `/api/explore/[code]/analyses`, `/api/courses/[code]/kuds/accept`, `/api/courses/[code]/profile` + `/profile/restore/[runId]`) plus their tests (10 test files). Removed `CourseProfileEditor.tsx` + `ProfileRunHistory.tsx` (mounted nowhere). Per-IP rate limit bumped 60 → 600/hr (60 was tripping mid-audit on shared faculty IP across chat + transcribe + saves). VoiceRecorder gained Permissions API pre-flight + actionable error messages (NotAllowedError → "Settings → Websites → Microphone → Allow") + live MM:SS timer + audio preservation across upload failures (single auto-retry + manual Retry button uses the preserved blob; no re-recording needed). | live | 2026-06-03 |

---

### Sibling repos

| Repo | Path on local Mac | Purpose |
| --- | --- | --- |
| [`chiptoe-svg/gc-curriculum-tool`](https://github.com/chiptoe-svg/gc-curriculum-tool) | `/Users/admin/projects/curriculum_developer/` | This repo. The app. |
| [`chiptoe-svg/gc-curriculum-wiki`](https://github.com/chiptoe-svg/gc-curriculum-wiki) | `/Users/admin/projects/gc-curriculum-wiki/` | LLM-maintained markdown corpus (Karpathy wiki pattern). Bootstrapped 2026-06-01 (Task A1 of the wiki+chat plan). Two layers: `raw/` (immutable snapshot JSON + transcripts) and `courses/competencies/targets/concepts/` (LLM-regenerated narrative). Read by the app's `/wiki/...` routes; populated by `wiki-update` (**v1 IMPLEMENTED + wired** — `lib/ai/wiki/update.ts` `updateWikiForSnapshot` + `lib/wiki/git-ops.ts` `writeAndPush`, fired **fire-and-forget** from `app/api/capture/[code]/snapshots/route.ts` on snapshot creation; non-blocking, try/catch-logged). **As-built v1 writes course + concept pages + `index.md` and pushes** (verified: 4 `courses/`, 2 `concepts/`, `index.md`, ~8 commits). **Known gaps vs the 2026-06-04 compile-loop design (which is a v2, not a from-scratch build):** (1) ~~`competencies/` + `targets/` never generate~~ **FIXED + BACK-POPULATED 2026-06-05** — root cause was the *monolithic* `updateWikiForSnapshot` (one strict-mode JSON for all ~37 affected pages) stalling indefinitely at full coverage, which would equally hang the post-coverage re-fire; fixed with bounded-batch page generation (`WIKI_PAGES_PER_CALL=6`, backward-compatible). 30 `competencies/` + 5 `targets/` pages now live (cross-course rollups, wiki commit `5b066f6`, via `scripts/_one-off/backpopulate-wiki.ts`); (2) concepts are hardcoded (`productive-failure` conditional, `three-act-structure` always) — no ≥2-source promotion gate; (3) no lint pass; (4) trigger is per-snapshot fire-and-forget, not the design's watermark/diff + `/refresh-wiki` reconcile. |
| [`chiptoe-svg/gc-curriculum-backups`](https://github.com/chiptoe-svg/gc-curriculum-backups) | `/Users/admin/projects/gc-curriculum-backups/` | Off-site weekly `pg_dump` backups. Tier 3 of the 4-tier backup chain. |

## Architecture (at-a-glance)

**Local-only deploy (post-2026-06-04 cutover).** The Vercel/Neon/Resend trio is gone; one box, one DB:

- **Mac (only).** Postgres 17 on `127.0.0.1:5433`, db `gc_curriculum`, launchd `com.gc.postgres`. Next.js bound 0.0.0.0:3000 served to LAN; exposed to the public internet via Tailscale Funnel at `https://admins-mac-studio-2.tailb723c1.ts.net` for the partner-survey path. Middleware runs in Node runtime (`runtime: 'nodejs'`) so it can use `node-postgres` for the per-request partner-session DB lookup.
- **Auth model.** Faculty surfaces gated by HTTP Basic Auth in middleware (`FACULTY_BASIC_AUTH` = full access; `CREATE_ONLY_AUTH` = create-only — 2026-06-13 two-tier auth on branch `feat/two-tier-faculty-auth`: `resolveRole`/`creatorAllowed` in `lib/auth/basic-auth.ts`, default-deny allowlist in middleware, bulk-guard in the roster route, role-aware `/courses/new` confirmation). Partner surfaces gated by URL-bearer magic tokens (validated against `partners.magic_token`). `/`, `/view/*`, `/partners/*`, `/api/partners/*`, `/api/mcp`, `/procurement.html`, `/sme-questions.html`, and `/api/procurement-intake` are public via `PUBLIC_PREFIXES`. Spec: `docs/superpowers/specs/2026-06-13-two-tier-faculty-auth-design.md` · plan: `docs/superpowers/plans/2026-06-13-two-tier-faculty-auth.md`. **Exception (2026-06-12): `/api/transcribe` is excluded from the middleware MATCHER entirely** — Node-middleware body buffering (`middlewareClientMaxBodySize`) broke real browser mic uploads ("Response body object should not be disturbed or locked" pre-handler, always right after a streamed chat turn on the same kept-alive connection; curl couldn't reproduce). The route enforces Basic Auth itself via `authorizedForBasicAuth` (+ slug + IP-rate + daily-cap) — protection unchanged, body proxying gone. Transcribe failures now log timestamp/name/stack (top-level catch); launchd log moved to `~/.local/state/gc-curriculum-tool/next.log` (was volatile `/tmp/curriculum-next.log`).
- **Partner survey distribution.** Faculty hand-copy magic-link URLs from `/admin/partners` and email them externally (no Resend dependency); the survey runs on the funnel — same Mac, same DB, no Vercel.

Setup details: [`docs/superpowers/running-locally.md`](./superpowers/running-locally.md). Hybrid-deploy rationale + open items: [`docs/superpowers/plans/2026-05-25-phase2-hybrid-deploy.md`](./superpowers/plans/2026-05-25-phase2-hybrid-deploy.md).

### AI provider + function tiers

- **Provider abstraction** at `lib/ai/provider.ts`: `getProvider()` returns OpenAI / Anthropic / Local / Campus / Fake. Structured output via OpenAI strict JSON-schema + Zod parse. **New as of 2026-05-26 (CourseCapture v2 Stage 1):** `completeWithTools` method on all five providers, built on Vercel AI SDK v6 — used by Stage 3's agent loop (not exposed by any AI function yet).
- **Campus provider** at `lib/ai/campus.ts` (added 2026-05-26): OpenAI-compatible client against the Clemson RCD endpoint `https://llm.rcd.clemson.edu/v1`. Default model `qwen3.6-35b-a3b-fp8` — MoE with 3B active params, ~180 tok/s on the shared cluster. Initial default was `glm-5.1-fp8` (754B) but a 2026-05-26 latency probe found GLM throughput at ~4 tok/s makes hot-path calls unworkable; GLM and `deepseek-v4-pro` (1M ctx, ~15 tok/s) stay reachable via per-call model override when reasoning depth justifies the latency. Cost reported as 0. Selected with `AI_PROVIDER=campus`. The campus branch in `buildProvider` lets `CAMPUS_LLM_DEFAULT_MODEL` win over DB-stored function-settings model names (which point at OpenAI models that don't exist on the campus endpoint); the full provider-aware tier refactor is still deferred — see "Deferred / debt".
- **Embeddings client** at `lib/ai/embeddings.ts` (added 2026-05-26): hits `/v1/embeddings` on the same campus endpoint with `qwen3-embedding-4b` (2560-dim). Same env vars as the campus provider (`CAMPUS_LLM_BASE_URL` / `CAMPUS_LLM_API_KEY`) regardless of which generation provider is active. Includes an `InMemoryVectorStore` used to exercise the chunk → embed → search path before Weaviate lands; Stage 2 will swap that store for Weaviate while keeping `embedBatch` / `embedText` unchanged.
- **Function tier system** at `lib/ai/function-settings.ts`: 24 named function IDs. **+3 added 2026-06-11** when their live scorers migrated off the env-default `getProvider()` to `getProviderForFunction(id)`: `parse-profile-fields`, `extract-course-kud` (the kuds/generate path), `synthesize-target` (admin synthesis orchestrator) — all default tier, preserving their prior effective model. Tier (light / default / heavy) maps to a model; per-function override stored in `ai_function_settings` table. 60s TTL resolver cache.

| Function ID | Default tier | Note |
| ----------- | ------------ | ---- |
| `capture-chat` | default | Bumped from light when audit bundle outgrew gpt-5.4-mini's 272k input cap |
| `capture-scores` | default | |
| `materials-analysis` | default | |
| `explore-draft-target` | default | |
| `explore-compare` | default | |
| `explore-what-if` | default | |
| `program-score-coverage` | heavy | Heaviest scorer; batches by target for cache reuse |
| `decompose-prereq-gap` | default | |
| `material-digest` | light | Per-material structured digest generated at extraction for every material (supersedes `material-summary`); loaded into audit agent's at-rest context |
| `chunk-contextualize` | light | Per-chunk position blurb prepended before embedding so the vector encodes position + content (Anthropic contextual-retrieval pattern) |
| `ingestion-checkin` | light | Per-page-open curation review; one call before audit chat begins; silent most of the time |
| `capture-chat-agent` | default | Stage 3 audit-chat per-turn loop; tool-using retrieval against Weaviate per-course tenants; returns structured finding + question + citations + readiness |
| `wiki-update` | heavy | Wiki narrative updater (v1 wired fire-and-forget on snapshot creation; writes course + concept pages + index). Tier is `heavy` in `function-settings.ts` |
| `curriculum-chat` | default | Curriculum chat over the wiki; tool-using navigation + verbatim-excerpt citations |
| `capture-stress-test` | heavy | Adversarial reviewer over a produced profile; on-demand only |
| `jd-extract` | light | One-shot extraction of structured fields from a JD (Docling markdown or pasted text); per-field confidence scores |
| `position-rated-items` | default | Generates 10 "experiences worth having" candidates from pages 1–4 inputs + career-target sub-competencies |
| `position-interview-agent` | default | Per-turn interview agent for page 6 of Position Capture; anchor-probe-confirm posture using pages 1–5 context |
| `position-synthesis` | default | Synthesis over a completed page-6 interview transcript + upstream inputs; produces a structured `PositionProfile` |
| `prereq-edge-seed` | default | One-shot extraction of skill-tagged prerequisite edges from `courses.prerequisites` free text + incoming-expectations; returns `(prereqCode, subCompetencyId, expectedK, expectedU, expectedD)` rows for faculty to confirm or edit |
| `intended-skills-extract` | default | One-shot extraction of intended (syllabus-asserted) K/U/D per sub-competency from catalog fields (`description`/`learningObjectives`/`majorProjects`/`skillsRequired`); stored in `course_intended_coverage` (migration `0031_clumsy_selene.sql`), banded `claimed`, never merged into measured attainment |
| `reconcile-feedback` | default | Per-section reconciliation in the guided faculty-reconciliation `reconcile` stage: takes one section of the profile (apparent outcomes / incoming expectations / outgoing KUDs) plus faculty's conversational feedback and PROPOSES per-item edits; faculty accept/tweak/reject before `applyReconciliation` writes them. Cost-capped. Route: `POST /api/capture/[code]/reconcile` |

**Reference-material compression / ingestion** (`lib/capture/material-compression.ts`, `lib/capture/finalize-extraction.ts`, `lib/ai/analyze/material-digest.ts`): every extraction call site routes through `finalizeExtraction`. Legacy path (no `COURSECAPTURE_V2_INGESTION`): long reference materials (≥15k tokens, Canvas File / Drive PDF / YouTube / plain upload) get a digest via `generateMaterialDigest`; Canvas dense kinds and Google Workspace materials are skipped. `effectiveAuditText(m)` substitutes the digest in the audit prompt when `useDigest` is true. Faculty toggle per row from the Materials panel; one-time backfill via `POST /api/courses/[code]/materials/compress`. V2 path (`COURSECAPTURE_V2_INGESTION=1`): FERPA detection → materials policy → digest every material → chunk + contextualize + embed + upsert into vector store. New modules shipped in Stage 2a: `lib/capture/chunker.ts`, `lib/capture/ferpa-detect.ts`, `lib/capture/materials-policy.ts`, `lib/capture/vector-store.ts` (in-memory backend; Weaviate adapter pending Stage 2b). `material-summary` was superseded by `material-digest` and removed in Stage 2a.

**Spreadsheet handling refined 2026-05-28.** xlsx/xls/xlsm files no longer auto-excluded — `lib/capture/materials-policy.ts` narrowed to gradebook-shaped filenames (`gradebook`, `grades`, `attendance`, `roster`, `scores`, `enrol(l)ment`) with `ferpaRisk: 'high'` on those matches. Docling's xlsx → markdown output is post-processed through `compactSpreadsheetMarkdown` (`lib/capture/spreadsheet-compact.ts`) to drop empty rows, drop empty columns, drop degenerate tables, AND strip inline base64 image data URIs (the dominant token cause on real workbooks with embedded charts). DoclingExtractor also passes `include_images=false` for xlsx inputs as upstream defense. Legacy `.xls` flows through the same code path via `legacy-converter.ts`'s LibreOffice upgrade. End-to-end smoke on GC 4800's `4800_budget_2025.xlsx`: 522k chars / 131k tokens → 12.8k chars / 3.2k tokens.

**Trial-readiness fixes shipped 2026-05-28** (post-Spreadsheet, alongside the AI_PROVIDER flip to openai):

- **Audit-chat opening turn.** `runAuditAgent` self-introduces from at-rest context when history is empty — no fake user row written; the auditor's first 3-paragraph reply is the first transcript entry. Closes the Stage-6 regression where Start session POST'd `{messages: []}` and the v2 chat route 400'd ("no user message in history"). Implementation: `userMessage` is now optional on `AuditAgentInput`; chat route forwards it conditionally; the agent appends a synthetic "begin per conversation rules" instruction to the LLM call only (not persisted).
- **Ingestion check-in catalog-richness signals + post-filter.** `/api/courses/[code]/checkin` now passes `context.catalogCoversSyllabus`, `context.hasCanvasAssignments`, and `context.canvasSyllabusSetAside` to the LLM directly (Rule 1 in `ingestion-checkin.md` references them when deciding whether to flag missing syllabus / rubrics). A post-filter on the LLM's `highlights[]` remains as belt-and-suspenders for models that ignore the context signals. Other LLM signals (FERPA, stacked auto-set-asides, near-empty digests) unchanged.
- **In-page help affordances.** `/capture/[code]` gains `CaptureHelpPanel` (collapsed-by-default "How this page works" explaining Materials indicators, top buttons, audit mode, conversation flow, behavioral expectations) and `CanvasImportSummary` (per-category present/absent for the 6 Canvas resource types + parsed per-assignment table with rubric ✓/— column).
- **"Why ignored" callout generalized.** `MaterialsPanel` now renders the reason for every ignored row (not just `autoSetAside` ones) with a fallback chain: `setAsideReason` → Canvas-Syllabus-aware default → "manually toggled off by the faculty reviewer." "Include anyway" button stays gated on `autoSetAside`.
- **Daily-cost panel on `/settings`.** Today's spend vs. `DAILY_COST_CAP_USD` cap with a tinted progress bar (green / amber / red), plus a 14-day sparkline. New helpers `getDailyCapCents` + `getDailyCostHistory(days)` on `lib/rate-limit/daily-cap.ts`. `DAILY_COST_CAP_USD` raised from `5` to `10` in the Mac `.env.local`.
- **AI_PROVIDER flipped to openai** on the Mac local deploy (`.env.local`, not committed). All AI functions now use the OpenAI tier mapping (default → `gpt-5.4`, light → `gpt-5.4-mini`, heavy → `gpt-5.5`). Campus-side is preserved as a one-line revert.

**OpenAI strict-mode JSON-schema sweep 2026-05-28** (commits `3a88850`, `342fc8d`). OpenAI's strict structured-output validator requires every property in `properties` to also appear in `required`; optional fields must be encoded as nullable union types. The Campus Qwen endpoint was tolerant of the absent-field encoding, masking the bug until provider flipped. Two files needed fixes:

- `lib/ai/agent/audit-response-schema.ts` — `chunkId` / `messageId` on citations made nullable + added to required.
- `lib/ai/analyze/capture-scores.ts` — `CITATIONS_ARRAY` (used at four sites) same fix; `source` + `citations` (Stage-4 additions) added to `required` at competencies / incoming_expectations / verification_summary / audit_notes; V2 schema variant's removal of `productive_failure_conditions` from required replaced with a nullable type widening (model emits null when Audit Area 7 wasn't probed).
- `lib/ai/synthesis/schema.ts` — `salaryDistribution.{p25,p50,p75}` and `proposedKUDEdits.targetDescriptorIndex` made nullable + added to required.
- Matching Zod `.nullable().optional()` updates in `lib/ai/capture/schema.ts` and `lib/ai/synthesis/schema.ts`.

An audit of all 36 strict-mode schemas in the codebase confirmed the remaining 33 were already strict-compliant — these were the only Stage-4-era additions that left optional fields outside `required`.

### Schema (local Postgres 17 via Drizzle)

Latest migration: **`0042_groovy_scarlet_spider.sql`** (2026-06-18 — `course_materials.tier` nullable text column; ingestion depth tier `'high'|'middle'|'background'`, null until the tiered-ingestion classifier runs; additive, no default; applied 2026-06-18. `mapMaterialRow` fixture updated for tsc). Preceded by **`0041`** (2026-06-15 — `sandbox_grants.course_code` made nullable, per external-access scoped magic-link arc); preceded by **`0040`** (2026-06-15 — `sandbox_grants` + `sandbox_sessions` tables for external-access scoped magic-link); preceded by **`0039`** (2026-06-15 — `courses.scope` + `courses.status` columns, course-scope-lifecycle foundation); preceded by **`0038`** (2026-06-14 — `reconciliation_log` JSONB nullable column on `course_capture_snapshots`; additive; stores the guided-reconciliation transcript (per-section proposals, faculty accept/tweak/reject decisions, and final applied edits) for audit-trail purposes; applied 2026-06-14. Preceded by **`0037`** (2026-06-13 — structured course identity + lecture/lab bundling: adds identity columns `prefix` (text), `course_number` (int, nullable), `number_suffix` (text) to `courses`; new `course_codes` child table (`course_code` FK → `courses.code`, `paired_code` unique, `role` lecture/lab/other, `canvas_course_name` + `canvas_imported_at` provenance); `course_materials.source_code` (nullable provenance tag — null ⇒ primary code). Applied 2026-06-13. Backfill parses all existing `courses.code` values via the canonical `parseCourseCode` parser into the three identity columns.); preceded by **`0036_petite_impossible_man.sql`** (2026-06-13 — int4-overflow hardening: widens all six `*_cents` cost columns `integer`→`bigint` — `prototype_runs`, `daily_cost.total_cost_usd_cents` ← the ever-growing accumulator, `synthesis_runs`, `course_materials.analysis_cost_usd_cents`, `course_profile_runs`, `course_kud_runs`. Pure `ALTER COLUMN … TYPE bigint`, applied 2026-06-13. Paired code hardening, same commit, no schema change: `sanitizeCents` clamps `recordSpend` against non-finite/negative/sentinel cost values; `replaceIntendedCoverage` sanitizes intended K/U/D to 0–5-or-null + SAVEPOINT per-row fallback so a poison row can't abort the whole batch; `embedBatch` now splits inputs under a conservative char budget for the campus qwen3-embedding-4b 32k-token/request cap instead of one-shotting); preceded by **`0035_milky_weapon_omega.sql`** (2026-06-12 — Canvas import provenance: `courses.canvas_course_name` + `canvas_imported_at` nullable, 8 courses backfilled, applied 2026-06-12); preceded by **`0034_pale_major_mapleleaf.sql`** (2026-06-12 — Faculty dispute flags: `flag_target_kind` + `flag_status` enums + `faculty_flags` table, purely additive, applied 2026-06-12); preceded by **`0033_married_infant_terrible.sql`** (2026-06-11 — Course categories + career flag: adds `course_category` enum (`gc_core|specialty|major_req|other`) + three `courses` columns `category` (default `other`), `builds_to_career` (boolean, default `false`), `catalog_url` (nullable), then backfills the 46 courses — 16 GC Core + 14 Specialty + 16 Major Req; `builds_to_career=true` for 27 (16 Core + 11 high-enrollment Major Req). **Applied 2026-06-11** — `pnpm db:migrate` (run with `DATABASE_URL` from `.env.local`; the config has no dotenv loader) applied it AND, via the created_at watermark, the previously-held inert `0032` (`career_target_demand`, now exists empty, still `DEMAND_COVERAGE_SEAM`-gated). Anti-drift test (`course-category-migration.test.ts`) pins the backfill SQL to the canonical `lib/db/course-category-seed.ts`); preceded by **`0032_slimy_mister_fear.sql`** (2026-06-07 — Demand→coverage seam: new `career_target_demand` table, per-`(careerTargetId, subCompetencyId)` partner-weighted employer demand `k/u/d_demand` real nullable + `contributing_position_ids`. ⚠️ **Generated offline (`db:generate`) but NOT applied** — on branch `audit-remediation-2026-06-07`, behind `DEMAND_COVERAGE_SEAM`; `pnpm db:migrate` to apply when activating); preceded by **`0031_clumsy_selene.sql`** (2026-06-05 — Intended-skills rough pass: new `course_intended_coverage` table storing syllabus-asserted K/U/D per sub-competency, banded `claimed`, seeded by `intended-skills-extract`); preceded by **`0030_supreme_iron_monger.sql`** (2026-06-05 — Prerequisite edges: new `prerequisite_edges` table with columns `focal_course_code`, `prereq_course_code`, `sub_competency_id`, `expected_k`, `expected_u`, `expected_d`, `basis` (`measured|intended|none`), `seeded_at`, `confirmed_at`, `confirmed_by`); preceded by **`0029_lean_silver_centurion.sql`** (2026-06-05 — Position Capture v1: renames `career_captures` → `position_captures`, `career_capture_messages` → `position_capture_messages`, adds the draft→submitted lifecycle columns (`status`/`company`/`position_title`/`structured_inputs`/`rated_skills`/`source_files`/`supersedes`/`completeness`/`submitted_at`) + the new `career_target_kud_aggregate` table. NOTE: the `sub_competency_id` join key and demand discipline (A1/A2/A4) live INSIDE the `profile`/`rated_skills` JSONB — no demand DB columns; numeric `k/u/d_demand` columns are deferred to audit step 4 per A5); preceded by **`0028_medical_stick.sql`** (2026-06-04 — CareerCapture v1: `career_capture_messages` + `career_captures`); preceded by `0027_opposite_black_bird.sql` (2026-06-03 — per-instructor capture: `instructor_name` on `course_capture_snapshots` + `capture_messages`) and `0026_add_ignored_items.sql` (2026-06-02 — `course_materials.ignored_items jsonb` for per-Canvas-item ignore lists; itself preceded by `0025_great_sentry.sql` `reviewer_note` and `0024_noisy_omega_flight.sql` `summary*` → `digest*` + FERPA / indexing columns).

Tables defined in [`lib/db/schema.ts`](../lib/db/schema.ts):

- **Career target framework:** `careerTargets`, `subCompetencies`, `prototypeTargetEdits`
- **Courses + catalog:** `courses` (with `builder_status` enum: `draft | profile_complete | kuds_generated | approved`; **+ `category` (`course_category` enum, display bucket), `builds_to_career` (boolean — single source of truth for career-coverage inclusion; decoupled from category), `catalog_url` (nullable Clemson catalog link) — migration `0033`**; **+ `prefix`, `course_number` (int nullable), `number_suffix` identity columns — migration `0037`**), `sheetSyncState`
- **`course_codes`** (paired lecture/lab codes for a primary course; `course_code` FK → `courses.code`, `paired_code` unique, `role` lecture/lab/other, Canvas provenance columns. NOT a `courses` row — the paired code is a secondary identity on the same catalog entry, not a separately-navigable course. Added migration `0037`.)
- **`course_materials.source_code`** (nullable provenance tag on course materials: null ⇒ primary code, non-null ⇒ the paired code whose Canvas import produced this material. Added migration `0037`.)
- **Course profile (M-trial):** `courseProfiles`, `courseProfileRuns`, `courseMaterials`, `courseKuds`, `courseKudRuns`, `coverageScores`
- **CourseCapture v1:** `courseCaptureProfiles` (mutable draft), `courseCaptureSnapshots` (immutable versioned + new `transcript_session_id` linking to v2 sessions), `captureConversations` (legacy; preserved as-is, no new writes after Stage 3 cutover)
- **CourseCapture v2 Foundation:** `captureMessages` (append-only conversation log keyed by `session_id` + `turn_index`, UNIQUE-constrained; replaces session-overwriting `captureConversations`)
- **Course-level toggle:** `courses.audit_mode` (`'full'` | `'simple'`, default `'full'`; UI lands in Stage 2)
- **Explore v1:** `courseExploreTargets`, `courseExploreAnalyses`, `courseExploreWhatIfs`
- **Phase 1A coverage:** `snapshotTargetCoverage` (composite PK: `snapshotId × careerTargetId × subCompetencyId`)
- **Partners:** `partners`, `partnerSessions`, `partnerEvents`, `partnerSubmissions`, `synthesisRuns`
- **AI settings + telemetry:** `aiFunctionSettings`, `prototypeRuns`, `prototypeFlags`
- **Position Capture v1:** `positionCaptures` (immutable per-position profile), `positionCaptureMessages` (per-turn interview log), `careerTargetKudAggregate` (roll-up per target; v1 = deterministic Markdown stub)
- **Demand→coverage seam (branch, build-ahead):** `careerTargetDemand` (per `(careerTargetId, subCompetencyId)` partner-weighted numeric demand; migration `0032`, **not applied**; gated by `DEMAND_COVERAGE_SEAM`)
- **Prerequisite edges:** `prerequisiteEdges` (focal × prereq course pair, skill-tagged, basis-stamped)
- **Intended coverage:** `courseIntendedCoverage` (syllabus-asserted K/U/D per course × sub-competency, evidence-ladder `claimed`, never merged into measured attainment)
- **Faculty dispute flags:** `facultyFlags` (polymorphic; stable-key; explicit-resolve)
- **Rate-limit / cost guard:** `dailyCost`, `ipHourly`

### Prompt library

26 `.md` files in `lib/ai/prompts/` (11 removed 2026-06-11: `draft-outcomes.md` + `score-coverage.md` (dead chains); `capture-chat.md` + `capture-scores.md` (v1 capture-path retirement); and 7 with the M-trial unreachable-scorer cleanup — `extract-course-prereqs`, `suggest-prerequisites`, `analyze-prerequisite-gaps`, `draft-course-outcomes`, `evaluate-scaffolding`, `score-prior-coverage`, `analyze-course-gaps`; `kud-chat.md` is MOUNTED, not dead). 22 active system prompts carry the `manning_skills:` frontmatter contract; shared partials in `lib/ai/prompts/shared/` (notably `depth-scale.md` — the authoritative KUD+ rubric — and `kud-rubric.md`). 16/22 Manning-encoded; the remaining 6 are pure I/O glue or deliberately deferred (ratio last verified 2026-05-25). See [`docs/superpowers/plans/2026-05-25-manning-encoding-backfill.md`](./superpowers/plans/2026-05-25-manning-encoding-backfill.md).

### Env vars

`.env.example` lists the full surface. Categories:

- **DB:** `DATABASE_URL`
- **AI:** `AI_PROVIDER`, `OPENAI_API_KEY`, `OPENAI_MODEL`, `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`, `LOCAL_BASE_URL`, `LOCAL_API_KEY`, `LOCAL_MODEL`, `CAMPUS_LLM_BASE_URL`, `CAMPUS_LLM_API_KEY`, `CAMPUS_LLM_DEFAULT_MODEL`, `COURSECAPTURE_V2_INGESTION`
- **Vector store:** `VECTOR_STORE` (`'in-memory'` default | `'weaviate'` — selects the backend for the v2 ingestion pipeline's chunk storage), `WEAVIATE_URL`, `WEAVIATE_GRPC_URL`, `WEAVIATE_API_KEY`, `WEAVIATE_TENANT_PREFIX`
- **PDF:** `PDF_PARSER`, `DOCLING_URL`, `DOCLING_VLM_*`
- **Auth / slug:** `FACULTY_BASIC_AUTH` (faculty gate on local), `CREATE_ONLY_AUTH` (create-only faculty credential — 2026-06-13 two-tier auth; when set, this credential may add a single course via `/courses/new` + the single-add roster API but receives 403 on every edit surface; when unset, only `FACULTY_BASIC_AUTH` exists — single-tier behavior preserved), `WIKI_MCP_TOKEN` (shared bearer token for the `/api/mcp` agent endpoint — fail-closed: unset ⇒ 401), `PROTOTYPE_SLUG` (single-user slug-gated session), `ADMIN_TOKEN` (optional, 2026-06-11 — admin-route dual-accept Bearer second factor; when unset, `checkAdminAuth` still accepts the slug in the `Authorization` header or the legacy `?slug=`)
- **Feedback intake:** `GITHUB_TOKEN`, `GITHUB_FEEDBACK_REPO` (set on the local Mac; when unset, `/api/feedback` returns 503)
- **Transcription (branch):** `WHISPER_BACKEND` (`openai` forces the paid hop), `WHISPER_OPENAI_FALLBACK` (must be `1` to allow the missing-local-binary → OpenAI fallback; otherwise transcription fails closed rather than shipping mic audio off-box — FERPA)
- **Demand→coverage seam (branch):** `DEMAND_COVERAGE_SEAM` (`1` shows the per-target sufficiency panel + populates `career_target_demand`; requires migration `0032` applied first; off by default)
- **Cost guard:** `DAILY_COST_CAP_USD`, `COST_ALERT_EMAIL`
- **Sheets / partners:** `GOOGLE_SHEET_ID`, `PARTNERS_BASE_URL`, `SYNTHESIS_STALENESS_THRESHOLD` (the dead `RESEND_*` / `BLOB_READ_WRITE_TOKEN` / `NEON_*` vars were swept from `.env.example` 2026-06-05; the example `DATABASE_URL` now points at local Postgres and `PARTNERS_BASE_URL` at the Funnel)
- **Local material storage:** `~/Library/Application Support/gc-curriculum-tool/materials/` served via `/api/storage/materials/<key>` (was Vercel Blob until 2026-06-04)

---

**Course Overview + editorial profile + `/courses` landing shipped 2026-05-31**: `CaptureProfile.overview` (nullable; `narrative` + `at_a_glance` bullets + `who_for` + `arc` + source/citations) drafted by the LLM at synthesis time per `capture-scores.md`. Surfaced as the front matter of `ProfileReviewPanel` via the new `<CourseOverview>` component — Fraunces serif display + narrative with drop-cap, DM Sans bullets with em-dash leaders, small-caps marginalia on the who-it's-for / semester-arc sidebar; click any section to edit in place (blur to save, no per-section save buttons). Three editorial fonts loaded once via `next/font/google` (Fraunces / DM Sans / IBM Plex Mono) and exposed as Tailwind v4 `font-display` / `font-body-sans` / `font-mono-plex` utilities — opt-in to the new surfaces, default sans on existing surfaces untouched. "Confirm and snapshot" renamed to **"Capture this profile"** (CTA visually promoted to high-contrast); semantics unchanged (still creates the immutable snapshot). New `/courses` route renders a typeset index grouped by course level: code (mono) · title (serif) · status pill · last-captured date · → arrow, with header counters and a `listCoursesWithStatus()` helper that derives the 5-rung status ladder from a single pass over courses + profiles + non-retired snapshots + recent capture_messages (24h window for in-audit detection).

## Wiki-readiness substrate

Data already captured that a future curriculum wiki (Karpathy-pattern markdown + bidirectional links, browsable in-app or in Obsidian) could synthesize over without further capture work:

- **Stable entity slugs.** `careerTargets.id` and `subCompetencies.id` are human-readable text PKs ("production-operations", "brand-strategy"). Future `[[wikilinks]]` resolve directly — no migration needed.
- **Immutable raw layer.** `course_capture_snapshots` (per-audit profile JSON, frozen), `capture_messages` (append-only audit transcript with tool calls + citations), `course_materials` (extracted text + digests + chunked vectors in Weaviate per-course tenants).
- **Per-finding provenance.** Stage 4 source flags (`instructor` / `materials` / `inferred`) + citations linking back to specific chunks or instructor turns.
- **Departmental narrative.** `reviewerNote` on `courseCaptureProfiles` is surfaced in the Review panel's "Departmental context" textarea (2026-05-30) and persisted into snapshots via the `reviewer_note` column added in migration `0025_great_sentry.sql` — the "why" behind overrides + decisions survives in the immutable record.
- **Cross-course observations.** The audit prompt probes both prereq direction (Audit Area 1) and downstream direction (Audit Area 1b, added 2026-05-30). The graph emerges from these prose exchanges in `capture_messages`; no normalized cross-course table is needed.

Anything not listed here that a wiki would want — edits with rationale, concept-level descriptors, cross-snapshot evolution diffs — is derivable from the raw layer at wiki-build time. No further capture infrastructure is blocking a wiki layer; it can be added (Option A: rendered view from Postgres; Option B: git-tracked markdown corpus; Option C: + MCP server) when faculty value warrants the build. See [`docs/superpowers/plans/2026-05-30-wiki-readiness-substrate.md`](./superpowers/plans/2026-05-30-wiki-readiness-substrate.md).

---

## Active arc

**Faculty trial period.** The trial period started with the M-trial prototype and continues now across CourseCapture + Explore + Program. The point hasn't changed: confirm that the analysis is good enough to be useful before building the rest of Phase 1.

**Structured course identity + lecture/lab bundling — SHIPPED 2026-06-13 (branch `feat/course-identity-bundling`).** Refined Option 3: identity columns (`prefix`/`course_number`/`number_suffix`) on `courses`, new `course_codes` child table for paired lecture/lab codes (NOT a separate `courses` row), `course_materials.source_code` provenance tag. `formatCourseLabel(code, pairedCodes)` renders bundled labels ("GC 3460/3461" shared-prefix; "GC 3460 + XX 1234" differing-prefix) across all four surfaces: public landing `/`, `/courses` list, capture header, and `/program` matrix. Capture pipeline + Weaviate tenant + synthesis deliberately frozen (no structural change to scoring). Spec: [`docs/superpowers/specs/2026-06-13-structured-course-identity-and-bundling-design.md`](./superpowers/specs/2026-06-13-structured-course-identity-and-bundling-design.md).

**CourseCapture entry refinements — SHIPPED + DEPLOYED 2026-06-11 (`7c29626`).** A live walkthrough of the Step-1 gate surfaced four issues, all fixed: **(1) Header trim** — the capture header dropped the `· v1` tag + `Program`/`Settings`/`💬 Ask`/`Explore →` and gained a **Course List** link (it had none); now just **Course List · Guide ↗ · Feedback**. **(2) GC-curriculum-sheet catalog source** — Step 1 now shows the catalog the auditor actually reads (the `# Course catalog` block from `audit-agent.ts`) as a distinct row: "GC curriculum sheet" badge + `catalogContributionSummary` (non-empty fields) + "synced *X ago*" + a **Re-sync** button (`POST /api/courses/[code]/sync-from-sheet`, updates in place; 404-aware for tab-less courses). **(3) Honest + actionable status** — material status now uses `materialReadability` (`pending`→"not indexed yet", `failed`/`skipped`→"not readable" + reason like unshared-doc/no-captions); an **Index now** button (shown when `hasFixablyUnindexed`) runs `POST /api/admin/v2-backfill` from the faculty surface (same Basic Auth tier) and refreshes via the new shared `lib/capture/fetch-course-materials.ts` (extracted from `MaterialsPanel.refetchMaterialsFromContext`). Primary button relabeled "+ Add or import materials" (Canvas import/update stays reachable). **(4) Canvas-list unroll** — Canvas list rows (e.g. "Canvas: Assignments") expand to their item titles via the existing `parseCanvasBlob`. New pure helpers in `material-display.ts` (`catalogContributionSummary`/`materialReadability`/`relativeTimeFromNow`/`hasFixablyUnindexed`, unit-tested). No schema/data-model change. Built via 6-task subagent-driven development. Spec [`2026-06-11-capture-entry-refinements-design.md`](./superpowers/specs/2026-06-11-capture-entry-refinements-design.md) · plan [`2026-06-11-capture-entry-refinements.md`](./superpowers/plans/2026-06-11-capture-entry-refinements.md). Suite **973** green, `tsc` clean.

**Indexing backlog backfilled (one-time op, 2026-06-11).** A live walkthrough revealed setup-era (2026-05-21) Canvas/linked materials stuck `indexing_status='pending'` — imported + text-extracted but never embedded into Weaviate, so the audit retrieval couldn't read them. Ran `POST /api/admin/v2-backfill` for the 5 affected courses (GC 1010, 1040, 3400, 3800, 4800): **`ready` 72 → 89**. The ~11 that remain `pending` are genuinely unindexable — ignored syllabi, caption-less YouTube, and Google Docs/Slides not shared ("anyone with link" off) — i.e. **source-side fixes** (sharing/captions), not a pipeline bug. The new Step-1 readability labels now surface exactly this.

**CourseCapture Step 1 materials-confirmation gate — SHIPPED + DEPLOYED 2026-06-11 (`f6ee29a`).** Fixes a real UX failure surfaced by the department head during a live capture: the 2026-06-10 goal-first landing buried the materials tray behind a small "⚙ Materials…" disclosure at the bottom and pushed the user straight into "Start the interview" — so a new user had no idea what materials were loaded or where they came from, despite corpus-confirmation being the most evidence-critical step. Now a genuinely fresh audit (`stage==='chat' && messages.length===0`) opens on a **Step 1 "Confirm materials"** screen before the interview: a clean per-row list (filename · provenance badge **Canvas / uploaded / linked doc** derived from the fileName prefix · ready/indexing status) with a "+ Add a material" / "⚙ Manage materials in detail" reveal of the existing `MaterialsPanel` (mutations reused, not reimplemented — `MaterialsPanel` gained an `initiallyExpanded` prop + `IndexingStatusDot` is now exported), and a **soft gate**: Continue is always available with ≥1 material; zero materials shows an empty-guard with a quiet "Start without materials anyway." Gated by a new `landingStep: 'materials' | 'interview'` in `CaptureClient` (resuming a saved conversation skips Step 1). New: `lib/capture/material-display.ts` (pure `materialProvenance`/labels/`shouldShowMaterialsStep`/`hasMaterials`, unit-tested), `app/capture/[code]/CaptureMaterialsStep.tsx` (testing-library render test). No schema/route/data-model/interview/synthesis change. Built via 5-task subagent-driven development; component review approved. Spec [`2026-06-11-capture-materials-confirmation-step-design.md`](./superpowers/specs/2026-06-11-capture-materials-confirmation-step-design.md) · plan [`2026-06-11-capture-materials-confirmation-step.md`](./superpowers/plans/2026-06-11-capture-materials-confirmation-step.md). Suite **964** green, `tsc` clean.

**Course categories + career-mapping flag — SHIPPED + DEPLOYED 2026-06-11 (`34703a3`; migration `0033` applied, backfill verified 16/16 · 14/0 · 16/11 = 27 mapped).** Reorganizes the public landing (`/`) from level-grouping to four category buckets (GC Core → Specialty/GC Tech → Major Req+GenEds → Other), and decouples "what builds toward the career" from display category via a new `builds_to_career` boolean. Career-coverage analysis (`getMatrixData` + `listStalePairs` in `program-coverage-queries.ts`) now filters on `builds_to_career=true` → the `/program` matrix is scoped to **27 courses** (16 GC Core + 11 high-enrollment Major Req: ACCT 2010/2020, MGT 2010, MKT 3010, PKSC 1020, STAT 2300, ENGL 1030, ENSP 2000, PSYC 2010, ECON 2110, PCID 3040); the 14 Specialty electives + 5 unselected choose-one Major Req sides (STAT 2220/3090/3300, ECON 2000, PCID 3140) + Other are excluded. Landing badges the 27 with a lucide `Target` icon. New affordances: "+ Add a course" on `/` links to the auth-funnel `/courses` form (no public write path); that form + a new per-course editor on `/courses` (`CourseClassControls.tsx` → new `PATCH /api/admin/courses/[code]`) set category/flag/catalog independently. Optional `catalog_url` threads through the add-course path (validated via new `lib/http/is-http-url.ts`). Canonical classification in `lib/db/course-category-seed.ts` (anti-drift-tested against the migration). `/view/[code]` renders the catalog link (http(s)-scheme-guarded). Built via 16-task subagent-driven development; final holistic review SHIP. Spec [`2026-06-11-course-categories-and-career-mapping-flag-design.md`](./superpowers/specs/2026-06-11-course-categories-and-career-mapping-flag-design.md) · plan [`2026-06-11-course-categories-and-career-mapping-flag.md`](./superpowers/plans/2026-06-11-course-categories-and-career-mapping-flag.md). Suite **951** green, `tsc` clean.

**Wiki trust-hardening (A–E) — IMPLEMENTED on branch `feat/wiki-trust-hardening` (off `dev`), NOT yet merged/deployed (2026-06-11).** Motivated by the myKG trust model; applies the same discipline (deterministic validation, confidence+provenance, loud-not-silent failure, "let the model decline") to the wiki **compile loop** — our least trust-engineered surface — and the agent **MCP**. Design: [`2026-06-11-wiki-trust-hardening-design.md`](./superpowers/specs/2026-06-11-wiki-trust-hardening-design.md). Five increments, full suite green (914): **(B)** `lib/ai/wiki/lint.ts` — deterministic no-LLM structural validator (broken wikilinks, orphans, missing required sections, ungated <2-source concepts) + `pnpm wiki:lint` CLI; **(E)** `lib/ai/wiki/schema.ts` `WIKI_SCHEMA` contract the lint enforces + prompt-injection fencing of snapshot-derived text in `wiki-update.md`; **(D)** typed-graph MCP/`/ask` tools `coverage_for_target` + `prereq_chain` (`lib/ai/wiki/graph-tools.ts`, over `getMatrixData()` / `listEdgePairs()`); **(C)** loud-not-silent batch reconcile (omitted page re-requested once, hard-logged if still missing) + `input_hash` frontmatter watermark for stale-detection (`update.ts`); **(A)** per-competency evidence band (claimed/materials_supported/artifact_verified, via `deriveEvidenceBand`) derived in `update.ts`, rendered as a `·claimed`/`·materials`/`·artifact` marker on the course page, with an optional `bandFloor` on `search_wiki` + band annotation on `read_wiki`. **Running the lint on the live wiki surfaced 153 broken wikilinks — a real compile-data issue, see Deferred/debt.** Remaining when merged: bump the MCP "What's live" row to 5 tools.

**`/view` full profile + "Apparent outcomes" reframe — SHIPPED 2026-06-14 (branch `feat/view-full-profile-apparent-outcomes`, Piece 1 of 2).** The public `/view/[code]` now renders the complete captured profile — apparent outcomes, incoming expectations *with* K/U/D depth chips, class structure, major projects, and course emphasis (previously a curated subset). `revised_objectives_draft` is reframed (synthesis prompt + review-panel label) as always-produced **"Apparent outcomes"** — "based on the materials + interview, what the course appears to deliver" (field key unchanged for snapshot back-compat). Spec [`2026-06-14-view-full-profile-and-apparent-outcomes-design.md`](./superpowers/specs/2026-06-14-view-full-profile-and-apparent-outcomes-design.md) · plan [`2026-06-14-view-full-profile-and-apparent-outcomes.md`](./superpowers/plans/2026-06-14-view-full-profile-and-apparent-outcomes.md).

**Guided faculty-reconciliation review — SHIPPED 2026-06-14 (branch `feat/guided-faculty-reconciliation`, Piece 2 of 2).** New `reconcile` capture stage between synthesis and the review panel: a hybrid stepper (apparent outcomes → incoming → outgoing KUDs) with a per-section conversational feedback box; the `reconcile-feedback` AI function PROPOSES per-item edits, faculty accept/tweak/reject, and the pure `applyReconciliation` applies them — a faculty override sets `source='instructor'` (the 'claimed' band, citations cleared, faculty-assertion evidence sentinel) so it's never silently presented as evidenced. The reconciled profile flows to the unchanged review panel → Save Snapshot, which persists the transcript in `reconciliation_log` (migration 0038). Spec [`2026-06-14-guided-faculty-reconciliation-review-design.md`](./superpowers/specs/2026-06-14-guided-faculty-reconciliation-review-design.md) · plan [`2026-06-14-guided-faculty-reconciliation-review.md`](./superpowers/plans/2026-06-14-guided-faculty-reconciliation-review.md).

**Portable OKF course profiles — SHIPPED 2026-06-14 (branch `feat/portable-okf-profiles`).** Each captured course profile is downloadable as a portable, self-contained OKF-v0.1 markdown file via a "↓ Markdown" link on `/view/<code>` (and the clean URL `/view/<code>/okf`). Generated deterministically on demand by a pure `profileToOkfMarkdown` serializer (`lib/okf/profile-to-okf.ts`) from the latest snapshot — full OKF frontmatter (`type/title/description/tags/timestamp/resource`) + the whole profile (apparent outcomes, competencies with K/U/D + evidence-band markers, incoming, class structure, major projects, course emphasis) + `# Citations`, PII-redacted. **Postgres remains the source of truth** (Model B — the markdown is an always-current projection, not storage). Spec [`2026-06-14-portable-okf-course-profiles-design.md`](./superpowers/specs/2026-06-14-portable-okf-course-profiles-design.md) · plan [`2026-06-14-portable-okf-course-profiles.md`](./superpowers/plans/2026-06-14-portable-okf-course-profiles.md).

**Wiki evidence-band floor now structured (frontmatter, not prose-scrape) — SHIPPED 2026-06-14 (branch `feat/wiki-structured-band-floor`).** `search_wiki`/`read_wiki` read `evidence_bands: [...]` from page frontmatter (stamped deterministically in `update.ts`) rather than scraping `·claimed`/`·materials`/`·artifact` prose tokens; prose-scrape kept as fallback for un-backfilled pages. Spec: [`2026-06-14-wiki-evidence-band-structured-design.md`](./superpowers/specs/2026-06-14-wiki-evidence-band-structured-design.md).

**CourseCapture v2 — Agentic Retrieval Architecture — ALL STAGES SHIPPED + LIVE (2026-06-11 status correction).** `COURSECAPTURE_V2_INGESTION=1` in **both** `dev` and the deploy worktree `.env.local`, and every course is `audit_mode='full'`, so the v2 path (tool-using audit agent + provenance synthesis) is what runs in production today — v1 is unreachable at runtime. Verified against prod 2026-06-11: 232 `capture_messages` across 24 distinct v2 sessions (most recent 2026-06-10). Stages 1, 2b, 3, 4, 5, 6, 7a, 7 all shipped (entries below). **Bug found + fixed during that verification (branch `feat/capture-v2-finish`):** `createSnapshot` never set `transcript_session_id`, so no snapshot linked its producing v2 session and the wiki raw-transcript layer never fired — now threaded through from the snapshots route. **v1 capture path RETIRED 2026-06-11** (branch `feat/capture-v2-finish`): deleted `captureChatTurn` (capture-chat.ts), `generateCaptureProfile` + v1 `formatTranscript` (capture-scores.ts; the shared `captureProfileJsonSchema` STAYS — v2 clones it), the `capture-chat.md` + `capture-scores.md` prompts, and the `if(v2)…else` fallback in both the chat + scores routes (v2 now unconditional; the `COURSECAPTURE_V2_INGESTION` flag still gates `finalize-extraction.ts` for a different purpose). tsc clean, full suite green (916). **Open v2 thread:** faculty profiles / cross-course memory (Stage-7 track, unspec'd — needs its own spec). Spec at [`docs/superpowers/specs/2026-05-26-coursecapture-agentic-retrieval-design.md`](./superpowers/specs/2026-05-26-coursecapture-agentic-retrieval-design.md); Stage 1 plan at [`docs/superpowers/plans/2026-05-26-coursecapture-v2-stage1-foundation.md`](./superpowers/plans/2026-05-26-coursecapture-v2-stage1-foundation.md). Replaces the "dump every material into one context" pipeline with three phases — per-material ingestion (chunk + digest + index in Weaviate), tool-using audit agent, synthesis with intrinsic provenance. Coexists with user's local agent infrastructure (shared Weaviate, per-course tenant). Supersedes the Phase 2 agent design ([`2026-05-22-phase2-agent-design.md`](./superpowers/plans/2026-05-22-phase2-agent-design.md)) and generalizes the reference-compression plan's two-tier future-directions.

**Stage 1 (Foundation) shipped 2026-05-26** (commits `a280db6` → `b4e0cac`, 9 commits):
- Schema: `capture_messages` append-only table with `UNIQUE(session_id, turn_index)`, `courses.audit_mode` toggle (default `'full'`), `course_capture_snapshots.transcript_session_id` link. Migrations `0022_mighty_firedrake.sql` + `0023_tiresome_rhino.sql`.
- Queries: `lib/db/capture-messages-queries.ts` (`appendMessage`, `getSessionMessages`, `startNewSession`).
- Data migration: existing `capture_conversations` row for GC 4800 mirrored into `capture_messages` (one-off script in `scripts/_one-off/`).
- Provider abstraction extended with `completeWithTools` across OpenAI, Anthropic, Local, Fake providers. Built on Vercel AI SDK v6 (`ai@6.0.191`) using `generateText` + `Output.object` + `tool` primitives (v6 replaces v4's `generateObject`). No AI function exposes this yet — wires up in Stage 3.
- Weaviate installed 2026-05-27 on the local Mac (v1.37.5 via Homebrew, launchd-managed at `~/Library/LaunchAgents/com.weaviate.plist`, anonymous access, HTTP `127.0.0.1:8090` + gRPC `127.0.0.1:50051`, data under `~/.weaviate/data`, registered in `~/.dev-ports.yaml`). Stage 2b shipped 2026-05-27 (WeaviateVectorStore, Materials panel UI, ingestion-checkin, admin backfill route, chunker fix). Stages 3–5 (agent loop, synthesis, migration) still ahead.

**Stage 3 (Audit-chat agent loop) shipped 2026-05-27**: `capture-chat-agent` function with three retrieval tools (`list_materials`, `fetch_material_section`, `search_materials`) against per-course Weaviate tenants **+ (2026-06-11) four read-only program-memory tools (`search_wiki`/`read_wiki`/`coverage_for_target`/`prereq_chain`) — full mode only, reference-never-evidence**; structured per-turn response (finding + question + citations + readiness); CaptureChatPanel UI with citation chips + session continuity; live-smoke fixes (campus.ts manual two-phase tool-call loop; audit-tools graceful empty-tenant handling).

**Stage 4 (Synthesis with source provenance) shipped 2026-05-27**: new `capture-synthesis.md` prompt that reads the full `capture_messages` transcript (with per-turn citations) plus the existing materials/digest/builder/prereq context, and emits a CaptureProfile where every finding carries an optional `source` (`instructor` / `materials` / `inferred`) and `citations[]`. `source` is derived mechanically from the citation types (pure helper at `lib/ai/synthesis/source-derivation.ts`, with tests). `POST /api/capture/[code]/scores` branches on `COURSECAPTURE_V2_INGESTION` + `audit_mode`: v2 calls `generateCaptureProfileV2`, v1 path unchanged. `productive_failure_conditions` is now optional in the v2 schema variant (emitted only if Audit Area 7 was probed). ProfileReviewPanel + VerificationSummary render small `SourceBadge` pills (teal / amber / gray) next to each finding; pre-v2 snapshots show no badge. (Stage 5 legacy migration shipped 2026-05-28 — see below; this "only remaining task" note is historical.)

**Stage 6 (Agent persona + discipline lift) shipped 2026-05-28**: `capture-chat-agent.md` gains a Manning-skill discipline anchor section (parallel to `capture-scores.md` / `capture-synthesis.md`); an explicit persona + posture + Evidence Rule + disagreement triangulation block; the disposition probe bank now covers all five baseline foundationals; conversation rules include periodic synthesis (every 5–7 turns), self-correction on readiness drift, and a pre-wrap-up recap turn. `ToolDefinition` gains an optional `usagePolicy` field with a `renderToolDescription` helper; per-tool guidance moves out of the prompt body and onto the three audit tools.

**Stage 7a (Streaming) shipped 2026-05-28**: `AIProvider.streamWithTools` added — OpenAI implementation via Vercel AI SDK v6 `streamText` + `Output.object` + `fullStream` iteration; Anthropic / Local / Campus / Fake throw "not implemented" stubs (no fallback needed since Mac deploy runs OpenAI). `lib/ai/agent/audit-agent.ts` refactored: `buildAgentCall` and `persistAssistantTurn` extracted as exported helpers shared by `runAuditAgent` and the new `streamAuditAgent` async generator (`lib/ai/agent/audit-agent-stream.ts`). v2 chat route `POST /api/capture/[code]/chat` accepts `?stream=1` or `Accept: text/event-stream` and returns NDJSON (`application/x-ndjson`) with `session` → `tool-start*` → `text-delta*` → `final` events. `CaptureChatPanel` consumes the stream with a `readNdjson` helper, renders an optimistic empty assistant turn and replaces its content per `text-delta`, surfaces tool-call banners briefly during retrieval, and reconciles to the canonical `finding + question` shape with citations on `final`. Eliminates the 3–7s silent wait per audit turn.

**Stage 7 — Session-continuity briefing shipped 2026-06-04**: new pure module `lib/ai/agent/session-briefing.ts` (`parseAssistantContent` / `composeSessionBriefing` / `renderBriefing`) replaces the raw "dump last 8 turns × 3 sessions" block in `buildAgentCall`'s at-rest context with a deterministic, **verbatim** structured briefing — per prior session: readiness (`covered`/`remaining`/score), the last 2–3 distinct findings (with citations preserved), and the last faculty turn. Nothing model-generated → no new hallucination surface, no new AI function, no migration. `PriorSessionSummary` slimmed to expose parsed assistant turns; the legacy dump fields were removed. Faculty-facing side benefit: a collapsed "Where we left off" recap card on `/capture/[code]` (`SessionBriefingView` prop-drilled `page → CaptureClient → CaptureChatPanel`). Design [spec](./superpowers/specs/2026-06-04-session-continuity-briefing-design.md) · [plan](./superpowers/plans/2026-06-04-session-continuity-briefing.md). Remaining Stage 7 track: faculty profiles / cross-course memory (unspec'd).

**Problem-solving capture fix shipped 2026-06-04** (audit step 1; [spec](./superpowers/specs/2026-06-04-problem-solving-capture-fix-design.md) · [plan](./superpowers/plans/2026-06-04-problem-solving-capture-fix.md)): productive-failure / structured-reflection capture is now honest. **Presence-as-sentinel** — a null `productive_failure_conditions` block means "Area 7 not assessed", a present block means "assessed" (an `absent` condition then genuinely means none). Scoring (`lib/program/scaffolding.ts`) gained a **`no_data` `pf_status` band**: not-assessed cells contribute `null` (excluded from the unproductive-success / premature-pedagogy rollups), never scored 0 — closing the scaffolding-spec "no data ≠ absent" requirement. The two prompt paths were unified (v1 `capture-scores.md` + its strict-mode JSON schema now match the already-honest v2: emit the block only if probed, never fabricate an all-`absent` block). `structured_post_mortem` above `absent` now requires a resolvable citation (`structured_post_mortem_evidence`, Zod-enforced). Legacy snapshots are reclassified to no-data via `PF_CONTRACT_EPOCH` (`lib/db/scaffolding-queries.ts`) — no migration. UX: a soft Area-7 generation nudge on `/capture/[code]` and a `no_data` band + course-level PF-grain disclaimer on `/program/scaffolding`. Audit origin: [`docs/superpowers/2026-06-04-capture-adequacy-audit.md`](./superpowers/2026-06-04-capture-adequacy-audit.md). Next audit steps: (2) Position Capture v1 pre-build tweaks, (3) evidence-traceability floor, (4) unified demand/coverage layer.

**Stage 5 (Legacy banner + citation drawer + conversation backfill) shipped 2026-05-28**: `LegacyBanner` (amber, with optional "Start fresh audit" button) renders above `ProfileReviewPanel` and `VerificationSummary` when every finding in the profile lacks a `source` flag (pre-v2 captures). `VectorStore` interface gained `fetchChunkById` with in-memory and Weaviate (`collection.query.fetchObjectById`) implementations. Two new endpoints: `GET /api/capture/[code]/chunks/[chunkId]` resolves a citation's chunk to its text + section + parent context; `GET /api/capture/[code]/messages/[messageId]` resolves an instructor-turn citation to its prose (assistant rows' JSON-stringified `{finding, question, ...}` is parsed and surfaced as `finding + question`). New `CitationDrawer` component (fixed overlay, click-scrim to close) opens from citation chips in `CaptureChatPanel` and from `SourceBadge` in both review panels (badge becomes a button when an `onCitationClick` handler is supplied and citations are non-empty). One-off backfill at `scripts/migrations/2026-05-28-migrate-capture-conversations.ts` synthesizes a `session_id` per legacy `capture_conversations` row, inserts one `capture_messages` per turn in order, and links the oldest unlinked snapshot via `transcript_session_id`; idempotent (skips courses that already have `capture_messages` rows). Ran post-merge: only legacy row was GC 4800, already mirrored in Stage 1 (skipped, no DB writes).

**Phase 1B Scaffolding Analysis — Stage 1 shipped 2026-05-28**: deterministic scoring primitives at `lib/program/scaffolding.ts` (depth-scaffolding status: `well_scaffolded / top_heavy / bottom_heavy / coverage_only / brittle_scaffold / not_addressed`; productive-failure cumulative score with depth-weight ramp 0/0.15/0.35/0.6/0.85/1.0 × reflection multiplier 1.0/0.75/0.5; `pf_status` band: `well_developed / developing / thin / absent` with `well_developed` requiring both cumulative ≥1.5 AND at least one D≥4 contributor). Phase-detection rule narrowed during implementation: D=2–3 only qualifies as a practice-phase signal when paired with K≥3 or U≥2 — the spec's literal independent-OR formulation flagged shallow-knowledge D=2 cells as practice, which made the `coverage_only` and `top_heavy` diagnostics noisy; the narrowed rule is documented inline in `scaffolding.ts` for revisit once real snapshot data accumulates. Query loader at `lib/db/scaffolding-queries.ts` joins `snapshot_target_coverage` with each snapshot's `audit_notes.productive_failure_conditions` block via the latest non-retired snapshot per course, ordered by `courses.level` then code. API: `GET /api/program/scaffolding?target=<id>&slug=…`. View 1 — scaffolding strip — at `/program/scaffolding`, with career-target selector, sticky sub-competency column, per-cell D-depth background ramp, PF density dot (top-left), structured-post-mortem badge (top-right "R"/"r"), right-margin status + PF chips. `/program` header now links over to `/program/scaffolding`. Views 2 (brittle-scaffold list) and 3 (course-contribution summary) plus the AI narrative + summary functions (`program-scaffolding-narrative`, `program-scaffolding-summary`) are Stage 2.

**Tactical Canvas-Syllabus suppression shipped 2026-05-26** (commit `8774b92`). Canvas Syllabus pages are skipped at import when the Sheets catalog has ≥1 learning objective; three existing rows (GC 1010, GC 3800, GC 4800) retroactively marked ignored. Step ahead of the broader curation rule set in the agentic-retrieval spec.

**Manning encoding backfill** (in progress). Phase A and Phase B (4 of 5) done; `capture-chat` deliberately deferred pending verification that the 4 encoded Phase B prompts produce better snapshots. If snapshot quality is unchanged or worse, Phase B becomes a revert candidate. **The capture-chat rewrite in the agentic-retrieval spec absorbs this work** — the new `capture-chat-agent.md` will carry Manning encoding from the start.

---

## Next-up

> **Vision-alignment review (2026-06-12)** — full audit of docs-vs-code-vs-literature with a prioritized action checklist (A1–A16): [`docs/superpowers/audits/2026-06-12-vision-alignment-review.md`](./superpowers/audits/2026-06-12-vision-alignment-review.md). Headlines: the executive brief's flag/dispute commitment is **dead code** (P0 doc-honesty pass needed); the depth instrument has **zero reliability data** despite being the project's own named falsification test (P1: run the reliability study; show bands not integers at program level); supply side ≈6/27 captured + demand side empty (P2: capture campaign before more analytics); no defined action loop for gaps (P3). A1+A3 shipped 2026-06-12 (flag mechanism built — option b — + dead-chain sweep); **A2+A8 shipped 2026-06-12** (per-instructor matrix rows — the guide's claim is now true as written); **A4+A5 shipped 2026-06-12** (criterion-relevance retitle + pre-committed failure criteria C1–C3, thresholds pending committee confirmation; superseded-banner on the original spec); **A7 shipped 2026-06-12** (program-level depth BANDS default — `depth-band.ts`, matrix Bands/Exact toggle + drawer integers, banded gap chips — operator chose bands-first, A6 study queued); **A9+A10 shipped 2026-06-12** (v2 synthesis now REQUIRES source+citations per competency — `captureProfileSchemaV2` — and `source` is re-derived from citations post-parse via the previously-dead `deriveSourceFlag`, fixing the Stage-4 "derived mechanically" claim drift; matrix cell drawer shows "Scored by [model]"). **A12 run 2026-06-12** (intended-skills `all-uncaptured` against prod: 243 rows / 34 courses into `course_intended_coverage` — cold-start data live for the capture campaign). **Step-1 walkthrough polish round 2 (2026-06-12):** capture header "Course List" → landing `/` (was the old `/courses` roster); syllabus status shows absolute "synced to Google Sheet on M/D/YY"; aggregate active/ignored/token footer dropped (token figure survives as a ≥150k amber warning only); "Manage all materials in detail" promoted to a real button. **Round 3 (2026-06-12):** Other-materials box rows now have FULL per-row parity with the manager (AI-summary toggle, preview, delete, meta line + "audit sends ~N tok"); Canvas box header shows imported Canvas course name + datestamp (migration 0035, 8 courses backfilled); retiring the manager's duplicate row-list is deferred pending walkthrough verdict. **Round 4:** dedicated `/courses/new` add-a-course page (code/title/catalog URL → straight into capture); sync-from-sheet refuses to stamp `lastSyncedAt` when the tab is missing/empty (404 instead — Google returns non-errors for missing tabs, so emptiness is the signal) and SyllabusBox shows 'not in the Google Sheet' for stamped-but-blank legacy rows; capture header Course List → absolute `http://gcworkflow.clemson.edu:3000/` (canonical public landing; relative `/` kept users on the funnel origin). **A6 parts i+ii run 2026-06-12** (synthesis 5× across 3 courses + coverage 5× across 2 pairs, ~$2.20): synthesis extremely stable (SD ≈ 0.02–0.05, foundational band-agree 100%); coverage scorer LOW on mini (D band-agree 25% mean — model-dependent). **A6 Part-ii A/B heavy re-run 2026-06-13** ($0.82): D band-agree 25% (mini) → 73.3% (heavy, gpt-5.5) +48.3pp — heavy stability is materially better; re-score of 120 pre-promotion mini cells RECOMMENDED (~$3.60 est.). `program-score-coverage` promoted light→heavy 2026-06-12 (code had drifted from the documented heavy tier; all 120 pre-promotion cells were mini-scored). **Re-score EXECUTED same night:** mini cells invalidated and the full matrix re-scored on heavy — **180/180 cells now `gpt-5.5`** (43 pairs, 0 failures, not capped). Gotcha for the record: `DEFAULT_TIERS` is a compile-time constant, so the long-running dev server kept serving the OLD tier after deploy — the first re-score attempt silently ran on mini and was redone after a `launchctl kickstart`; tier changes require a service restart. Day's AI spend incl. study + re-scores: $8.21 of the $10 cap. Report `2026-06-12-reliability-study.md`. Faculty-rater part iii still pending. **Reliability is now a STANDING check, not a one-off: `pnpm reliability:check` (scripts/reliability/check.ts) re-runs the coverage scorer on fixed pairs and exits non-zero if band-agreement drops below RELIABILITY_THRESHOLDS (lib/program/reliability-summary.ts, single source of truth); pre-commit nags when a scoring prompt/tier changes; the /program cell drawer surfaces the last-measured stability per model. Closes the blind spot that let the mini tier drift go unnoticed.** **P0 fully cleared; P1 A6 parts i+ii + A/B done, part iii (faculty raters) pending; of P2 only A11 (operator capture campaign) + A13 (demand gate) remain;** of P3, **A16 shipped 2026-06-12** (`isMentionOnly` — K1-only cells badged *"mention only"* in matrix cells/legends/drawer: exposure, not coverage); A14 (action loop, with operator) + A15 (approve rubber-stamp guard, awaiting operator yes/no) remain. **Round 5: review screen rebuilt as honest Step 2 of 2 (spec 2026-06-12-review-step2-redesign-design.md) — verification-first order, audit notes collapsible, presentation blocks behind a 'Preview the record' disclosure, sticky approve/save/stress-test bar, setup trays + DRAFT banner + 'Done reviewing?' footer removed from the stage.** Round 6: auditor picker at Step-1 top (shared chooser state); Canvas-syllabus row explains why it's unused (sheet catalog covers it); 'Replace syllabus' label when sheet-synced; Canvas box shows total points + per-item rubric ✓ (parsing shared with CanvasImportSummary via `parseAssignmentSummaries` in `lib/canvas/parseCanvasBlob.ts`); Canvas import gains a default-on auto-scan-linked-files checkbox. **Round 7 — incoming-skills discipline:** `capture-chat-agent.md` Area 1 rewritten derive-then-verify (Step 0 derives presupposed skills from the assignments BEFORE the catalog list; empty/thin list = a finding; readiness-gated like the theme gate); NEW paste-ready "Incoming requirements" output (`lib/capture/incoming-requirements.ts`) whose **bijective verb-phrase table IS the KUD encoding** ("…recognize the terminology, explain the why in your own words, and work independently." ⇔ K2/U2/D3 — syllabus-readable, machine-reversible, 1:1 test-enforced), derived from confirmed `incoming_expectations`, retroactive. Confident-zone rows gained collapse affordances (expansion was one-way). Post-approval: green completion card then **auto-return to the course list** (~2s) — approval is the end of the job. First real walkthrough snapshot captured (GC 3800, by the operator). **Round 8 — review-list merge + two confirm-flow bugfixes (2026-06-16, deployed):** the Step-2 competency list is now **ONE section in course order** (`Competencies (N)`) — the old two-zone "Worth a look" / "interviewer was confident about these" split is gone. Triage membership is **frozen at load** (`needsReview`/`reasonOf` in `ProfileReviewPanel`, computed once from the arriving profile): questionable rows render highlighted + expanded with their reason flag + a "Looks right ✓" confirm button; confident rows roll up to one-line `CompetencyRow`s (Open-all / Roll-up-all toggle). Editing a score no longer (a) **auto-confirms** the row — confirmation is the explicit button click; a stray slider tick used to flip it to ✓ Confirmed — nor (b) **migrates** a row between zones / reorders the list when a score crosses the flag threshold (operator report: a row jumped on a Do 4→1→2 drag). The `dirty` flag still independently satisfies the A15 approve guard, so the rubber-stamp protection is unchanged. Regression tests: `tests/app/capture/profile-review-confirm.test.tsx`; `review-step2-order.test.tsx` updated to the merged heading. **Also shipped 2026-06-16 — Signature Projects Capture (Part A)** ([spec](./superpowers/specs/2026-06-16-signature-projects-capture-design.md) / [plan](./superpowers/plans/2026-06-16-signature-projects-capture.md)): extended the existing `major_projects` field (NOT a new schema) with `deliverables`/`what_it_develops`/`weight_pct`/`duration_weeks` (Zod `majorProjectItemSchema` + strict v2 JSON schema in `capture-scores.ts`, all four in `required`, `weight_pct` nullable number / `duration_weeks` nullable **integer**); synthesis (`capture-synthesis.md`) emits them; new **Area 9** in `capture-chat-agent.md` (materials-first signature-projects pass, does NOT gate readiness); wiki `wiki-update.md` §8b renamed **`## Signature projects`** with a rich full-capture format (deliverables `·`-joined, weight/duration parenthetical, *what it develops*) + legacy fallback for pre-2026-06-16 snapshots. Prompt-only + additive-field — no migration. **Part B (position-capture employer-rating step) deferred** — see Deferred/debt. **Round 9 — material-ingest reliability + backgrounding (2026-06-16, DEPLOYED — merged `dev`→`main` `6f4fdf0`, service kickstarted, root 200). Also live from this deploy: multi-file material upload, inline "Save edits" under expanded competency cards, and a "↓ PDF" (browser print-to-PDF) button on `/view/<code>`.** Triggered by a GC 2400 faculty PDF-upload incident. THREE distinct bugs fixed + one feature, all committed to `dev` (main still at the `..` fix `f0f2698`; the running service predates all of today — **nothing below is live until a coordinated `launchctl kickstart`**, deferred because the faculty was mid-session): **(1)** `safeFilename` collapses `..` so a double-dot filename (`Substrates v1..2.pdf`) no longer 503s on the path-traversal guard; **(2)** the bare `/api/courses/<code>/materials` path is **excluded from the middleware matcher** (precise `(?!/)` keeps `/materials/<id>` + `/compress` gated) and POST/DELETE **self-enforce Basic Auth** (`gateBasicAuth`) — fixes the intermittent-under-load `Response body object should not be disturbed or locked` 500 (same Node-middleware body-replay class as imscc-import/transcribe; adversarial Opus review confirmed two-factor posture preserved); **(3)** linked Drive PDFs detect type by **`%PDF` magic bytes** not the `Content-Type` header (Drive serves PDFs as `application/octet-stream` → the old header check mislabeled them "unsupported type"); HTML bodies report "not accessible". Plus **stage-timing logs** (`[ingest]` extract/digest/contextualize/embed/upsert) to attribute ingest latency. **The feature — Background Material Ingest** ([spec](./superpowers/specs/2026-06-16-background-ingest-design.md) / [plan](./superpowers/plans/2026-06-16-background-ingest.md), 7 tasks subagent-driven, 1333 suite green CI-flow): indexing moved off the request path. New `indexing_status='queued'`; new `lib/capture/ingest-queue.ts` (in-process worker — `enqueue` → atomic `claimNextQueued` via `FOR UPDATE SKIP LOCKED` → `processMaterial` extract-if-needed→`finalizeExtraction`, `MAX_CONCURRENCY=2`, wake-flag race guard, boot-recovery resets stuck `indexing`→`queued` once per process). ALL seven ingest paths (materials POST, canvas-import, scan-linked-docs, imscc-import, canvas-reextract, v2-backfill) now **enqueue + return fast** with `indexingStatus:'queued'` instead of blocking on `finalizeExtraction`; MaterialsPanel renders `queued` + polls until terminal. Final Opus review caught + fixed 4 integration gaps (terminal status on non-ok extraction so the UI doesn't spin forever; re-instated vision-cost metering dropped in Phase A; restored the Canvas-syllabus LO set-aside by fetching course LOs in the worker; scan-linked-docs failed-fetch rows set `skipped` instead of enqueuing an unprocessable row). **Provider note:** the per-chunk `chunk-contextualize` + `material-digest` are light-tier = **`gpt-5.4-mini` (OpenAI, external)**; embeddings always campus Qwen. A bake-off (campus-120B vs gpt-5.4-mini, thinking-off gate) is deferred — see Deferred/debt. **Round 10 — review/view refinements + problem-solving reframe (2026-06-16, DEPLOYED `20368d5`).** Three operator-driven features, subagent-driven + merged/restarted: **(1) K/U/D override rationale** — raising a score above the AI/baseline now requires a per-competency reason before **Approve** (fixes the A15 inversion where a bare edit unlocked approval); inline level-aware "why this depth?" field; overrides frozen into the snapshot as `reviewer_overrides` (new optional `CaptureProfile` field, no migration); helpers `lib/ai/capture/score-overrides.ts`. **(2) Course-emphasis de-granularized** on `/view` — centrality bands (central/supporting/peripheral) instead of precise `pts · %` (the point values invited pushback). **(3) Problem-solving / productive-failure reframe** — dropped the interview's "didn't cover problem-solving" deficiency warning, added an opt-in **"Ask me one more important question"** button (injects a canned turn so the agent asks one final high-value question in its own words); the `/view` productive-failure section now shows a one-line qualitative band (*"shows {no real/slight/moderate/significant} evidence toward building habits of problem-solving and critical thinking"*, weighted `problemSolvingBand` over the 5 Area-7 conditions) with the condition detail in a collapsed roll-down. Also live from earlier 2026-06-16 work: the multi-file upload, inline "Save edits", and `/view` PDF (browser print). Suite 1349 green, tsc clean.

### Next steps — ordered (the working sequence)

> Consolidated 2026-06-05 so nothing drops. Status: ✅ shipped · 📋 spec'd, ready to build · ◻ not spec'd. The detailed catalog is the "Spec'd, not yet implemented" table below; this is the *order* + dependencies.

**Arc A — Employer demand → curriculum-sufficiency loop (vision Q1 spine; current priority — IN PROGRESS)**
1. ✅ **Position Capture v1 — the full planned 6-page flow** shipped 2026-06-05. JD ingest (Docling + `jd-extract`) → uniqueness → interview Qs → trajectory → AI-rated experiences (`position-rated-items`) → agent interview (`position-interview-agent`) → synthesis (`position-synthesis`) → immutable `position_captures` row. Pre-build amendments folded in (sub-competency join key, demand discipline, A5 seam spec). Schema 0029. Deployed for live user testing. Plan: [`2026-06-04-position-capture-v1.md`](./superpowers/plans/2026-06-04-position-capture-v1.md).
2. 🔨 **Step 4 — demand half (unified demand/coverage layer): BUILT-AHEAD on branch `audit-remediation-2026-06-07`, flag-gated, NOT activated.** Numeric partner-weighted `k/u/d_demand` in the new `career_target_demand` table (migration `0032`, not applied), keyed by `sub_competency_id`, populated from interviewed positions and differenced against course attainment → **Q1 *sufficiency* computable.** Pure engine + rollup + read + per-target panel all behind `DEMAND_COVERAGE_SEAM`. Design: [`2026-06-07-demand-coverage-sufficiency-seam-design.md`](./superpowers/specs/2026-06-07-demand-coverage-sufficiency-seam-design.md). Activate: `pnpm db:migrate` → `DEMAND_COVERAGE_SEAM=1` → regenerate aggregate. v1 excludes unmapped (null `sub_competency_id`) demand and uses partner-weight-only (2a); company-normalization (2b) deferred.

**Arc B — Prerequisite support (Q2); independent of the employer side, can run in parallel**
5. ✅ **Step 4 — prereq-edges half shipped 2026-06-05:** `prerequisite_edges` table (migration 0030) + `prereq-edge-seed` AI function + `computePrereqGaps` engine + `/courses` roster + `/courses/[code]` confirm/edit + gap view. **Deferred next:** `intended`-basis rough-pass increment (seed edges from syllabus promises before snapshot data exists) + program-wide graph/rollup viz.

**Arc C — Capture-credibility follow-ups (smaller; from the evidence ladder + adversarial review)**
6. ◻ **Evidence-ladder program-rollup split** (claimed-vs-supported counts in the matrix) — needs per-cell `source`/`citations` plumbed through the matrix API (`TODO(evidence-ladder)` marks the spot).
7. ◻ **"Diagnostic constraint modeling" Area-7 condition** (GC-specific troubleshooting; no-migration — JSON field on `productive_failure_conditions`).
8. ◻ Rubric-vs-prompt evidence typing (the L1/L2 distinction; ladder v2).

**Arc D — Program analytics**
9. 📋 **Phase 1B Scaffolding Stage 2** (AI narrative/summary functions, View 2 brittle-scaffold list, View 3 course-contribution, cross-target named patterns).
10. ◻ Phase 1D — Advising View.

**Arc E — Wiki / memory layer**
11. 📋 **Evolve `wiki-update` v1 → the compile-loop design.** NOTE: a v1 is already shipped + wired (writes course/concept pages + `index.md` on snapshot creation — `/wiki` is partially populated, NOT empty). The 2026-06-04 design is a **v2 maintenance loop**, not a from-scratch build. Highest-value gaps, in order: **(a)** competency/target pages never generate (wiki-update fires before `snapshot_target_coverage` is scored → re-fire on coverage scoring, or move to the design's watermark/diff trigger); **(b)** ≥2-source concept-promotion gate (replace the two hardcoded concepts); **(c)** deterministic lint pass → `gc-wiki-lint` issue (incl. summary-drift). Then writing-plans for the rest.
12. ◻ CourseCapture v2 Stage 7 — **faculty profiles** (cross-course memory); still unspec'd.

**Arc F — Documentation debt (interleave anytime)**
13. ◻ Deep-dive `.html` re-export (new identity + competence/framing sections live in `.md` only — no md→html generator in repo).
14. ✅ **DONE 2026-06-21 — validation plan pre-registered in the doc.** New **"Section 4 — Pre-Registration & Validation Plan"** in `graduate-outcome-validation.html`: H1/H0 (criterion relevance, above a chance baseline) + explicit out-of-scope (not attainment/causal/"validates"); the two sides kept independent (predictor = KUD+ coverage; criterion = first-destination O\*NET demand, seed-then-refine; circularity guardrail); pre-registered specs (≈10yr window fixed at registration, every-job/**first-career-relevant** as the unit, job→SOC coding + "verified" def, inter-rater κ ≥ 0.70 target, inclusion/exclusion); analysis plan committed before results (per-target alignment vs chance; crosswalk-vs-reality; asymmetric no-clean-match findings); an **indicators→controls/threats** summary table (drift / survivorship / circularity / progression-confound / coder-subjectivity / O\*NET-coarseness); pre-committed failure criteria extending C1–C3. Reading twin regenerated. *(Original ask: review #9's hypotheses/indicators/controls/threats table — done.)*

**Arc G — Graduate Outcome study (criterion-relevance): ELEVATED from deferred → active (2026-06-20)**
15. 🔭 **Data collection starts week of 2026-06-22 — a student is assigned.** Pulled up from the deferred slot deliberately: the B/C deep dive (memo §4.1e) established that graduate-outcome data is *not* optional polish — it is **(a)** the only check on the *correlated systematic bias* that no LLM-judge ensemble can cancel (9-judge panel ≈ 2 effective votes; Kim et al. ICML 2025, Kohli 2025), **and (b)** the *same* dependency that gates Hypothesis B2's decay/consolidation parameters. So the one dataset unblocks the two highest-value unknowns at once. **Immediate dependency:** finish #14 (lock the indicators / controls / threats table in `graduate-outcome-validation.html`) *this week*, so the student collects against the pre-committed design rather than ad-hoc fields. Design doc: [`graduate-outcome-validation.html`](../../graduate-outcome-validation.html). Pre-committed failure criteria already drafted there (per its 2026-06-12 retitle to "criterion-relevance").
16. 📋 **Grad-study design decisions to lock in a future brainstorm** (raised 2026-06-21 while de-jargoning the exec brief; student is collecting *now*, so these want nailing down soon):
    - **Collection window:** extend the Clemson set from the original **2022–2025 / N≈268** toward **~10 years**. The window was originally a function of limited time, not design. **Pre-register the window** before looking at results (same discipline as the pre-committed failure criteria).
    - **Job mapping (decided):** capture **every job per graduate, first one labeled.** Open decision: **anchor the criterion-relevance alignment on the first (or first 1–2) destination**, and keep later jobs as *trajectory context in a separate analytic lane* — later roles reflect post-grad learning + market moves the curriculum can't be credited/debited for, so folding all jobs equally into "where graduates land" would muddy the alignment signal.
    - **Define "first":** first *career-relevant full-time* role vs. literal first paycheck (so a summer retail gig isn't coded as a GC destination).
    - **Pre-registration packet:** window + first-vs-all-jobs rule + job-title→SOC coding method, on record before the student goes further.
    - **Comparability ceiling:** ~10yr is the sensible bound — older destinations reflect a *different curriculum and labor market*; fine for **criterion-relevance** (the study disclaims causal/attainment), not beyond.
    - **Doc corrections pending — do as ONE comprehensive pass when N + window settle (numbers are interdependent):** `graduate-outcome-validation.html` currently **mislabels the Clemson 268 as "2018–2026"** — that range is the **ACCGC parent dataset (603 records, 9 programs)**; the Clemson subset is **2022–2025**. When the new N lands, update the count + every dependent figure together (9-of-268, 237-of-268, "43 categories") **and** the `docs/index.html` card. (Exec brief already de-specified to "graduates from the past ten years" so it won't need re-touching.)
    - Per the operator: total graduates over 10yr is **well over 268**; the 268 was the *coded* set, not the population.
    - **Seed destination skills from O\*NET / Course-Skill Atlas — don't start from scratch** (operator idea 2026-06-21; = memo O\*NET rec #4, now also in background §11): each coded SOC already has an O\*NET / Atlas work-activity profile that can seed that destination's competency demand, leaving faculty + employer (Position Capture) to refine it. **Two guardrails:** (a) O\*NET/Atlas is coarse for GC specifics — where a GC competency (Typography, Color Science, Press Chemistry) finds *no clean match*, that absence is a **finding**, not a defect to paper over; (b) **circularity** — keep the seed (the curriculum's career-*target* definition) methodologically separable from the criterion (where graduates *landed* + its occupational profile), or the test degrades to "O\*NET vs O\*NET." The grad dataset's independence + seed-then-*refine* (never seed-as-truth) is the safeguard.
    - **Doc rescoped 2026-06-21 → two phases + student role** (`graduate-outcome-validation.html`, GH-Pages): **Phase 1 — Pilot** (the 268-record / 2022–2025 set, *complete*; established the crosswalk-vs-reality gap) and **Phase 2 — Expansion** (full ~10yr, every-job/first-labeled, + demand-side skills mapping + criterion-relevance alignment, *underway*, pre-registered). New **"Student-Researcher Role (2-credit course)"** section — Core: collect/code + pre-registered method + the destinations finding + a visualization + a TAGA/symposium poster-or-talk with co-authorship; Extension: demand-side skills mapping (O\*NET seed + posting enrichment) + inter-rater κ (the reliability data the project lacks). No Phase-2 N invented.
    - **Text-only reading version** `docs/graduate-outcome-validation-readable.html` (added 2026-06-21): a **generated** prose-only twin (headings + paragraphs + lists; tables / stats-bar / doc-map nav stripped) so iOS/Android "listen-aloud" runs straight through — the full page's SOC table stalled the reader. **⚠ Drift risk:** it's a snapshot, NOT auto-synced — regenerate (strip `<table>` + `.stats*` + `nav`) if the source grad doc changes.
    - **What the grad study does / doesn't unblock — gap analysis (2026-06-21, a LIVE pre-collection decision):** the Phase-2 design (destinations → SOC → criterion-relevance alignment) is the external-criterion gate that **fully serves** — **C** (the correlated-error external check + coding-κ), **A** (destination SOCs anchor the demand side + O\*NET seed), the **criterion-relevance core**, and the **§13 scaffolding claim** (well-scaffolded → better-aligned destinations). It does **NOT** directly provide — **B2 decay *parameters*** (destinations ≠ retained depth; you can't fit a decay curve from where someone got a job), **pull-up / transference** (needs competency-level measurement), or **direct graduate problem-solving / competence** (destinations are a proxy). **Correction to our own framing:** "B2 gated on graduate-outcome data" was optimistic — the destination study *constrains B2 and validates its direction* but cannot, on destination data alone, *fit* its parameters. **Softened in the docs 2026-06-21** (companion §2/§4/§6 open-questions + background §6). **Method constraint (clarified 2026-06-21): the student is doing LinkedIn-only, observational collection — NO contact/outreach.** That means there is **no retained-depth signal available at all** (LinkedIn Skills = self-curated presence, not depth; no assessment without contact). So B2-parameter-fitting and transference are **out of scope for this study, period** — not a "cheap augmentation away." A real durability/transference measure would need a *separate, contact-based* study (longitudinal assessment or graduate/employer competency ratings). The most LinkedIn can squeeze toward the durability *question* is **weak observational proxies** — competency-demand persistence across the role trajectory (LLM-extract which competencies each role exercises; do deeply-built ones recur over time?), self-listed-skills presence, and role-text "is this competency used on the job" — all describe patterns, none measure retention or fit a curve. **Honest scope: the study is criterion-relevance + trajectory (A, C, the core), and B2/transference are a separate out-of-scope study.** (Earlier note here proposed an alumni/employer rating "slottable into Phase-2 now" — withdrawn: it assumed outreach she isn't doing.) Docs corrected 2026-06-21 (companion B2 item + shared-dependency open-q; background §6).

**Single most-next thing:** build Position Capture v1 — the full 6-page flow (amendments folded into schema/synthesis) — and deploy it for live user testing. **In parallel and now time-boxed:** lock the Graduate Outcome validation plan (#14) before the student's data collection starts the week of 2026-06-22 (Arc G). Arc B (5, Q2/prereq edges) can also run alongside since it's course-side and independent of the employer build.

---

### Spec'd, not yet implemented

> **CourseCapture Step 1 three-source redesign — SHIPPED + DEPLOYED 2026-06-11 (`9cfe2aa`; built via ultracode workflow — 6 agents; 993 suite green, tsc clean; live capture page verified HTTP 200). Pending the operator's live visual feel-review + iteration.** From a live walkthrough: the current Step-1 (clean list + bolted-on `MaterialsPanel` reveal) reads as two unrelated UIs and misrepresents depth ("2 Canvas items" hides 23 graded items + the catalog + linked docs). Redesign = **three accordion source-boxes**: **Syllabus & course info** (top course-overview summary; unrolls to the extracted `CatalogOverview` block; source = synced GC sheet [default] / attach / — Canvas syllabus shown under Canvas, labeled), **Canvas** (all Canvas materials, import/reimport prominent, per-item ignore + Index-now), **Other materials** (uploads + linked docs, add/scan). Reuses existing endpoints + helpers; **keeps `MaterialsPanel`** (becomes the deep "Materials manager", e.g. from Course Builder). No schema change (syllabus inferred from the `Canvas: Syllabus` label). 6-task plan. Spec [`2026-06-11-capture-three-source-redesign-design.md`](./superpowers/specs/2026-06-11-capture-three-source-redesign-design.md) (+ two "Resolved in review" rounds) · plan [`2026-06-11-capture-three-source-redesign.md`](./superpowers/plans/2026-06-11-capture-three-source-redesign.md). **Biggest capture surface change to date; the real remaining check is the operator's visual feel-review on the live host — expect a polish pass.** (This entry sits under "Spec'd, not yet implemented" for now — move to Recently-shipped on next `/refresh-state`.)

> **Capture adequacy audit (2026-06-04):** two multi-agent audits assessed whether course- and career-level capture record what the vision needs. Findings + prioritized fixes in [`docs/superpowers/2026-06-04-capture-adequacy-audit.md`](./2026-06-04-capture-adequacy-audit.md). Headline: Q1 *breadth* is computable but Q1 *sufficiency* and Q2 are not (career demand is free-text/unkeyed with no numeric demand axis; prereq links are free-text; problem-solving/productive-failure capture is the weakest area — "not probed" collapses to "absent", violating the scaffolding spec). Drives the **unified demand/coverage layer** (audit step 4 — the strategic spine; design pending, see Next-up) and the Position Capture v1 pre-build amendments.

| Increment | Spec | Description |
| --------- | ---- | ----------- |
| **Evidence ladder** (audit step 3, reframed) | [design](./superpowers/specs/2026-06-04-evidence-ladder-design.md) | Surface per-claim credibility (claimed / materials-supported / artifact-verified) **derived at read time** from existing `source`+`citations` — no schema change, no migration, retroactive. **Subsumes & reframes the held step 3**: per the conversation-engine-first identity, the evidence floor becomes a *transparency layer* (flag a high-but-instructor-claimed score, never gate/reject it). **Shipped 2026-06-04** (`83b7a03`): pure read-time `deriveEvidenceBand` (`lib/program/evidence-ladder.ts`), an evidence-band chip + an "⚠ unverified" flag on D/U≥3-but-`claimed` competencies in `ProfileReviewPanel`. No schema change, no migration, retroactive, no score gated. Decisions: 3 bands / defer rubric typing / compute-at-read / light nudge. **Deferred:** the program-rollup claimed-vs-supported split (matrix cells don't yet carry per-claim `source`/`citations` — `TODO(evidence-ladder)` left in `ProgramCoverageClient`); `artifact_verified` band is defined but unreachable until a student-artifact citation type exists. |
| **Problem-solving capture fix** (audit step 1) | [design](./superpowers/specs/2026-06-04-problem-solving-capture-fix-design.md) | Make productive-failure / reflection capture honest: presence-as-sentinel (null ⇒ *not assessed* ≠ `absent`), unify the two prompt paths (stop fabricating all-`absent` blocks), new `no_data` `pf_status` band excluded from rollups, soft generation nudge when Area 7 unprobed, citation-backed `structured_post_mortem`, legacy snapshots reclassified to no-data via a deploy-time cutoff, course-level PF-grain disclaimer. No migration. Closes the scaffolding-spec "no data ≠ absent" requirement. **Shipped 2026-06-04** ([plan](./superpowers/plans/2026-06-04-problem-solving-capture-fix.md); `lib/program/scaffolding.ts`, `lib/db/scaffolding-queries.ts` `PF_CONTRACT_EPOCH`, `lib/ai/capture/schema.ts`, `lib/ai/analyze/capture-scores.ts`, both capture prompts, `ScaffoldingStripClient.tsx`, `CaptureChatPanel.tsx`). |
| **CourseCapture v2 — Agentic Retrieval** | [spec](./superpowers/specs/2026-05-26-coursecapture-agentic-retrieval-design.md) | Three-phase architecture: per-material ingestion (chunk + digest + Weaviate index), tool-using audit agent, synthesis with intrinsic provenance. **Stages 1, 2a, 2b, 3, 4, 5, 6, 7a all shipped 2026-05-26 through 2026-05-28 (see Active arc).** Stage 7 remainder: **session-continuity briefing shipped 2026-06-04** ([design](./superpowers/specs/2026-06-04-session-continuity-briefing-design.md) · [plan](./superpowers/plans/2026-06-04-session-continuity-briefing.md)) — deterministic structured briefing (`lib/ai/agent/session-briefing.ts`) replaced the raw-transcript dump in the audit agent's at-rest context, plus a "Where we left off" recap card on `/capture/[code]`; no new AI call, no migration. Faculty profiles still unspec'd. |
| **Phase 1B — Scaffolding Analysis Stage 2** | [spec](./superpowers/specs/2026-05-25-scaffolding-analysis-design.md) | Stage 1 (deterministic scoring + View 1 strip) shipped 2026-05-28. Stage 2 = AI narrative + summary functions (`program-scaffolding-narrative`, `program-scaffolding-summary`), View 2 (brittle-scaffold list at `?lens=brittle`), View 3 (course-contribution summary at `?lens=course-contributions`), sub-competency type classification (`technical / horizontal / mixed`), cross-target rollups (unproductive-success / premature-pedagogy / coverage-without-integration named patterns). |
| **Position Capture v1** | [plan](./superpowers/plans/2026-06-04-position-capture-v1.md) | **Shipped 2026-06-05.** 6-page partner flow: JD ingest (Docling + `jd-extract`) → uniqueness → interview questions → trajectory → AI-rated "experiences worth having" (`position-rated-items`) → agent interview (`position-interview-agent`) → synthesis (`position-synthesis`) → immutable `position_captures` row. Many positions per target roll up into `career_target_kud_aggregate` (v1 = deterministic Markdown stub; AI synthesis deferred). **Retires CareerCapture v1** (prototype, never shipped): schema 0029 renames `career_captures` → `position_captures`, `career_capture_messages` → `position_capture_messages`. Pre-build amendments (A1–A5) folded into schema + synthesis: nullable `sub_competency_id` join key on qualifying competencies + Page-5 ratings; `required_for_success` anchored to day-one entry level; demand-side `evidenced_by`/`confidence` discipline added. **A5 seam (demand→coverage, not yet built):** demand is captured keyed to `sub_competency_id` but is NOT yet compared against course attainment; the demand→`snapshot_target_coverage` seam is specified in [`2026-06-04-position-capture-v1-prebuild-amendments.md`](./superpowers/specs/2026-06-04-position-capture-v1-prebuild-amendments.md) (Amendment 5) and built in the unified demand/coverage layer (audit step 4). (Amendment 6 — descriptor-edit coverage-matrix invalidation — shipped standalone 2026-06-04, `923409b`.) Partner API routes under `/api/partners/[token]/positions/*` (CRUD, extract-jd, upload-doc, generate-items, chat). |
| **⭐ Unified demand/coverage layer + persisted prereq edges** (audit step 4 — **the strategic spine**) | split into two half-specs (sequencing decided 2026-06-05: **Q2/prereq-edges first** — it's independent of the ~empty position table; the Q1/demand half follows) — Q2 half now spec'd ⤵, Q1/demand half design still pending | **The increment that actually makes Q1 *sufficiency* and Q2 computable.** Today the matrix answers Q1 *breadth* only (is the competency covered?) — **not** sufficiency (is the depth *enough* for the career?) — and Q2 (do the prerequisites support what a course expects?) is not computable program-wide. Root cause (per the capture-adequacy audit): KUD+ judgments are stranded in free-text/JSONB instead of FK-keyed coverage rows. This layer fixes both: **(1) demand axis** — add numeric per-dimension **`k/u/d_demand`** (entry-level, keyed by `sub_competency_id`) to the catalog, populated from the Position Capture aggregate, against which the *existing* keyed course-attainment depths are differenced → **Q1 sufficiency**; **(2) persisted prereq edges** — parse `courses.prerequisites` into an FK-backed **`course_prerequisites`** table and store per-competency `incoming_expectation → prereq-competency` alignment rows → **Q2, program-wide** (this **subsumes Phase 1C**). One FK-keyed target-coverage layer instead of three on-demand reconstruction paths. **Depends on** the Position Capture v1 pre-build amendments (the nullable `sub_competency_id` join key) landing first. Not yet spec'd. |
| **Persisted skill-tagged prerequisite edges** (audit step 4, **Q2 half**) | [design](./superpowers/specs/2026-06-05-prerequisite-edges-design.md) — **shipped 2026-06-05** | The course-side spine. New `prerequisite_edges` table (migration `0030_supreme_iron_monger.sql`): **direct, skill-tagged** edges `(focalCourse → prereqCourse, sub_competency, expected_k/u/d)`. LLM-seeded (`prereq-edge-seed`) from the free-text `courses.prerequisites` + incoming-expectations, faculty-confirmed via `/courses/[code]`. `computePrereqGaps(courseCode)` differences the focal course's incoming need against each direct prereq's measured `snapshotTargetCoverage` attainment — **deterministic, ordinal-MAX aggregation (no sum / no double-count)**; transitivity derived by traversal, never authored. `basis: measured|intended|none` stamps each gap. `not yet captured` prerequisites show as no-data, never gap-free. Admin routes: `GET /api/admin/courses/roster` + `GET /api/admin/courses/[code]/prereq-edges`. **Subsumes Phase 1C.** Deferred: `intended`/rough-pass increment (now spec'd ⤵) + program-wide graph/rollup viz. |
| **Intended-skills rough pass** (audit step 4 — cold-start companion) | [design](./superpowers/specs/2026-06-05-intended-skills-rough-pass-design.md) — **shipped 2026-06-05** | Cheap one-LLM-call-per-course extractor (`intended-skills-extract`, default tier, count 20→21) that reads catalog fields (`description`/`learningObjectives`/`majorProjects`/`skillsRequired`) + the sub-competency catalog and emits **intended** (syllabus-asserted) K/U/D per sub-competency into a new `course_intended_coverage` table (migration `0031_clumsy_selene.sql`), banded evidence-ladder `claimed`, **never merged into measured `snapshotTargetCoverage`**. Feeds the prereq engine's already-wired `basis:'intended'` seam (per-prereq, measured-wins) + the `/courses` `intended` data-state badge, so cold-start gap views are non-empty on day one. **BINDING RULE:** intended is reference-only in CourseCapture — never prefills answer fields; the credibility band upgrades out of `claimed` only by real evidence, never by confirmation (prevents acquiescence-bias laundering of aspiration→attainment). v1 = engine (migration `0031`, `intended-skills-extract` AI function, gap-engine `basis:'intended'` seam, admin route) + map consumers; the in-capture reference-panel is deferred. Admin: `POST /api/admin/courses/intended-skills` (`mode:'one'`/`'all-uncaptured'`), daily-cap gated. |
| **Phase 1C — Prerequisite Gap Analysis** | sketched in [Phase 1 umbrella spec](./superpowers/specs/2026-05-24-program-coverage-views-spec.md) | For each captured course, compare its `incoming_expectations` against captured snapshots of its declared prerequisites. **Subsumed by the persisted skill-tagged prerequisite edges** (above, now spec'd 2026-06-05) — that is the structural form of this; track it there. |
| **Phase 1D — Advising View** | sketched in same umbrella | Per-target recommended course sequence + gap detection. |
| **`wiki-update` compile loop** (wiki Task A2 — v2 of an **already-shipped v1**) | [design](./superpowers/specs/2026-06-04-wiki-update-compile-loop-design.md) — **design is a v2; v1 ALREADY BUILT + wired** | ⚠️ **STATE drift corrected 2026-06-05:** a working v1 already exists (`lib/ai/wiki/update.ts` + `lib/wiki/git-ops.ts`, fire-and-forget on snapshot creation) and `/wiki` is partially populated — this row previously claimed "not yet implemented / render empty," which was wrong. The 2026-06-04 design is therefore a **v2 maintenance loop**, not a from-scratch build. v1 does: deterministic dependency fan-out (`computeAffectedPages` from `snapshot_target_coverage`) + model prose (strict-mode JSON) + git write/push; writes course + concept pages + `index.md`. **The design's deltas (still to build):** (a) **trigger-timing fixed (`df98168`) AND the deeper stall fixed + back-populated 2026-06-05.** The real blocker was NOT relaunch timing — it was the *monolithic* `updateWikiForSnapshot` emitting all ~37 affected pages (course + index + 30 competency + 5 target) as one strict-mode JSON, which stalls indefinitely at full coverage (one captured snapshot is scored against every sub-competency, so the affected set is the whole space; this would equally hang the post-coverage re-fire). Fixed with bounded-batch page generation (`WIKI_PAGES_PER_CALL=6` in `lib/ai/wiki/update.ts`, backward-compatible — single-batch path unchanged, 50 wiki tests green). Back-populated via `scripts/_one-off/backpopulate-wiki.ts` (one-off, decoupled from launchd so relaunch-safe by construction): GC 1010's run alone populated all 30 `competencies/` + 5 `targets/` cross-course-rollup pages (wiki commit `5b066f6`). **Still open:** fire-and-forget background jobs still lack relaunch-safety + a UI "running" signal → the watermark/`/refresh-wiki` reconcile (delta d) remains the durable fix. (b) ≥2-source concept-promotion gate (v1 hardcodes `productive-failure` + `three-act-structure`); (c) deterministic lint → `gc-wiki-lint` issue (broken wikilinks / orphans / staleness / summary-drift); (d) watermark/diff trigger + `/refresh-wiki` reconcile (vs v1's per-snapshot fire-and-forget); (e) zone model (`notes/` human zone, `WIKI.md`) + summary-first index discipline + input-hash skip. Provenance + FERPA gates: reused from the capture path. 3 open questions (prose-drift skip, multi-instructor canonical, concept-slug authority). **Action: evolve v1 — fix (a) first (highest value, unblocks competency/target pages), then (b)/(c) → writing-plans for the rest.** |

### Blocked

- **Phase 2 conversational agents** ([plan](./superpowers/plans/2026-05-22-phase2-agent-design.md)) — **superseded by the agentic-retrieval spec** above. The remaining pieces (materials auditor + KUD chat as standalone agents) are absorbed into the v2 architecture; nanoclaw block is no longer load-bearing since the v2 design uses the existing provider abstraction extended with tool-use.
- **CareerCapture v1** — prototype built 2026-06-04, never worked in practice. **Retired by Position Capture v1** (shipped 2026-06-05); its tables were renamed in schema 0029.

### Exploratory projects (off the product surface)

- **Voice-interview lab — SPUN OUT to its own repo `~/projects/voicelab` (2026-06-09).** Was an in-app spike (`/voicelab`, 2026-06-07 → 08) to decide whether a real-time *spoken* model could drive Position Capture and which local model to use. On 2026-06-09 it was **extracted to a standalone Next 15 repo** so its rapid experimentation no longer churns this codebase. **No longer in this repo** — `app/voicelab/*`, `app/api/voicelab/*`, the middleware `/api/voicelab` exclusion, and `scripts/_one-off/kokoro_server.py` were all removed (the `/api/voicelab` exclusion was reverted, so `middleware.ts` is back to its standard matcher). New home: launchd **`com.voicelab.dev`** on `0.0.0.0:3100`, public via a **second Tailscale Funnel port `:8443`** (`https://admins-mac-studio-2.tailb723c1.ts.net:8443/` — the `:443→:3000` partner survey was left untouched), gated by its own `FACULTY_BASIC_AUTH` (middleware + per-handler). Reads the `gc-curriculum-wiki` clone **read-only** (slimmed `lib/wiki/git-ops.ts` with no write/push surface). Still depends on omlx (`:8000`), the **Kokoro TTS sidecar** (`:8123`, now `pnpm kokoro` from the voicelab repo; auto-downloads its ~325 MB model), and `ffmpeg`. The `~/.dev-ports.yaml` entry was promoted from the temporary `voicelab_kokoro` block to a proper `voicelab` project (next_dev 3100 + kokoro_tts 8123). The Stage-0 terminal version still lives here at `scripts/_one-off/voice-spike.py`. Findings below are retained because they describe omlx/local-model behavior this tool also relies on.
  - **What it does now:** 3 input modes — Whisper transcript / **direct audio** (Gemma 4 input_audio, skips Whisper) / **audio understanding** (a small audio model emits an 8-field structured reading — literal+cleaned transcript, essence, decisions, unresolved questions, confidence/hesitation, curriculum implications, handoff JSON — which the reasoning model works from); 3 scenarios — Position Capture (optional JD ingest), Course Capture (GC 3460), and **Curriculum Q&A** (roles flip: you ask, the model answers grounded in the live wiki via per-question retrieval reusing the `search/read/list_wiki` functions); a **replay bench** that runs one fixed input set through many models and has a frontier ruler (`gpt-5.5`, blind, scenario-aware rubric) score+rank the outputs; and an optional **background-analyst "fast/slow" layer** — a stronger model (Qwen-35B default) reviews each fast turn off the live loop and queues addenda woven into later turns + speaks an immediate correction when the fast reply was wrong. Plus warm/free-memory model management and a browser mic-permission helper.
  - **Findings worth keeping:** direct audio works **only** on Gemma 4 E2B/E4B/12B — verified the 26B-A4B silently reads audio as gibberish (no encoder) and Qwen errors, so it's gated; Gemma E4B is the standing live recommendation (≈219 ms TTFT warm; user couldn't perceptually distinguish it from 26B); Qwen "thinking" must be disabled via `chat_template_kwargs:{enable_thinking:false}` (the `/no_think` soft switch is NOT honored by this MLX build); E4B reliably produced the full 8-field perception JSON from a hesitant spoken answer.
  - **Open decision:** whether/how to fold the spoken-interview, audio-understanding-perception, and fast/slow-analyst ideas into the real Position Capture page-6 interview. That experimentation now happens in the standalone `~/projects/voicelab` repo; nothing lands in this codebase until a concrete design is approved.

### Deferred / debt

- **FUTURE TEST — local (omlx) vision bake-off + gemma pan-and-scan knob (queued 2026-06-23).** Context: `transcribeDocument` (image-based PDF/slide vision) is wired **ONLY for OpenAI** — `lib/ai/local.ts` + `lib/ai/campus.ts` both *throw* "does not support document vision transcription". An out-of-band bake-off (harness preserved at `~/.local/share/gc-curriculum-tool/vision-bench/` — `bench.py` + 2 ground-truth slides + README recipe) compared providers on one easy + one dense slide: **OpenAI gpt-5.4** resolution-robust + most complete (~5-6s, paid); **Clemson `qwen3-omni-30b-a3b`** accurate but ~25s (too slow for the 40-page hot path); **Clemson `gemma-4-31b`** 3.5s + free but tokenizes every image to a **fixed 318 prompt tokens** (single 896px crop — misses fine print, breaks into a repetition loop at native 4000px, non-deterministic completeness 322↔621 chars same settings). **gemma's `do_pan_and_scan` resolution flag is NOT client-tunable on Clemson** (per-request `mm_processor_kwargs`/`chat_template_kwargs` both ignored — it's a vLLM launch-time setting; would need RCD to relaunch, or self-host). **Local gemma-4-26b A4B MoE was UNTESTABLE** — omlx returned 507 OOM (a ~37GB job held the box; MoE needs 15GB over the 51.84GB ceiling). **The queued test (run when omlx frees memory):** (1) bench local `gemma-4-26B-A4B-it-QAT-MLX-4bit`, `Qwen3-Omni-30B-A3B-Instruct-4bit`, `gemma-4-31B-it`, `gemma-4-12B-it` vs the Clemson/OpenAI baselines; (2) **check whether omlx/MLX-VLM exposes a pan-and-scan / image-resolution knob at launch** (we own omlx, so unlike Clemson this lever is ours) — that's the path to raising gemma's effective resolution. No code changed; pure exploration toward a possible non-OpenAI vision path.

- **Triage-mode Ingest fixed + a known vision cap — 2026-06-22 (DEPLOYED `016fa8e`).** Bug: with `COURSECAPTURE_V2_INGESTION=1` (triage flow on), uploads are stored un-extracted (`extraction_status='pending'`, no text, blob on disk) and rely on the explicit "Ingest & continue" step to extract them — but `POST /api/admin/v2-backfill` guarded on `extraction_status === 'ok'`, so it **skipped every freshly-uploaded file** and returned instantly (operator hit this on GC 1010: 7 pending slide decks, ingest was a no-op). Fix: new pure `lib/capture/ingest-selection.ts` `ingestAction()` — enqueue any material the worker can process (extracted text OR a readable local blob), **skip already-`ready`** (re-running metered vision OCR on completed work is wasteful — operator chose only-unprocessed over re-process-everything) and skip rows with neither text nor a local blob; wired into the route. Image-based decks flow through the existing `extract-text.ts` vision fallback unchanged. Unit tests `tests/lib/capture/ingest-selection.test.ts`. **Known limitation (NOT fixed):** vision transcription is capped at **40 pages/deck** (`VISION_PAGE_CAP` in `extract-text.ts`) to bound OpenAI cost/latency — decks longer than 40 pages get only their first 40 pages transcribed, silently. Raising/paginating that cap is deferred. **Deploy-process note (learned this session):** `com.gc.curriculum-tool` runs `next start` (compiled build), NOT a dev server — a deploy REQUIRES `pnpm -C ...-deploy build` before `launchctl kickstart`; restart-only serves stale code. `~/.dev-ports.yaml` corrected (it wrongly said `dev:lan`).

- **Writeup-review citation/precision pass — DONE 2026-06-20 (background.html + problem-solving-deep-dive.html; GitHub-Pages docs).** From an external review of both writeups + a reviewer-supplied 56-card reference set (forward-staged to the recommendations). Wired live: **Abrami 2015** (§4 — immersion-over-generic CT, converts the biggest exposure into corroboration), the **transfer-skepticism canon** Thorndike-Woodworth 1901 / Detterman 1993 / Sala 2019 (§4), the **metacognition pair** Veenman 2006 / Schoenfeld 1992 (§4), the **competency-critique sources** NRC-O*NET 2010 / Gonczi 1994 / Brockmann-Clarke-Winch 2008 (§11); **NVQ claim corrected** to Dearden et al. 2004's precise "poor-to-negative wage returns + ability-bias caveat" (dropped the causal "(IFS)" overstatement, §11 + §3 table); **Hattie demoted** (deep-dive §7, Donker carries it); **Gentner / Gick & Holyoak anchors renamed** to match the reviewer cards. `docs/references/` grew 42→52 audited rows (INDEX rows 43–52) + 3 staged; anchor integrity 50/50; reference-audit Findings #4 (Dearden) logged. **Still deferred from the review:** (1) **3 staged cards** — `belenky-nokes-malach-2012` + `camerer-loewenstein-weber-1989` carry "partially inconsistent" cite-as-drafted warnings (Belenky would *invert* the study), `bilalic-2009` has no natural anchor yet — left un-wired on purpose; (2) the review's **figures** — **the 4 priority figures shipped 2026-06-20 into `background.html` §4** (Fig 1 Kapur productive/unproductive × failure/success 2×2 w/ a GC example per cell; Fig 2 Gick-Holyoak transfer bars ~10/30/50%; Fig 3 "what transfers" flow w/ the non-travelling cognitive-skill path; Fig 4 GC-module→mechanism→evidence table), as inline HTML/CSS (no SVG/build dep), palette-matched, browser-render-verified via Playwright; **also mirrored into `problem-solving-deep-dive.html` 2026-06-20** (Fig 1 + Fig 4 in §3 productive-failure; Fig 2 + Fig 3 in §5 transfer — same styles, render-verified); **still optional:** the depth-vs-pedagogy program-reading 2×2, the Macnamara variance bars, the three-senses-of-problem-solving table, the implementation-chain pipeline strip; (3) **hoist the three-senses-of-problem-solving taxonomy** from deep-dive §12 to §1; (4) **de-dup background §8 vs the deep-dive** (drift risk, flagged in CLAUDE.md too); (5) the reviewer's **`citations/load_bearing/` PDF set is NOT committed here** (the cards' "Local copy" lines point to the reviewer's workspace; arXiv/DOI links are the canonical accessible copies). The review's "make the validation plan concrete" = Arc G #14, already tracked.

- **Adversarial-review pass (background.html + problem-solving-deep-dive.html) — DONE 2026-06-20 (all three tiers).** From two further reviews (adversarial + hypothesis-driven addendum). **Tier 1 (4 confirmed errors, each verified vs our own cards/code first):** Abrami de-overstated (Ennis ranking is non-sig p=.25 → content-specific g+≈0.57 vs generic 0.30); Belenky §5 de-inverted (invention/PFL *reduces* mastery-orientation dependence); Gick-Holyoak "~30% with a hint" relabeled (control / one-analog-no-hint / two-analog, prose + Figure 2 both docs); abstraction-and-bridging "deferred" → "shipped" (it's Area-7 condition e + schema). **Tier 2 (hypothesis additions, citation-backed):** durability caveat on the depth-maps (Arthur 1998 / Bahrick 1984 — instantaneous ≠ durable depth, doubles as the B2 roadmap; §4 + deep-dive §10); promoted the correlated-error finding (Kim 2025 / Kohli 2025) + named the auditor's *own* Xu-keyword-inflation bias + SEM/aleatoric ceiling (Hüllermeier-Waegeman 2021) into §7 / deep-dive §7; "tool is the experiment" refined to separate internal calibration from external construct validation. **Tier 3:** verb-discipline pass (diagnose/measure/answer-exactly softened where they outran validation), O*NET (NRC = content-model weakness, not measured low-reliability) + NVQ (retirement split from Dearden) tone, three-senses-of-problem-solving framing hoisted to deep-dive §1 (pointer to §12). Anchor integrity 50/50→55 refs both docs; figures render-verified. **Residual:** (a) **5 new background §15 refs (Arthur, Bahrick, Kim, Kohli, Hüllermeier) lack standalone audit cards** in `docs/references/` — verified in the B/C deep dive, card backfill pending; (b) the addendum's "Tschirschwitz & Rodehorst 2024" (label-reliability) was **unverifiable / absent from our library — NOT used** (Hüllermeier-Waegeman substituted); (c) "validation-plan pre-committed numbers" still = Arc G #14. Detail: reference-audit INDEX Finding #5. **Propagation pass (2026-06-20):** the one genuinely deep-dive-only new framing — the three-senses-of-problem-solving distinction — added to `background.html` §4; the durability caveat + the correlated-error/external-validation-is-structurally-necessary sharpening flowed into the **vision** (failure-modes + course-opportunity sections); the **executive brief was left as-is** (already careful — domain-embedded, technical-vs-design, "validation agenda not settled"). **Vision consolidated to HTML-only 2026-06-20:** the `gc-curriculum-tool-vision.md` twin (a non-compliant leftover — CLAUDE.md §"Doc format convention" already mandates HTML-only; the deep-dives were consolidated 2026-06-14) was **retired**; verified the `.html` carried all `.md` substance first; live pointers in `CLAUDE.md` + `superpowers/README.md` repointed to `.html` (append-only plan/spec files left as historical record).

- **Editorial-review pass — all 4 live docs — DONE 2026-06-20 (exec-brief + vision + background + problem-solving; GH-Pages docs).** From an external editorial review (flow/de-duplication/hedging). Executed all three tiers, Tier 3 (owner-per-concept de-dup) applied *selectively* to protect standalone readability. **Ownership matrix fixed:** KUD+/why-not-Bloom/evidence-rule → Background; problem-solving mechanism → deep-dive; 3-Act → 3-Act companion; through-line/surfaces/governance/**AI-cost** → Vision; 3-min what/why/status → exec brief. **Bug fixes:** Vision's "core questions" prose was self-contradictory (4 cards Q1–Q4 but prose said "*Both* questions… the *two* answers") → rewritten as "Q1/Q2 foundational pair + Q3/Q4 constraint-layer extensions"; reconciled the two-vs-four count across exec(2)/vision(2+2); cut the twice-stated evidence rule in exec; de-duped the near-verbatim AI-cost paragraph (kept in Vision, exec glosses + links); relabeled exec "Three-minute read"→"Overview". **Cuts:** exec 2,702→1,945 w (−28%, dropped the through-line section + per-surface paragraphs); vision 12,222→11,209 (KUD academic section −70% + its in-vision ref list, → link to Background); background 18,176→17,777 (stacked-hedging pass + §8 baselines halved — de-dup only, every citation/caveat preserved, verified vs diff); problem-solving 10,876→10,794 (front-matter consolidated to one "How to read" box; degrees-not-thresholds caveat de-duped 4×→1). **Added:** a "What to read" doc-map block to all four (cross-links + "you are here"). Stock-phrase sweep ("not a marketing assertion", "oral tradition", "genuinely living", "the whole point is"). **Verified post-edit:** all 4 tag-balanced (0 mismatches), figures 4/4 each, 55 background refs intact, no dangling nav anchors. Executed via 4 parallel Sonnet subagents against a shared spec + my own diff review. **Not done (review's optional items, left as debt):** the further optional figures (depth-vs-pedagogy 2×2, Macnamara bars, three-senses table, implementation pipeline) and any deeper Tier-3 wholesale de-dup beyond what protects standalone reading.

- **Hypotheses memo §4.2 added — 2026-06-20 ([`2026-06-19-curriculum-measurement-hypotheses.md`](./superpowers/specs/2026-06-19-curriculum-measurement-hypotheses.md)).** "Scale representation: integer judgments, continuous derivations" — the measurement-representation decision B and C both depend on: faculty input/storage/display of *direct* KUD judgments stays **integer 0–5** (ordinal anchored rungs; a 3.5 has no behavioral anchor; evidence-gates are integer thresholds), but every *derived* quantity (B2 decay `depth₀·e^(−t/τ)`, neighbor-foundation/prereq-gap aggregation, pull-up increment) is **float carrying an SEM-style interval** because rounding mid-computation erases the partial-erosion-below-the-next-rung signal that is the point of B. Guardrails: never write a float to a faculty-score field or display "D3.2" to faculty (bands/arrows instead); the interval-scale assumption (4−2==3−1) is stated, not assumed, because C forbids over-claiming — the honest derived value is "D3.1 ± 0.6", which *is* §4.1(b)'s SEM/interval-not-point output. Recorded as the (C) contract applied to B's arithmetic; not specced/built.

- **"KUD as the universal currency" keystone framing — added 2026-06-20 (memo + vision + background).** Names the previously-implicit-and-scattered idea that **KUD+depth is the unit of account the whole framework is denominated in** — the common K/U/D × depth axis that makes course output, prerequisite expectation, program coverage, and career demand *commensurable*, so boundary-crossing questions are arithmetic on a shared axis (prereq-gap = subtraction; curriculum = roll-up; career = coverage-vs-demand) rather than translation between vocabularies. The unifying frame for A/B/C: it's a **sound internal currency** (course→curriculum, one taxonomy) and a **pegged-but-lossy external one** (into careers — A's match-quality stamps are the FX spread); **B prices its decay** (instantaneous≠durable), **C governs its arithmetic** (interval-scale + SEM intervals, §4.2) **and its truth-claims** (commensurable, not true); and it's why §5 resolves as it does (KUD+depth is the *denomination comparisons settle in*, not a representation competing with embeddings). Why no alternative works: names/embeddings/O*NET-generic-skills/Bloom/credits all break at the boundaries. **Landed:** memo new "## The keystone" section + TL;DR bullet; vision through-line lead paragraph; background §1 "common metric / commensurability" paragraph (links §11–§12). Docs-only; GH-Pages.

- **Narrative-framing pass — all 4 published docs + grad-study retrofit — DONE 2026-06-21 (GH-Pages docs).** From an external framing review (audio critique + Reviewer-1 framing recs + a placement map): the docs had the right substance but the narrative was getting lost under mechanism/jargon. Fixes: lead with the problem + the **evidence-above-zero** rule; name KUD+depth as the **shared currency / universal translator** (course→curriculum→career); put the problem-solving **emergent-property definition + the honest equation** (*opportunity = upper-depth domain work + productive failure + structured reflection + abstraction across varied cases, read at the program level — "what the framework looks for, not a computed score"*) **before** the generic-skill teardown. **Per doc:** **background** §1 evidence-rule side-by-side table + §4 reorder (emergent-property-first) + §11 shared-ruler sentence; **vision** through-line shared-ruler + *illustrative* single-data-point walkthrough (down-edited the heavy 2026-06-20 currency paragraph) + producing emergent-property line; **problem-solving** equation + "relocated, not removed" atop the one-page version; **exec brief** (shipped earlier, `0b7e798`→`5a56438`) pain-first lede, KUD-vs-Bloom recast (job-requirement-match → universal-translator lead; dropped "too granular"), de-jargoned "Where it stands today" (no Phase codes / v1-v2 / MCP / route slugs), **removed the internal STATE.md link**, added a **graduate-outcome section** (criterion-relevance + working-hypotheses framing), depth-vs-coverage moved to a rules bullet. Calibration held throughout per Reviewer-1's wording guidance (criterion-relevance / disputable / opportunity — NOT validates/proves/measures); illustrative examples framed hypothetical + honest about awaiting-activation. **Grad-study doc (`graduate-outcome-validation.html`) retrofitted** to the family look (terracotta `#C8682A` / Playfair / Source-Serif / DM-Mono + "In this series" doc-map nav, replacing the navy/gold Georgia report style) and updated to the active design: status **in-progress**; **date error fixed** (Clemson coded set is **2022–2025**, not 2018–2026 — that range is the **ACCGC parent**, 603 records / 9 programs); extending toward **~10 years**; Data-Methods design written in (every-job / first-labeled, alignment **anchored on first destination**, later jobs as separate trajectory lane, pre-registration); stats relabeled "coded to date"; **no numbers invented**. **Pending number-update** when the student's extended N lands (the 268 + 9-of-268 / 237-of-268 / 43-categories + `docs/index.html` card all move together) — tracked at **Arc G #16**. Pre-pass backup (`.narrative-before-20260620/`) removed on deploy.

- **Hypotheses-memo harvest into `background.html` — DONE 2026-06-21 (GH-Pages docs).** Per the operator ("the background is effectively a lit search — 'not in the tool yet' isn't the criterion; lean to inclusion"), the *publish-ready* literature from the curriculum-measurement-hypotheses memo was pulled into background at **limits-confidence** (a boundary the framework owns, not grounding it claims). **Landed:** §6 two new subsections — *"Integer judgments in, continuous derivations out"* (ordinal-anchored input / continuous-with-intervals derivations; the ordinal-as-interval simplification named) and *"Depth is instantaneous, not durable"* (durability boundary: Murre-Dros two-component forgetting curve, Arthur ~1SD/yr decay, Bahrick permastore, spaced reinforcement; the HLR/FSRS decay-model flagged "recorded, not built, gated on graduate-outcome data"); §7 extended with no-clean-human-oracle (Landis-Koch κ 0.4–0.7), G-theory's "more items, not more raters" (Cronbach/Brennan), Many-Facet Rasch (Linacre); §13 **date error fixed** (Clemson coded set 2022–2025 not 2018–2026 — same fix as the grad doc; tag → "Data collection underway"); §15 +5 refs (Landis-Koch, Cronbach, Linacre, Murre-Dros, Settles-Meeder), 55→60, anchors resolve. **Left in the memo (the open pile):** B2 decay *parameters*, pull-up mechanism, A cross-walk design, embedder bake-off, sequencing, open Qs — build-direction, not defensible grounding. Memo stays the append-only R&D record.

- **Hypotheses published as a public companion + background §15 addendum — DONE 2026-06-21 (GH-Pages).** Operator: the harvested hypothesis material was getting *lost* scattered as in-body caveats; give the forward arc a home. Followed the doc family's own pattern (brief § + HTML deep-dive companion, like problem-solving / 3-Act). **New published companion `docs/measurement-hypotheses-deep-dive.html`** — *"Curriculum Measurement — Open Hypotheses & Research Agenda"*, a **cleaned, public, house-styled** HTML rendering of the memo's substance (keystone/currency, B durability, A cross-walk, C uncertainty, skill-relatedness/embeddings, scale representation, open questions, references). **Internal-ops stripped** (Arc refs, sequencing/"actionable now", $ caps, operator-leans, STATE refs, changelog date-stamps) — verified 0 leaks; forward/unvalidated register throughout; no invented numbers. The **internal memo `superpowers/specs/2026-06-19-…md` stays** the append-only R&D record (the companion is its public face, same as the deep-dives). **Background §15 "Open hypotheses and the research agenda"** addendum added (References → §16): one scannable block per hypothesis, each cross-linking its in-body bounded treatment + the companion; nav-link + doc-map row added. **Re-pointed** the 3 background→internal-`.md` links (§11×2, §12×1) to the public companion. Built via a Sonnet subagent against a keep/strip spec + the deep-dive template; render-verified house style. Deploy: companion + background shipped together so no link dangles.

- **TO EXPLORE / HYPOTHESIS (recorded in depth — [`2026-06-19-curriculum-measurement-hypotheses.md`](./superpowers/specs/2026-06-19-curriculum-measurement-hypotheses.md)) — durability/"bleed-off", competency→career cross-walk, uncertainty discipline (2026-06-19; NOT specced/built/sequenced).** From a skill-mapping-literature review × the framework. **(B) Durability/bleed-off** [headline; model GAP]: KUD = *instantaneous* (end-of-course) depth, but *durable graduation-time depth* differs — depth decays between courses unless reinforced (repeated × varied × recency consolidates; "2–3 D4's in a related area → a D4 that sticks"), and high-depth attainment "pulls up" related neighbors (knowledge integration, depth-gated; operator lean: durability-lift primary, maybe a level-floor too). Splits **B1 structural (buildable NOW; enriches the scaffolding analysis)** / **B2 quantitative (gated on graduate-outcome data — don't hard-code params)**; also sharpens Q2 prereq-gap (a prereq's depth *when the dependent course starts*, not when its course ended). **(A) Cross-walk** via [Course-Skill Atlas](https://www.nature.com/articles/s41597-024-03931-8) (national O*NET-DWA-by-major) — fuzzy/many-to-many, match-quality-stamped, "no clean match" = asymmetric finding (O*NET-coarse-for-GC vs our-blind-spot); embeddings shortlist → depth-aware LLM/human adjudicate; map *durable* depth (B), not instantaneous. **(C) Uncertainty discipline** [load-bearing]: no reliable oracle (humans + LLMs both ~<80%); the residual is mostly *irreducible* (label noise + ambiguity + under-specified inputs) → **manage uncertainty, don't chase it** (evidence-over-estimation, confidence-stamped revisable claims, convergence across signals, disagreement-as-signal, graduate-outcome-validation = the only systematic-bias check). **Resolved:** embeddings-vs-attention is *informational not mechanistic* — KUD+depth **is** the orthogonal factorization a generic embedding entangles; a depth-factored embedding would be cost/scale only, **not** accuracy. **⭐ ACTIONABLE NEXT (the foundation) — a structural competency-thread measurement over existing snapshots:** per competency, its exposures × KUD-depth × curriculum-sequence × variety + a lone-peak vs reinforced-thread flag, confidence-stamped. Simultaneously *learn* (is bleed-off even prevalent in GC?) and *build* (the substrate B2 / A / outcome-validation all plug into; durability-enriches the scaffolding analysis). Run as a **report first**; graduate to a scaffolding-analysis view if the phenomena are real. **Also actionable (no training):** an **embedder bake-off on our data** — §5.1's "leaderboard trap" (MTEB rank does NOT predict domain performance) means we pick our embedder blind; benchmark campus-Qwen vs a domain-adjacent vs a strong general-v3 on a small labeled spine eval before considering any fine-tune. **Domain-embedding research (§5.1, 2026-06-19):** domain-tuned embedders beat generic ones but it's a *recall*-stage win (composes with retrieve-then-reason; does NOT carry depth-entailment); **education** embedders exist off-the-shelf (skill/competency line, SPECTER2, training-domain SBERT), but **no print/graphics/packaging embedder exists** — closest adjacents are materials (MatSciBERT/MELT) / chemistry (ChEmbed) / perceptual-color; a GC-domain model = adapter/contrastive fine-tune over our own corpus, gated on the bake-off. **Deep dive (memo §2.7/§4.1, 2026-06-20):** **(B)** phenomenon well-grounded (forgetting curves; cognitive-skill decay ~1 SD/yr; Bahrick permastore = the durable-vs-instantaneous existence proof); **B2 has a borrowable form — spaced-repetition memory models (Half-Life Regression / FSRS), `Stability` grows per spaced success — lifted to (competency×course) scale (no prior model does this)**; "pull neighbors up" must be modeled CONSERVATIVELY (transfer literature pessimistic → bound to Knowledge-Space-Theory surmise-neighbors, never auto-lift on depth). **(C) is the best-supported hypothesis** (G-theory/SEM/Many-Facet-Rasch, aleatoric uncertainty, label-convergence ceiling, perspectivism, selective prediction all map to planks). **⚑ PRIORITY-CHANGER:** ensemble/convergence provably canNOT cancel the *correlated systematic bias* all LLM judges share (9-judge panel ≈ 2 effective votes — Kim et al. ICML 2025, Kohli 2025) → **graduate-outcome validation is structurally necessary, not optional**, and is the SAME dependency gating B2's params. **→ ACTED ON 2026-06-20: elevated to Arc G** (data collection starts week of 2026-06-22, student assigned; lock the validation plan #14 first). **Deferred:** B2 *parameters* (await the Arc G data), A (durable depth), depth-factored embedding (scale only), custom GC-domain embedder (gated on bake-off), `background.html` §3 occupational-frameworks row (O*NET-memo to-do). *Full reasoning + citations (the skill-mapping field trajectory + cognitive-science grounding + the B/C deep-dive literature) in the memo.*
- **✅ SHIPPED 2026-06-19 (merged to `main`+`dev` @ `32029ab`, prod rebuilt + restarted, root 200; `program` tenant backfilled).** [spec](./superpowers/specs/2026-06-19-cross-course-evidence-spine-design.md) · [plan](./superpowers/plans/2026-06-19-cross-course-evidence-spine.md). Phase 1 of a "graph search in RAG" exploration: lifts the already-embedded **per-course** material chunks into one **cross-course, semantic, citable** retrieval surface the curriculum-chat agent (`/ask` + Explore Ask) can query — so free-chat reaches the **primary source** (actual syllabi/assignments/rubrics/slides), not just curated wiki prose + KUD scores. Mechanism: a reserved **`program` Weaviate tenant** (`tenantForProgram()` → `coursecapture-program`) holding the union of all courses' chunks (reuses the existing `MaterialChunk` class + read path; **chunks-only in Phase 1 — no sections, so cross-course hits carry no parent-section enrichment yet**; adds a `courseCode` filter to `SearchInput`), rebuilt **incrementally on the snapshot hook** (independent of the wiki update; full rebuild on demand = backfill), deriving the current set from Postgres (`ready ∧ ¬ignored ∧ retired_at IS NULL`) + `uploadedAt`/`snapshotId` provenance stamps. **Shipped surfaces (on branch):** migration **0044** `course_materials.retired_at` (**already applied to the shared DB** — additive/nullable, old code ignores it); new tool **`search_curriculum`** (global / drill-down / per-course-diversified compare) registered on the curriculum-chat agent + MCP server; `lib/capture/program-index.ts` (`refreshProgramIndex`/`rebuildProgramIndex`); admin route **`POST /api/admin/program-index/rebuild`**; manual retire/restore control in the materials manager (`PATCH /materials/[id] {retired}`); material-chunk citation variant (strict-mode-safe); `docs/architecture.html` updated (per-course → curriculum-scale storage/retrieval incl. the spine). Full suite green (1553), tsc clean. **Source-of-truth framing:** Postgres authoritative; wiki + spine + per-course tenants are all derived projections. **DEPLOYED 2026-06-19:** merged to main+dev, prod `pnpm build` + `launchctl kickstart` (root 200), and the one-time backfill ran (`rebuildProgramIndex`, 47/47 courses → ~1,296 chunks in the `program` tenant across the 9 courses with indexed materials: GC 4440 254, GC 1040 243, GC 1010 184, GC 4060 172, GC 3460 137, GC 4800 88, GC 2400 85, MKT 4320 81, GC 3800 52). Smoke verified: `search_curriculum("color management", perCourse)` → 9 hits across 3 courses, diversified. (The backfill was run via `tsx --env-file=<deploy>/.env.local rebuildProgramIndex` since the admin route needs a Bearer second factor; the HTTP route remains for future on-demand rebuilds.) Future incremental refreshes fire automatically on snapshot creation. **DEFERRED within/around this spec (decisions, no diff):** (1) **Phase 2 concept cards / concept layer** — concept extraction + alias resolution (the GraphRAG entity-resolution risk) + per-concept cross-course synthesis into the wiki `concepts/` pages + concept-level diagnostics; agent does cross-course synthesis *live* from the spine for now; materialize later, judged on real chat quality. (2) **Auto-retire triggering** (snapshot-diff: content present-then-absent → retire candidate) — deferred; **depends on building snapshot→material/evidence linkage**, which doesn't exist (today `snapshot_target_coverage.evidenceExcerpt` is free text, no `materialId`); that linkage is its own spec + eval (asymmetric risk: bad attribution → retiring still-taught material), and is the *same* foundation a fully-automatic "index only latest-snapshot-endorsed material" approach needs (one investment). **Operator considered pulling the linkage into Phase 1 (2026-06-19) and chose to keep it deferred** — different surface (the scoring pipeline, not retrieval), only deferred features consume it (YAGNI), and the "capture-now-or-lose-it" pull is weak (changing courses get recaptured → fresh linkage). (3) **Material-version retention / primary-source "what changed" diffing** — spine is current-state only; narrative/structured change already lives in per-snapshot coverage + wiki git history, best surfaced later via a dedicated `course_history`/diff tool. (4) Whether `retired` should also exclude from capture/scoring (flagged, not decided). (5) Reranker/router on chat retrieval (noted, not planned). (6) **Material-chunk citation drawer hookup** — `search_curriculum` citations render a plain `courseCode · fileName` label in `AskTab`; the full citation-drawer resolution via `fetchChunkById` (as wiki-path citations get) is not wired for material chunks yet (Phase-1 minimal; the label is sourced + sufficient). (7) **Parent-section enrichment at program scope** — the program tenant is chunks-only, so cross-course hits return `parentSectionText: null`; add a shared section store later if needed.
- **PROPOSED (spec written, awaiting plan) — Tiered ingestion with a triage step (2026-06-18).** [`2026-06-18-tiered-ingestion-triage-design.md`](./superpowers/specs/2026-06-18-tiered-ingestion-triage-design.md). Splits Canvas/material ingest into **(1) discover & list** (pull the inventory with NO extraction so faculty prune/ignore for free) → **(2) triage & tiered ingest** (a screen with three auto-classified tiers — high: assignments/quizzes/rubrics → full pipeline; middle: per-**unit** summary — slides → per-slide **vision** notes, prose → per-section text summaries (both in v1); background: readings → one digest — faculty move/downgrade/ignore, then ingest). Middle tier renders each slide → one **gemma-4-E4B vision pass** on omlx (`{topic,teaches,key_visual,content_level}`, skip low-content), **embed per-slide for retrieval but cite at deck level** (slide index never surfaced — resolves the "citing slide 4" concern). Classifier is structure-first (Canvas types are authoritative), LLM only for the ambiguous file bucket on cheap signals (filename/mime/size/peek — no extraction). Adds a per-row **time estimate** + concurrency-adjusted total by the Ingest button (calibrated from `[ingest]` stage logs + measured ~1s/slide vision; ranges not stopwatch; time-focused since local/campus is $0). Directly motivated by the Canvas-import slowness trace (synchronous per-file Docling extraction). Reuses the ingest queue (+ new `tier` column), `generateMaterialDigest`, full pipeline, LibreOffice render, the omlx vision path. **Supersedes (in part)** the guided-sources spec below — "Import" becomes "list for triage," not ingest. **Increment 1 IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-1-phase1-list.md`](./superpowers/plans/2026-06-18-tiered-ingestion-1-phase1-list.md)) — **behind the new `COURSECAPTURE_TRIAGE` env flag (default off → today's import behavior byte-for-byte unchanged; NOT deployed/flipped).** When `COURSECAPTURE_TRIAGE=1`, Canvas import runs list-mode (`app/api/courses/[code]/canvas-import/list-import.ts`): fetch+assemble as usual, **download file bytes + `putLocal` but no Docling extraction**, insert every row `indexing_status='pending'` with **no `enqueue`**, return `{manifest:{rows,skipped,decksPresent}}` with cheap size probes (`lib/capture/size-probe.ts` — PDF pages via unpdf, PPTX slides via yauzl, never-throws). New `lib/capture/triage-flag.ts` (`isTriageEnabled()`). Unsupported/oversized/failed-download files surface in `manifest.skipped[]` (not stored). 225/225 capture+api+courses tests green, tsc clean. **Increment 2a IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-2a-classifier.md`](./superpowers/plans/2026-06-18-tiered-ingestion-2a-classifier.md)) — **classifier + tier persistence.** New `course_materials.tier` column (`'high'|'middle'|'background'|null`, migration **0042**). New `lib/capture/material-tier.ts`: `classifyByKind` (graded→high, instructional→middle, file→null) + `classifyFile` (PPTX/slide → middle deterministic, else the new light AI function **`material-classify`** decides deck=middle vs reading=background, **biased to background on any error/low-confidence**) + `classifyManifestItem`. `runListImport` classifies + persists `tier` per row (`updateMaterialTier`) and includes it in the manifest; legacy `runImport` untouched. **Tier is persisted but NOT yet honored by the worker** — that's Increment 3 (a flag-on ingest today would still run the existing full pipeline for every row). 596 capture+api+ai tests green. **Increment 2b IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-2b-triage-ui.md`](./superpowers/plans/2026-06-18-tiered-ingestion-2b-triage-ui.md)) — the **ingest screen** (first user-visible depth surface). **Operator-corrected design:** Phase-1 listing/pruning **reuses the existing materials UI** (the Step-1 source boxes — unchanged); the tier-sorted screen is a **separate step AFTER materials**, reading the **live `CaptureMaterial[]`** (now carries `tier`), not the import manifest. New `app/capture/[code]/TriageStep.tsx`: groups live materials into high/middle/background sections (null-tier → high bucket), per-row **▲/▼ upgrade/downgrade** (`PATCH /materials/[id] {tier}`), **ignore** (`PATCH {ignored}`), **delete** (`DELETE`), positive slides-nudge (shown only when no middle material), and **Ingest & continue** (`POST /admin/v2-backfill` → enqueue all non-ignored → interview). `PATCH /materials/[id]` now also accepts `tier`. Wired as a flag-gated `landingStep==='ingest'` in `CaptureClient` between materials and interview (`onContinue` → `isTriageEnabled() ? 'ingest' : 'interview'`); **flag-off path byte-for-byte unchanged**. Time estimates omitted (Increment 4). **Tier still NOT honored by the worker** (Increment 3) — Ingest runs the existing full pipeline for every kept row; the screen records tiers for when Inc 3 lands. 1414 suite green, tsc clean. **Increment 3a IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-3a-tier-routing.md`](./superpowers/plans/2026-06-18-tiered-ingestion-3a-tier-routing.md)) — the worker now **honors `tier`**: `finalizeExtraction` takes `tier`, and **`background` materials embed the digest as a single retrieval unit** (skip `chunkMaterial`/`contextualizeChunk`); `high`/`null`/`middle` fall through to the existing full pipeline **unchanged** (branch sits after the no-vectorStore guard, before the chunk path, and returns). `Tier` type consolidated to `lib/capture/material-tier.ts`. 138 capture tests green (9 new). **Increment 3b IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-3b-slide-vision.md`](./superpowers/plans/2026-06-18-tiered-ingestion-3b-slide-vision.md)) — the **middle-tier slide-vision path**. New `lib/capture/render-pages.ts` (`renderToImages` — PDF→PNG via **poppler `pdftoppm`** + PPTX/legacy→PDF via **`soffice`**, both already installed; 60-slide cap, temp-dir cleanup, never-throws) + `lib/capture/slide-vision.ts` (`describeSlide` — omlx **gemma-4-E4B** vision via `LOCAL_BASE_URL`/`LOCAL_API_KEY`, env `SLIDE_VISION_MODEL`, safe-default on any failure, 60s timeout). `finalizeExtraction` middle branch (for slide-like materials with `fileBytes`): render → per-slide vision note (concurrency-capped at 4) → keep `substantive` → embed each note as one chunk with **`sectionTitle = fileName` (doc-level citation; slide ordinal only in the internal record id)** + a deck-rollup section → upsert. **Graceful fallback:** render `[]` / all-low / any error → falls through to the existing full chunk pipeline (no stuck rows, no double-write). `processMaterial` threads `fileBytes`/`mimeType`. Live-smoked: `describeSlide` → omlx returned a real structured note in ~4.8s. 256 capture+api tests green, tsc clean. **Increment 3c IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-3c-prose-section.md`](./superpowers/plans/2026-06-18-tiered-ingestion-3c-prose-section.md)) — the **middle-tier prose-section path** (no new AI function/infra). In `finalizeExtraction`'s middle branch, after the slide path falls through, a non-slide middle material is split via `chunkMaterial`'s `sections`, sections ≥200 chars are kept (need ≥2, else fall through to full pipeline), each summarized via `generateMaterialDigest` (concurrency-capped at 4), and embedded as one chunk per section with **`sectionTitle=fileName` (doc-level citation; section index/title only in the internal record id)** + a doc-rollup section. Branch order for middle: **slides → prose → full**; any error → full pipeline (no stuck rows). 262 capture+api tests green. **✅ Increment 3 COMPLETE** — the worker now fully honors all three tiers: **high**=full pipeline, **background**=digest-as-single-unit, **middle**=slides-via-vision OR prose-via-section. **Increment 4 IMPLEMENTED** (on `dev`+`main`, subagent-driven; plan [`2026-06-18-tiered-ingestion-4-estimates.md`](./superpowers/plans/2026-06-18-tiered-ingestion-4-estimates.md)) — **time estimates on the ingest screen.** New pure `lib/capture/ingest-estimate.ts` (`estimateSeconds` per material by tier × size signal — pageCount/text-len/bytes — calibrated from `[ingest]` logs + ~1s/slide vision; `formatDuration` coarse buckets; `estimateTotal` = Σ ÷ concurrency(2) as a RANGE). `TriageStep` shows a per-row estimate chip + a live total ("Estimated: ~X–Y · 2 at a time · rough estimate") derived inline so it recomputes on every upgrade/downgrade/ignore. Time-only ($0). 1495 suite green. **Fix 2026-06-18:** the client-side flag gate was dead — `CaptureClient` (a client component) read `process.env.COURSECAPTURE_TRIAGE`, which isn't in the browser bundle, so the ingest step never showed even with the flag on. Now resolved server-side in `page.tsx` (`isTriageEnabled()`) and passed as the `triageEnabled` prop; tests drive it via the prop (a module mock had masked the boundary). **✅✅ TIERED-INGESTION FEATURE COMPLETE** behind `COURSECAPTURE_TRIAGE` (default off): list → triage (existing materials UI) → tier-sorted ingest screen (upgrade/downgrade/ignore/delete + estimates) → tier-aware worker (high=full, background=digest-unit, middle=slides-via-vision/prose-via-section). **SHIPPED 2026-06-18:** `COURSECAPTURE_TRIAGE=1` added to the deploy-worktree `.env.local`, prod rebuilt (`pnpm build`) + `launchctl kickstart`-restarted; root 200, INSTRUCTIONS/Canvas-first confirmed live. **The tiered flow + guided Canvas-first reorder are now LIVE for all faculty on :3000** (operator chose to deploy + flip on, will validate by re-capturing a real course). Caveats now in production: guided Task 2 (progressive-disclosure lock/unlock) still UNBUILT — faculty get the reorder but not gating; "Add slides" is a stub; the slide-vision/prose-section ingest paths are getting their FIRST real-faculty exercise. To revert: remove the flag line from the deploy `.env.local` + rebuild/restart. **Post-go-live fixes (2026-06-18) — SHIPPED to prod (rebuilt `pnpm build` + `launchctl kickstart`; root + capture 200; `raw_cleared`+`tier` columns confirmed in the shared DB):** (1) materials **upload path** now flag-aware list-mode — under the flag it stores + classifies a tier but does NOT enqueue (processing waits for Ingest), and the **upload cap raised 15MB→100MB** (real lecture decks were rejected); flag-off unchanged (`5045584`). (2) **migration `0043`** adds `course_materials.raw_cleared`; on snapshot approval a flag-gated, best-effort `clearRawBlobsForCourse` **deletes local raw blobs** (uploaded + downloaded Canvas attachments; remote refs skipped) to reclaim storage + cut PII-at-rest — keeps text/digest/vectors; row shows "original cleared after approval" + hides preview (`b759498`,`8217530`). **NOTE: 0043 is ALREADY applied to the shared dev/prod DB** (the column exists; old running prod code ignores it via its default — safe). (3) UI polish: Syllabus header un-bolded; uniform source-box header spacing (`9f12e90`,`c3ed481`). **(4) Materials-row UX (SHIPPED 2026-06-18 — prod rebuilt `pnpm build` + `launchctl kickstart`; root + view 200):** in the Phase-1 source boxes (CanvasBox/OtherMaterialsBox) the per-row **indexing status (dot + "not indexed yet") and "Index now"** are now **hidden when `COURSECAPTURE_TRIAGE` is on** — indexing is deferred to the Ingest step, so "pending" is the expected state, not an error (threaded via a `triageEnabled` prop; flag-off unchanged). A **FERPA/auto-set-aside row that faculty override** ("Include anyway") now **looks normal** in all three surfaces (CanvasBox, OtherMaterialsBox, MaterialsPanel) — the amber why-ignored band + row dimming key on `ignored` only (was `ignored || autoSetAside`), so an overridden row carries no persistent warning. **(5) Upload speed + progress bar (SHIPPED 2026-06-18 — prod rebuilt `pnpm build` + `launchctl kickstart`; root + view 200):** tier classification (probe + `material-classify` LLM) was running **synchronously on the upload request**, adding seconds of fixed per-file latency regardless of size; it now runs in the **background via Next `after()`** (bytes already in memory; pipelines with the next upload) so `POST /materials` returns the instant the file is on disk. `TriageStep` refetches tiers on mount (bounded poll until settled; stops on first user edit) so background-classified tiers land before ingest. New `lib/capture/upload-with-progress.ts` (XHR upload reporting **real byte-level progress** — `fetch` can't report upload progress) + shared `app/capture/[code]/UploadProgressBar.tsx`, wired into all three `/materials` upload sites (Other materials, Syllabus, MaterialsPanel manager). 1533 suite green, tsc clean. **(6) `.imscc` cartridge upload hidden for Clemson faculty (SHIPPED 2026-06-18):** the `.imscc` fallback (the no-Canvas-token path — slow, big-file, no API) is now shown **only to external scoped testers**, who lack a Clemson Canvas API token; Clemson faculty see just the API-token import + Add files. UI-only — the `imscc-import` endpoint stays (it's on the sandbox allowlist; testers need it). Threaded a `showImscc` prop CanvasBox ← CaptureMaterialsStep ← CaptureClient ← `page.tsx` (`isExternalTester = isScopedForThis`); `showImscc` defaults true on CanvasBox (direct callers/tests unchanged), false on CaptureMaterialsStep (faculty default). Because faculty no longer reach the `.imscc` upload, the no-progress-bar concern below is now moot for them. **Deferred to-dos:** Add-slides upload wiring (stub), `EXT_TO_MIME` dedup. (Dropped: `.imscc` progress bar — only external testers hit that path now; revisit only if they complain.) **Debt:** (a) the ingest-screen **"Add slides" nudge button is a stub (no-op)** — wire it to the Other-materials add-files upload handler (deferred to-do; nudge copy is final). (b) **✅ RESOLVED 2026-06-18** — the byte-identical `EXT_TO_MIME` in `canvas-import/route.ts` + `list-import.ts` was extracted to `lib/canvas/ext-to-mime.ts` (both import it; zero behavior change). **FULLY RESOLVED 2026-06-18** (`f9b18b3`): all 5 paths now import the canonical SUPERSET `lib/canvas/ext-to-mime.ts` (16 entries; no path lost a mapping). **Approved behavior change:** `canvas-reextract` (route + `scripts/reextract-canvas-files.ts`) had a deliberate guard that SKIPPED legacy Office ("re-save as modern"); it now ACCEPTS legacy `.doc/.ppt/.xls` and converts+extracts them via `extractText`, matching `canvas-import` (skip only when `!isSupported && !isLegacy`). 311 tests green incl. 10 new reextract tests.
- **PARTIALLY IMPLEMENTED 2026-06-18 — Capture Step-1 guided Canvas-first source sequence (spec 2026-06-17). **Task 1 BUILT** (flag-gated under COURSECAPTURE_TRIAGE; plan `2026-06-18-capture-step1-guided-reorder.md`, commit `11deae3`): when the flag is on, the materials step reorders to **Canvas → Syllabus → Other**, adds an INSTRUCTIONS paragraph ("surface everything, more is better"), widens the framing blurb (`CaptureWhyBlurb wide` prop), and relabels the button "Continue →"; flag-off byte-for-byte unchanged (tested). `triageEnabled` threaded as a prop (not env — client component). **Task 2 PENDING:** addressed-state progressive disclosure + per-box fresh/locked controls (the bigger piece). NOT the earlier "superseded" framing — it is being implemented, just behind the flag alongside the rest of the new capture flow.** [`2026-06-17-capture-step1-guided-sources-design.md`](./superpowers/specs/2026-06-17-capture-step1-guided-sources-design.md). UI/interaction redesign of the Step-1 confirm-materials screen (`CaptureMaterialsStep` + the three boxes + `CaptureWhyBlurb`): widen the top blurb to box width (prop-gated — shared with Step-2 hero), add a labeled INSTRUCTIONS paragraph ("surface everything, more is better"), reorder boxes **Canvas → Syllabus → Other**, and add **addressed-state-driven progressive disclosure** (a box is locked/gray until every prior box is addressed; **addressed = imported/synced/skipped**, so a pre-synced syllabus or a populated course shows nothing grayed). Fresh-state controls: Canvas [Import][Skip]; Syllabus [Use Existing (enabled iff Canvas carried a syllabus)][Import][Skip]; Other [Add files][Skip]. **Skip is session-local + non-blocking** (never disables Continue; not persisted — recomputed from real data on reload). No schema/endpoint change. Spec at user-review gate.
- **PROPOSED (spec written, awaiting plan) — common-failure-mode elicitation → productive-failure evidence (2026-06-17).** [`2026-06-17-common-failure-modes-productive-failure-design.md`](./superpowers/specs/2026-06-17-common-failure-modes-productive-failure-design.md). Prompt-only change to `capture-chat-agent.md`: Area 9 (signature projects) gains a positive, de-identified per-project elicitation — *"where do students typically stumble on this?"* — and Area 7 interprets the named failure modes as the evidence substrate for conditions (c)/(d)/(e), with a contextual "converted-vs-eaten" resolution probe (an unconverted/eaten failure corroborates nothing — it's the tell for unproductive-success). Operator-locked decisions: **internal evidence only** (no schema/field/`/view`/wiki surface), **contextual** resolution follow-up (not a required pairing), no readiness gating, claimed-tier evidence (can't lift the band above artifact evidence). Prompt-guided not code-enforced; verification is stress-test + manual walkthrough. Spec at user-review gate (brainstorming → writing-plans next if approved).
- **Override-rationale follow-ups — three deferred in the spec, kept parked (operator decision 2026-06-17).** From [`2026-06-16-kud-override-rationale-design.md`](./superpowers/specs/2026-06-16-kud-override-rationale-design.md) §"What this does NOT include". (1) **Surface overrides downstream** — `reviewer_overrides` (shape `{statement, changes[] per-dim from→to, reason}`, `schema.ts:316`) is **written into every approved snapshot but read nowhere**: zero usages in `/view` or `/program`. Rendering "Faculty raised D2→D4: ‹reason›" on `/view/<code>` + the `/program` matrix cell drawer is **render-only, no schema change** — the highest-value, lowest-risk of the three (it's the public-trust payoff; without it an edited score silently detaches from its cited evidence). Recommended first when picked up. (2) **Cumulative cross-session drift detection** — the guard compares against the session-loaded baseline, so it catches in-session bumps but not multi-session creep (a prior inflated save becomes the new baseline); closing it needs synthesis to stamp the **immutable original AI scores** (a schema change). (3) **Per-dimension reasons** — one reason per competency was a deliberate friction tradeoff (`changes[]` already records each K/U/D from→to); reversing adds friction for marginal gain. (Downward-edit justification was decided *out of scope*, not deferred.) Operator reviewed all three 2026-06-17 and chose to keep them parked (scoping only).
- **TO BRAINSTORM — KUD depth-scale anchor examples: relatability + inflation-inviting presentation (parked 2026-06-16).** The per-dimension K/U/D level examples shown under each slider in the review panel (the depth-scale anchors — `DEPTH_ANCHORS` / `describeDepth` in `lib/ai/capture/depth-anchors.ts`, rendered by `DepthSlider` in `ProfileReviewPanel`) aren't always relatable to the course/domain, and the operator finds their presentation in the final-approval flow **nudges instructors to score higher** (the higher-level anchor reads as aspirational/flattering). Brainstorm: (a) more domain-relatable anchor language (per dimension, perhaps per track/level), and (b) a presentation that doesn't bias upward — pairs with the "score low, correct upward" ethos and the just-shipped override-rationale gate. Backlog #5.

- **✅ SHIPPED 2026-06-16 (deployed `0e33796`, prod mode) — instructor/auditor picker free-add.** Shared `InstructorSelect` (hero + materials step + mid-session badge) adds an "Add a new name…" option → text input; typed name used for this capture + stored on the snapshot (use-now-only, no global roster change). Backlog #6 done. Original framing: When an instructor adds a new course (`/courses/new`) or opens an existing one, the flow reaches a **prepopulated instructor/auditor picker** (the Step-1 auditor chooser, shared chooser state — see Active-arc Round 6) with **no way to enter a NEW instructor name** — you can only pick from the existing list. Need a free-add affordance (type a new name → use it, persist for reuse). Surfaces: the auditor-picker component + the instructor-list source; `instructorName` is carried on the snapshot. Likely a small fix rather than a brainstorm, but parked with the list. Backlog #6.

- **TO BRAINSTORM — employer-named transferable competencies (parked 2026-06-16).** Industry feedback named the competencies that matter to them as: *storytelling from data · teamwork · relationship building · critical thinking · written and verbal communication.* These don't map cleanly onto the current model and need a design session: *written/verbal communication* ≈ the existing **Communication** foundational; *critical thinking* relates to the program-level **problem-solving emergent-property** thesis (not a taggable per-course competency); but *teamwork* and *relationship building* are **not** in the five baseline foundationals (Agency, Attention to Detail, Resilience, Curiosity, Communication); and *storytelling from data* is a hybrid technical+communication competency. Open question: extend the foundational set? add a transferable-/durable-skills layer? map onto existing constructs and surface them in the demand side (position capture)? No design yet — brainstorm before touching the schema or the foundational list. (First of a couple of backlog items the operator is collecting — more to come.)

- **✅ SHIPPED 2026-06-16 (deployed `20368d5`) — require a rationale when faculty override K/U/D scores at approval.** [Round 10, item 1.] Backlog #2 done. Original framing retained for history: Operator: faculty can drag the sliders and approve with no justification. Worse, the **A15 approve guard inverts the incentive** — `approveUnlocked = dirty || allWorthLookReviewed || noteSubstantive`, so *making an edit* (dirty) alone unlocks Approve; editing scores currently demands *less* justification than leaving them. Overriding an evidence-grounded score is exactly when a recorded reason matters (trust model / evidence ladder — an edited score otherwise silently detaches from its cited evidence). Design questions for the session: per-competency reason vs one session-level "why I changed scores" note; how to detect "a change" (diff `working` vs the original profile per competency); gate Save or only Approve; and how the rationale flows into the snapshot's audit trail. No design yet — brainstorm before changing the guard. (Backlog item #2 the operator is collecting.)

- **✅ SHIPPED 2026-06-16 (deployed `20368d5`) — course-emphasis on `/view/<code>` de-granularized** (centrality bands, no precise pts·%). [Round 10, item 2.] Backlog #3 done. Original framing: The `course_emphasis` block shows precise points/percentages per competency; the operator finds exact point values invite pushback. Want the big-picture "where the efforts are" — the **centrality bands** (central / supporting / peripheral, already derived: ≥20% / 5–19% / <5% of graded points) or a coarse visual — *without* the precise point/percent figures. Likely a small `CapturedView` rendering change (drop/soften the numbers, keep the band). Backlog item #3.

- **✅ SHIPPED 2026-06-16 (deployed `20368d5`) — problem-solving / productive-failure reframed on both surfaces** (dropped interview nag + "one last question" button; `/view` qualitative band + roll-down). [Round 10, item 3.] Backlog #4 done. Original framing: Two linked operator concerns: **(a) Interview side** — keep the AI *tracking/synthesizing* problem-solving (Area 7) evidence, but **stop calling attention to it in the chat** — drop the bottom-of-turn sentence that flags the instructor when problem-solving hasn't been covered (many courses don't aim at it; the nag reads as a deficiency). Capture it quietly, don't prompt for it. **(b) Course-view side** — the productive-failure section on `/view/<code>` shows every condition as "absent" for the many courses not trying to build it, which causes consternation (reads as failure, not "not applicable"). Reframe to a single **qualitative summary**: *"This course shows [none / slight / moderate / significant] evidence toward building habits of problem-solving and critical thinking,"* with the detailed conditions behind a **collapsible roll-down**. Design Qs: what derives the qualitative level (count/depth of present Area-7 conditions, at synthesis or at render?); wording of the bands; keep the detail discoverable but not alarming. Surfaces: `capture-chat-agent.md` Area 7 + readiness gating; `CapturedView`'s Area-7/productive-failure section (`Area7Conditions`). No design yet — brainstorm. Backlog item #4.

- **Background-ingest worker — in-process, dies on web-server restart (2026-06-16; part (a) RESOLVED 2026-06-17).** The ingest worker (`lib/capture/ingest-queue.ts`) runs inside the Next server process (chosen for v1 simplicity — no new launchd service). Boot-recovery (`resetStuckIndexing`) re-queues anything left `indexing`, so nothing is lost. **(a) RESOLVED 2026-06-17:** a Next root-level `instrumentation.ts` `register()` now calls `ensureWorker()` on every server boot (Node runtime only, wrapped so a failure can't block startup; `ensureWorker` hardened with a `.catch` so a boot-recovery crash can't surface as an unhandled rejection) — leftover `queued`/stuck-`indexing` rows now drain on restart without waiting for the next upload. Residual: **(b)** the enqueue/idle race is closed by a wake-flag but a sub-tick window self-heals on the next enqueue. The most robust form (a dedicated launchd worker behind the same Postgres queue) is still deferred. `MAX_CONCURRENCY=2` is an untuned starting point.
- **✅ DONE 2026-06-16 — switched to production mode (`next start`).** The launchd `com.gc.curriculum-tool` plist `ProgramArguments` changed `pnpm dev:lan` → **`pnpm start:lan`** (`next start --hostname 0.0.0.0`); a production `pnpm build` (`next build --turbopack`) was run in the deploy worktree first. Result: "✓ Ready in 246ms", root 200 in ~0.13s, routes serve in 20–60 ms (no on-demand compile), and the dev-only "Response body disturbed or locked" 500 surface is gone. **⚠️ THE DEPLOY RITUAL CHANGED:** a code change is now `git merge dev → cd /Users/admin/projects/curriculum_developer-deploy → pnpm build → launchctl kickstart -k gui/$(id -u)/com.gc.curriculum-tool`. **`next start` serves the prebuilt `.next/` — a `git merge` + restart WITHOUT a `pnpm build` ships STALE code.** Docs-only changes don't need the build (no runtime impact). To revert to dev mode: swap the plist back to `dev:lan` + `launchctl bootout`/`bootstrap` (backup at `/tmp/com.gc.curriculum-tool.plist.bak`).
- **✅ ACTED ON 2026-06-17 — `chunk-contextualize` + `material-digest` switched to campus `gptoss-120b` with `gpt-5.4-mini` automatic fallback.** New `lib/ai/analyze/chunk-llm-provider.ts` exposes `chunkLlmComplete(funcId, args)`: campus `gptoss-120b` first (constructed with `reasoningEffort:'low'` so reasoning isolates into a separate field and `content` stays clean JSON), falling back to the function's configured provider (OpenAI `gpt-5.4-mini`) on **any** campus error — unreachable, non-JSON, or a `validate` rejection. Both consumers (`contextualizeChunk`, `generateMaterialDigest`) now call it and return the actually-used model for provenance. New env knobs: **`CHUNK_LLM_SKIP_CAMPUS=1`** forces the OpenAI path; **`CHUNK_LLM_CAMPUS_MODEL`** overrides the campus model (default `gptoss-120b`). `CampusProvider` gained an optional `{reasoningEffort}` ctor opt. Tests: `tests/ai/analyze/chunk-llm-provider.test.ts` (campus-on-success / OpenAI-fallback-on-throw / skip-when-flag / skip-when-env-absent / model-override); the two consumer tests pin `CHUNK_LLM_SKIP_CAMPUS=1` so the mocked provider is exercised regardless of `.env.local` campus vars. Open decision still: re-check under real concurrent campus-cluster load (latency was measured on-campus, single judge pass, not under contention). Original bake-off detail retained below for history.
- **Chunk-LLM provider bake-off — campus `gptoss-120b` vs `gpt-5.4-mini` — RAN 2026-06-16, gpt-oss-120b won on every axis.** Harness `scripts/bench/chunk-llm-bakeoff.ts` (6 varied GC chunks, gpt-5.5 pairwise judge). **Result: oss-120b avg 411 ms/call vs mini 1192 ms (~3× faster); 6-chunk burst 1.1 s vs 1.9 s; strict-`{blurb}`-JSON 0 failures both (thinking-off gate PASSES — gpt-oss at `reasoning_effort:low` isolates reasoning in a separate `reasoning` field, content stays clean JSON); gpt-5.5 judge preferred oss-120b 6/6; cost $0 (campus) vs OpenAI per-token.** **Real-chunk re-run (`scripts/bench/chunk-llm-bakeoff-real.ts`, GC 2400 "Canvas: Assignments" → production `chunkMaterial`, 10 real chunks) CONFIRMS it: oss-120b avg 565 ms vs mini 2475 ms (~4.4×, incl. a 15.7 s mini outlier); 10-chunk burst 0.94 s vs 1.17 s; 0 JSON fails both; judge oss 8 / mini 2 / ties 0.** Quality nuance from the real run: mini's blurbs sometimes add document-sequence context ("after Coding 1, before the final site") that oss's more formulaic "appears under the 'X' heading" lacks — the 2 mini wins were those; the gap is modest/stylistic, not a chasm. Caveats: single judge pass, latency measured on-campus (proximity helps), and NOT measured under concurrent campus-cluster load (the contention-relocation risk is unverified). **Recommendation:** flip `chunk-contextualize` + `material-digest` to campus `gptoss-120b` (per-function provider/model override — config, not code; the background ingest worker benefits automatically). Open decisions before flipping: (a) keep OpenAI `gpt-5.4-mini` as an automatic fallback when the campus endpoint is busy/down; (b) re-check under real concurrent load. Fallback ladder still: 120B → campus `qwen3.6-35b` → `gpt-5.4-mini`. Provider plumbing note: `CampusProvider.complete` would need to pass `reasoning_effort:'low'` (extra body) for gpt-oss.
- **✅ FIXED 2026-06-17 — `tests/courses/extract-text.test.ts` env-leak.** Root cause: `extractText` routes through `getExtractorFor` (`material-extractor.ts`), which the suite did **not** mock — only `mammoth`/`unpdf`/provider. When `.env.local` was sourced it set `PDF_PARSER=docling` + `DOCLING_URL`, so DOCX/PDF hit the real `DoclingExtractor` (HTTP :5001), bypassing the mocks entirely → all 8 timed out at 5s (~40s total). Fix: `delete process.env.PDF_PARSER` / `DOCLING_URL` in `beforeEach` (same env-independence pattern as the route suites), forcing the default `unpdf` path where the existing mocks apply. Verified green both with and without `.env.local` (8/8, ~380ms).
- **Signature Projects Capture — Part B (position-capture employer-rating step) deferred (2026-06-16).** Part A (capture + store + wiki `## Signature projects`) shipped 2026-06-16 (see Active arc Round 8). Part B — surfacing wiki-captured projects to an employer during position capture and having them rate/comment on the most relevant ones — was intentionally held: build the content layer first, get signature-project coverage across the captured courses, *then* design the rating interaction against real data. Not scheduled. Backing detail: [`2026-06-16-signature-projects-capture-design.md`](./superpowers/specs/2026-06-16-signature-projects-capture-design.md) (§"What this does NOT include").

- **External-university testing arc — (1)+(2)+(3)+(4) SHIPPED — ARC COMPLETE (2026-06-15).** A four-sub-project arc lets an outside party capture *their* course, isolated from the GC program record, and leave with a self-contained result. **(1) Course scope & lifecycle model — SHIPPED 2026-06-15** (merged to dev/main, `feat/course-scope-lifecycle`; **migration 0039** adds the two columns; **dormant** — behaviorally a no-op until a non-gc/non-offered course exists, since all existing courses are backfilled `gc`/`offered`). Foundation: two orthogonal columns on `courses` (`scope` `gc|external`, `status` `offered|proposed|sandbox|retired`) + one central `isProgramVisible` predicate (`scope='gc' AND status='offered'`) routed through *every* rollup (public list, matrix, wiki compile, scaffolding, prereq, `/view`, `/ask`/MCP) so external/proposed never leak; operator-minted scoped magic-link (partner-session pattern) for tester access; sandbox `/view`+`/okf` private to link+operator. Spec: [`2026-06-15-course-scope-lifecycle-design.md`](./superpowers/specs/2026-06-15-course-scope-lifecycle-design.md); implementation plan (9 tasks, data-model foundation only — external-access auth is its own plan): [`2026-06-15-course-scope-lifecycle-plan.md`](./superpowers/plans/2026-06-15-course-scope-lifecycle-plan.md). **(2) IMSCC (Common Cartridge) import — SHIPPED 2026-06-15** (merged dev→main, `feat/imscc-import`, 7 commits; spec [`2026-06-15-imscc-import-design.md`](./superpowers/specs/2026-06-15-imscc-import-design.md); 8-task plan [`2026-06-15-imscc-import-plan.md`](./superpowers/plans/2026-06-15-imscc-import-plan.md)): new route `POST /api/courses/[code]/imscc-import` (multipart `.imscc` upload). Parser (`lib/canvas/parseImscc.ts`) emits the same `CanvasCourseData` shape as `fetchCanvasCourse` and feeds a shared `assembleCanvasMaterials` (`lib/canvas/assemble-canvas-materials.ts`, extracted from the canvas-import route — behavior-preserving), so a `.imscc` upload produces identical `Canvas:` materials. Full parity incl. quizzes (QTI 1.2 reader, `lib/canvas/parseQti.ts`). Large-cartridge handling: stream to temp file + `yauzl` random-access reads + skip media/over-cap files (the size hogs have no capture value). New deps: `yauzl` + `fast-xml-parser`. Upload affordance in the Canvas box (single + per-slot). The external content path (Canvas-token is institution-bound); also stands alone for any faculty. To let real (hundreds-of-MB) cartridge uploads through, the global `middlewareClientMaxBodySize` was raised 25MB→**600MB** (`next.config.ts`; each route still enforces its own tighter cap — IMSCC route caps at 500MB). The route buffers the upload to a temp file via `arrayBuffer()` (off-heap, fine for a few-hundred-MB cartridge); a `file.stream()` pipeline was tried and reverted — a `File` from `req.formData()` is body-consumed, so `.stream()` throws undici's "Response body object should not be disturbed or locked" (comment in the route warns against re-attempting). **The same TypeError also fired from Node-runtime middleware body-replay on real-size uploads (a 65MB cartridge 500'd before the route ran);** fixed exactly as `/api/transcribe` was (2026-06-12) — `api/courses/<code>/imscc-import` is **excluded from the middleware matcher** and the route enforces Basic Auth itself via `authorizedForBasicAuth`. **First real-cartridge run (a 65MB GC 4800 export) also surfaced two follow-ups:** (i) the parser's `MIME_MAP`/`ALLOWED_EXTS` only listed `.pdf/.docx/.pptx/.txt/.html` so legacy `.doc/.ppt/.xls` + `.xlsx` content files were wrongly dropped — now included (the route's Docling path reads them via `isLegacyOfficeMime`); (ii) skipped files (over the 25MB per-file cap = the "big stuff" we deliberately ignore, or unreadable media) are now **returned in the response** (`skipped: {name, reason, sizeBytes}[]`) and surfaced in the Canvas-box message ("Skipped 1 large file — upload separately if needed: …"), and the route logs per-material `indexing N/M … in Xms` so the slow stage is observable. **Real-cartridge upload + parse now confirmed working (65MB GC 4800 export); full end-to-end NOT yet confirmed** — the first run appeared to stall in the per-material `finalizeExtraction` indexing stage (chunk → `contextualizeChunk` LLM-call-per-chunk → embed → Weaviate upsert) and was aborted; the new per-material logging is to pin where the time goes on the next run. **Indexing-slowness + progress-ticker — DEFERRED (see own entry below).** **(3) Self-contained OKF bundle export — SHIPPED 2026-06-15** (spec [`2026-06-15-okf-bundle-export-design.md`](./superpowers/specs/2026-06-15-okf-bundle-export-design.md); plan [`2026-06-15-okf-bundle-export.md`](./superpowers/plans/2026-06-15-okf-bundle-export.md); built subagent-driven, 7 tasks, 1257 suite green, tsc clean): new route **`GET /view/[code]/okf-bundle`** returns a single-course `.zip` of `index.md` + `profile.md` (reuses the shipped `profileToOkfMarkdown`) + `transcript.md` + `materials/*.md`, every file light-OKF-framed (human-readable + tool-openable). New files `lib/okf/okf-doc.ts` (frontmatter builder), `material-to-okf.ts`, `transcript-to-okf.ts`, `bundle.ts` (yazl zip). New dep: `yazl` (+ `@types/yazl`). Scoped sibling of `/okf` — same `isProgramVisible` gate (opaque 404, sandbox reachable only via the future scoped link) + `redactPiiDeep` on profile **and** transcript. Capture-surface "↓ Bundle (.zip)" link beside the "↓ Markdown" one (both header + post-capture card). Reuses existing snapshot/messages/materials queries — no new DB code. The tester take-away; subsumes the deferred self-contained-snapshot durability + (narrowed from) the whole-curriculum OKF bundle, which stays deferred. **Tiny test debt:** the route test asserts the zip + 404 gates but not that a seeded name is absent from `transcript.md` (redaction is wired; `redactPiiDeep` itself is tested elsewhere). **(4) External-access scoped magic-link — SHIPPED 2026-06-15** (spec [`2026-06-15-external-access-scoped-link-design.md`](./superpowers/specs/2026-06-15-external-access-scoped-link-design.md); plan [`2026-06-15-external-access-scoped-link.md`](./superpowers/plans/2026-06-15-external-access-scoped-link.md); built subagent-driven, 10 tasks, 1277 suite green, tsc clean): operator mints a **generic, course-less invite** (label + expiry) at **`/admin?slug=…`** (new `POST/GET/DELETE /api/admin/sandbox-grants`); the tester opens **`/sandbox/[token]`** (new public page; `/sandbox` added to `PUBLIC_PREFIXES`), enters **their course code + title + name** → **`POST /sandbox/[token]/start`** **creates their sandbox course** (`lib/sandbox/courses.ts:createSandboxCourse` — a namespaced, collision-proof `EXT-…` internal code, `scope=external/status=sandbox`, entered code+title as the display title), mints a `gc_sandbox_sess` cookie (24h) bound to the generated code → redirected into `/capture/<EXT-…>`. **Operator review:** `/admin` lists the created sandbox courses (`listSandboxCourses`) with View/Review links, and `isCourseReadableBy` has a **faculty-slug override** so the operator can open `/view` + the OKF bundle for any sandbox course (`?slug=`; testers never get the slug). New tables **`sandbox_grants`** (course-LESS generic invite, 30-day expiry, revocable; `course_code` nullable per **migration `0041`**) + **`sandbox_sessions`** (carries the self-entered `instructor_name` + the tester-created course code) — **migrations `0040`+`0041`**. New modules `lib/sandbox/{grants,sessions,access}.ts`. **Security model (least-privilege, two gates):** authorization lives in `lib/sandbox/access.ts` (`courseFromScopedPath` allowlist + `resolveScopedSession` + `authorizeCourseWrite` + `isCourseReadableBy`). **Gate 1:** middleware, for a bound session on an allowed path, **skips Basic Auth** (`NextResponse.next()`) — it does NOT inject the faculty slug (a `file.searchParams`-injection variant was built then **reverted during the adversarial security review**: the slug is a *client-exposed* credential — the capture page serializes it into `CaptureClient` props + fetch URLs — so injecting it leaked the faculty master secret to the tester + into URLs/logs, and missed body-read slug routes). **Gate 2:** each of the ~22 allowlisted routes + the capture page independently re-checks `authorizeCourseWrite(req, code, slug)` (valid slug OR session bound to exactly `code`); the capture page passes an **empty** slug to the client for scoped testers. A session opens ONLY its course's `/capture`, the whole `/api/capture/<c>/*` engine, and an allowlist under `/api/courses/<c>/` (`materials,imscc-import,kuds,scan-linked-docs,checkin,analyze-materials,parse-profile`); **blocked**: `canvas-import`/`canvas-reextract` (Clemson-Canvas-bound), `sync-from-sheet`, the bare course resource, and everything else (other courses, `/program`, `/admin`, etc.) → opaque 401/404. `imscc-import` (matcher-excluded) accepts the session directly. Read surfaces (`/view`+`/okf`+`/okf-bundle`) gate on `isCourseReadableBy` (program-visible OR bound session). The admin API enforces `checkAdminAuth` (Bearer slug/`ADMIN_TOKEN`). **Adversarial security review (Opus, 2026-06-15): no exploitable escape found** — cross-course, blocked-route, program/admin, slug-leak, path-parse (`%2F`/`..`/`//`/case), revoked-grant, and admin-mint attacks all blocked; the two exact-match gates are independent. Informational: defense-in-depth assumes no catch-all route under the scoped namespaces (guard comment added). **Known v1 gaps (deferred):** capture-surface buttons that call `/api/admin/*` (e.g. "Index now" → `v2-backfill`) are not course-scoped, so a scoped tester can't use them; and the IMSCC indexing-slowness (own entry below) applies to scoped captures too. Also expressible-but-deferred via the scope model: hypothetical/`proposed` what-if course modeling, and true multi-program (`programs` FK) for other majors. None scheduled.

- **Faculty `slug`-in-URL convention — residual, app-wide (flagged 2026-06-15).** Faculty page navigation carries the slug second factor in the URL query (`/capture/<code>?slug=`, `/admin?slug=`, the `/view` page's own Edit link, the new sandbox-grants panel's "Review in capture" link, etc.). A query secret leaks to access logs + the Referer header; the codebase started migrating *admin APIs* to `Authorization: Bearer` (`lib/auth/admin-auth.ts`) but **page navigation** still needs the slug in the URL (a browser nav can't send a Bearer header, and there's no cookie-based operator session). Security review flagged the sandbox panel's `/capture?slug=` link; kept because it matches the established convention on a Basic-Auth-*gated* route (the genuinely new *public*-route leak — `/view?slug=` — was fixed: operator override is now Bearer-header-only). **The real fix is an app-wide migration** to a short-lived httpOnly operator cookie set after slug validation, so `/capture`/`/view`/`/admin` consult the cookie instead of `?slug=`. Deferred — its own effort.
- **Bulk material wipe SHIPPED + per-id-delete Weaviate gap (2026-06-15).** New `DELETE /api/courses/[code]/materials` (collection route) wipes **every** material on a course — DB rows + local-disk blobs + per-material chunks in the course's Weaviate tenant (`deleteByMaterial`) — then clears the Canvas/cartridge import provenance (`clearCourseCanvasImport`). UI: a destructive **"Clear all materials"** button in the Materials-manager panel behind a two-step confirm. Built after a wrong-course cartridge import (65MB GC 4800 export landed in GC 2400) had to be cleaned by hand. **Residual debt:** the *per-id* `DELETE /api/courses/[code]/materials/[id]` still only removes the row + file, NOT its Weaviate chunks — so single-material deletes can orphan chunks in the tenant. Only the bulk wipe cleans vectors. Fold the `deleteByMaterial` call into the per-id route too.
- **IMSCC import is slow to index + has no progress feedback — DEFERRED (2026-06-15).** A real 65MB cartridge import ran minutes-long (much slower than a Canvas-API import) in the per-material `finalizeExtraction` stage — the V2 ingestion does chunk → `contextualizeChunk` (one LLM call per chunk) → embed → Weaviate upsert, so a full-course export with many large materials is many sequential LLM calls. Two deferred follow-ups the operator asked for: **(a)** a **progress ticker** showing files/materials as they're captured — the route is currently a single fire-and-return POST, so a live ticker needs either SSE streaming or a poll-able job-status endpoint (a real increment; design before building); **(b)** deciding whether to make indexing **async/background** (return on parse, index after) and/or cap/skip contextualization for very large materials. Per-material timing logs were added to the route to measure the real cost before designing the fix. Same bottleneck exists in `canvas-import` but is less visible there.
- **Canvas reimport URL-switch merges, does not replace (2026-06-15).** Single-mode Canvas **Reimport** now offers an optional Canvas course URL so faculty can switch to a different course (e.g. a new semester's section); a URL routes to `canvas-import` (parses the course ID, upserts materials by fileName), blank re-extracts the currently-linked course in place via `canvas-reextract`. **Caveat:** because the import upserts by name, the standard `Canvas: Syllabus/Assignments/…` list rows refresh cleanly on a switch, but unique `Canvas File:` PDFs from the *prior* course are NOT removed — they linger until manually ignored/deleted. A true "replace, don't merge" option (clear prior Canvas-sourced materials for that `source_code` before import) is **deferred**; per-file cleanup is the workaround for now.

- **O*NET / occupational-framework skills-taxonomy anchor — researched, NOT adopted as proposed (2026-06-15, decision).** Prompted by "30 sub-competencies is too few; use O*NET." A deep research dive (2 web agents + internal-doc reconciliation) reached a **split verdict**: O*NET's *generic Skills tier* (Critical Thinking, Complex Problem Solving) is rejected by the very authorities `background.html` §4 already cites (Willingham, Perkins & Salomon, Gick & Holyoak) **plus** the National Academies' 2010 construct-validity critique of that same tier; the CBE *atomization* critique (Gonczi, Lombarts, Winch) + the empirical **UK NVQ failure** warn against an external skill-checklist as the *backbone*; and O*NET is **not** a ready-made GC vocabulary (33 Knowledge areas too coarse; needs hand-curated DWA/Task extraction from GC SOC codes; ≥2 of 5 targets have no clean SOC home). It **aligns** only at the domain-task tier and on the **demand side** (Importance/Level as a career-requirement benchmark), and our **KUD depth is the exact layer the skills-intelligence field is missing** — so don't dilute it. **Decision: keep the hand-curated, evidence-anchored sub-competencies as the backbone; (if prioritized — not urgent) expand granularity by hand using O*NET GC-SOC Tasks/DWAs as a sourced checklist, add O*NET only as a demand-side benchmark, embeddings only as candidate-surfacing + a near-miss queue with depth-aware human/LLM adjudication, and leave capture (the interview) untouched — post-processing reproduces the same mapping; any coverage-radar is a later, fenced, advisory optimization.** Also surfaced: `background.html` §3 has **no occupational-frameworks row** — a doc gap to fill with this verdict. Full memo + sources: [`2026-06-15-occupational-frameworks-onet-research.md`](./superpowers/specs/2026-06-15-occupational-frameworks-onet-research.md); the phased **utilization plan** (SOC bindings for the 5 targets, O*NET ingest mechanics, industry-standards domain vocabulary, demand benchmark, embedding near-miss queue — Phases 0–4, not scheduled) is in [`2026-06-15-occupational-framework-utilization-plan.md`](./superpowers/specs/2026-06-15-occupational-framework-utilization-plan.md). Background research updated: `background.html` §3 gains an occupational-frameworks row + §11 a split-verdict subsection. **Sharpened by the operational research:** O*NET is even more bounded for GC than the memo assumed — it omits GC's actual disciplines (color management, imposition, substrate, trapping, PDF/X, packaging, press chemistry), so the GC domain vocabulary should come from **industry-standards bodies (Idealliance/G7, Ghent Workgroup, FTA/FIRST, GAERF)**, with O*NET used for SOC labeling + demand benchmark + a partial production-tier checklist; and **no published O*NET-Level→KUD crosswalk exists** (keep demand and attainment as separate columns). (My earlier rush to spec an "O*NET-anchored taxonomy + interview radar" was premature — the research does not support it.)

- **Candidate sixth CourseCapture Area-7 condition: *abstraction-and-bridging* — deferred design (2026-06-14, surfaced by the problem-solving transfer research review).** The transfer literature (Gick & Holyoak 1983; Gentner/Loewenstein/Thompson 2003; Perkins & Salomon 1989) identifies abstraction across surface-varied cases + deliberate bridging to new contexts as *the* active ingredient that makes problem-solving transfer — and the current five Area-7 probes (generate-consolidate, ill-structured, revision, post-mortem, depth) don't capture it. A sixth probe ("does the course require abstracting a principle across multiple varied cases and applying it to a genuinely new context?") was the highest-value research-driven audit extension. **DONE 2026-06-14** (`feat/abstraction-bridging-condition`, capture-first MVP): Area-7 interview probe (e) + synthesis emission + `abstraction_bridging` schema field (Zod-optional for snapshot back-compat, strict-required for new captures, evidence-above-zero for non-absent) + a new per-course Area-7 conditions block on `/view`. Spec [`2026-06-14-abstraction-bridging-condition-design.md`](./superpowers/specs/2026-06-14-abstraction-bridging-condition-design.md). Backing detail + citations in [`docs/problem-solving-deep-dive.html`](./problem-solving-deep-dive.html) (transfer section + Area-7 Claim 2). **STILL deferred:** the program-level **ScaffoldingStrip aggregation** of the new condition (no data until courses are re-captured — snapshots are immutable, so the field populates per course on re-capture, never by backfill).

- **Guided faculty-reconciliation review — Piece 2 — RESOLVED 2026-06-14 (branch `feat/guided-faculty-reconciliation`).** Shipped: hybrid stepper (`reconcile` capture stage), `reconcile-feedback` AI function, `applyReconciliation` pure engine, `ReconciliationStepper` UI, migration 0038 `reconciliation_log`. Residual v1 limitations (not blocking): **(1)** in-progress reconcile state is **client-side only** — a mid-pass browser reload restarts the reconcile (the synthesized profile is unharmed); server-side `reconcile_messages` persistence is the future enhancement. **(2)** The optional `revised_objectives_draft` → `apparent_outcomes` field rename is still deferred (kept for snapshot back-compat; Piece 1 of 2 reframes the label without renaming the key). **(3)** Depth-tweak inputs NaN-guard + stale-log reset shipped in the review-fix commit (pre-existing, resolved in-branch).

- **OKF-align the wiki layer + ship a `/wiki/graph` view — deferred follow-up (2026-06-13, operator-requested after an OKF spike).** Google's Open Knowledge Format ([blog](https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing/)) is a published formalization of the pattern the `gc-curriculum-wiki` layer already implements (typed markdown + frontmatter + cross-links + deterministic lint; cf. the myKG/Karpathy LLM-wiki lineage of `feat/wiki-trust-hardening`). A throwaway spike rendered the live wiki (42 nodes / 261 edges — 5 courses, 30 competencies, 5 targets, 2 concepts; 109 typed edges + 152 prose `[[mention]]` edges) into a single self-contained 40 KB HTML force-graph straight from the existing frontmatter relations (`develops_competencies`/`contributes_to_targets`/`sub_competencies`/`career_target`/`prerequisites`) — **no schema change, it's a read.** Two deferred pieces: (1) a standing `pnpm wiki:graph` script or `/wiki/graph` route that regenerates the self-contained visualizer on wiki/snapshot updates (fits local-only deploy — no backend/CDN); (2) a light frontmatter naming pass to OKF v0.1 conformance (`type`/`title`/`resource`/`tags`/`timestamp`) so the wiki bundle becomes a portable interchange format for external tools/agents — best folded into `feat/wiki-trust-hardening` when it merges. Explicitly **out of scope**: the quantitative KUD+ core (coverage matrix, depth scores, evidence bands, prereq edges, reliability) stays relational in Postgres — OKF is a prose-grounding format, not a metrics/semantic layer, and forcing scores into markdown would lose queryability + the reliability tripwire. The interop payoff is latent until there is an external consumer of the knowledge bundle; revisit then. Spike artifacts (not committed): `/tmp/curriculum-knowledge-graph.{html,png}`. **Per-course OKF profile export — DONE 2026-06-14** (`feat/portable-okf-profiles`): each captured course is now downloadable as a self-contained OKF-v0.1 markdown file via `/view/<code>/okf` + "↓ Markdown" link; reuses `redactPiiDeep`, pure serializer `lib/okf/profile-to-okf.ts`. **Capture-surface OKF download — DONE 2026-06-14** (`feat/capture-okf-download`): the faculty `/capture/<code>` review surface now has a "↓ Markdown" link to `/view/<code>/okf` (last-saved snapshot), shown when a snapshot exists or was just captured; UI-only, reuses the shipped route + serializer. Spec [`2026-06-14-capture-surface-okf-download-design.md`](./superpowers/specs/2026-06-14-capture-surface-okf-download-design.md). **Wiki OKF-v0.1 frontmatter alignment — DONE 2026-06-14** (`feat/wiki-okf-frontmatter`): the `gc-curriculum-wiki` narrative pages (4 types + per-section `index.md` hubs) now carry OKF v0.1 frontmatter (`type/title/description/slug/tags/timestamp/resource`), stamped deterministically on regen (pure `lib/ai/wiki/okf-frontmatter.ts` + `section-index.ts`, wired into `update.ts` post-stamp loop + `git-ops.ts` `writeAndPushSerial`), backfilled via `pnpm wiki:backfill-okf` (43 pages migrated, 4 index hubs, wiki repo `b6915f7`), enforced by `gc-wiki-lint` (`okf-frontmatter-missing` = error). `description` is agent-authored going forward / body-derived (wikilink-stripped) on backfill. Domain relations preserved; top-level `index.md` addressed in the root-index follow-on (below). Wiki prompt + wiki-repo `CLAUDE.md` schema updated to the OKF vocab. Spec [`2026-06-14-wiki-okf-frontmatter-alignment-design.md`](./superpowers/specs/2026-06-14-wiki-okf-frontmatter-alignment-design.md). **Root `index.md` OKF conformance — DONE 2026-06-14** (`feat/wiki-root-index-okf`): the wiki's top-level dashboard `index.md` now carries OKF v0.1 frontmatter too — stamped on regen (the `update.ts` exclusion dropped; `okfResource` maps `type:index` → `/wiki`), backfilled (`backfillRootIndex` + `deriveTitle` in `wiki-backfill-okf.ts`, wiki repo `14b3413`), and lint-enforced (a root-index `okf-frontmatter-missing` check). The **whole wiki** is now OKF-conformant. Spec [`2026-06-14-wiki-root-index-okf-design.md`](./superpowers/specs/2026-06-14-wiki-root-index-okf-design.md). STILL deferred: whole-curriculum **bundle zip** (all courses' .md + index.md, reuses the serializer); and the **`/wiki/graph`** view. (Note: `pnpm wiki:lint` still reports ~45 pre-existing `broken-wikilink` errors — prose forward-refs to uncaptured competency/course pages, unrelated to OKF; 0 `okf-frontmatter-missing`.)

- **Two-tier faculty auth is role-tiering with shared credentials, NOT per-user identity (2026-06-13, branch `feat/two-tier-faculty-auth`).** `gcfaculty` (full access) and `cufaculty` (create-only) are shared secrets — there is no per-person attribution, audit trail, or per-user revocation; rotating either secret affects everyone who knows it. `faculty` can still EDIT ANY course (no per-course ownership or lock). The real-auth work (SSO/Shibboleth, magic-link faculty sessions) remains deferred — see "Real faculty auth" below. The two-tier model is a stopgap that adds a narrower sharing credential without building real identity.

- **Structured course identity Option-1 migration seam — deferred by design (2026-06-13, branch `feat/course-identity-bundling`).** The `course_codes` table holds a paired code as a child row of its primary course, NOT as an independent `courses` row. Promoting a `course_codes.paired_code` to its own `courses` row is a data migration (populate code/title/level/track from the parent, repoint any `course_materials.source_code` rows), NOT a redesign — the schema already supports it. Defer until there is an independently-navigable or prereq-able lab (e.g., a lab section with its own syllabus, LOs, and prerequisites to analyze). Also deferred: bundle-aware Canvas import verified by component tests only — a live visual smoke (Step-1 CanvasBox showing both codes + a bundled import) was not run because GC 3460 (the only paired-code candidate) already has a profile and opens on the review stage, bypassing Step 1.
  - **Final-review minor findings, not fixed (2026-06-13):** (a) `createCourse` normalizes the *paired* code through `composeCourseCode(parseCourseCode(...))` but the *primary* `code` is only `.trim()`ed — asymmetric; harmless on the real roster (codes arrive pre-formatted from the add-flow), revisit if codes ever arrive un-normalized. (b) `createCourse` will attempt the paired-code link even when the primary already existed (`onConflictDoNothing` on the course insert); benign because the 23505 dup-catch absorbs a re-link, but the "link-on-existing" path is intentional and undocumented in code. The final reviewer's one Important finding — the in-session context-refresh route (`app/api/capture/[code]/context/route.ts`) dropped `source_code`, collapsing the lecture/lab grouping until a full reload — **was fixed in-branch** (the route now returns `sourceCode`).



- **Faculty flags — deferred by design (2026-06-12).** `prototype_flags` TABLE retained after the dead-chain sweep (dropping it is a standalone migration decision). Profile-competency flags resurface on EXACT statement match only (fuzzy matching out of scope — unmatched flags still appear in the /program roll-up). Review-panel surfaces file flags but render no per-row open-flag markers (matrix-only markers, per plan). `AnalysisFrame` in lib/domain/types.ts is consumer-less after the sweep (left with an honest comment; delete in a future tidy). Latent cascade risk: `faculty_flags` FKs are ON DELETE CASCADE — dormant today (no hard-delete path exists for courses/targets/sub-competencies), but a future hard-delete route would silently destroy flags, contradicting the never-auto-clears design; switch to `set null` + annotation if such a route is ever added.
- **CourseCapture entry refinements — deferred (branch `feat/capture-entry-refinements`, 2026-06-11).** (1) The Step-1 **Index now** button reuses the *admin* `POST /api/admin/v2-backfill` from a faculty surface — acceptable under the single-user LAN model (faculty + admin share one Basic Auth tier), but a dedicated faculty-tier `POST /api/courses/[code]/index-materials` would be cleaner. (2) Materials that are genuinely unindexable stay `indexing_status='pending'` rather than being auto-marked `skipped`, so they still read as "pending" in raw data (the UI now labels them honestly via `materialReadability`, but the underlying status isn't reconciled). (3) `Explore →` was dropped from the capture header per the operator; trivially restorable if missed.
- **CourseCapture Step 1 materials gate — deferred polish (branch `feat/capture-materials-step`, 2026-06-11).** (1) No explicit "N material still indexing — it'll be ready shortly" banner on Step 1 — Continue is allowed while indexing finishes in the background (acceptable; the per-row status dot already shows "indexing…"). (2) Final placement of the **Help** + **Snapshot-history** affordances: with the bottom "⚙ Materials…" gear disclosure no longer leading on a fresh landing, those two trays currently still live in that disclosure on the *interview* sub-step (post-Continue) — fine for now, but a tidier home (a small header menu, or folded into Step 1's secondary area) is unbuilt. Neither blocks the fix.
- **Course categories / career flag — deferred by design (branch `feat/course-categories-career-flag`, 2026-06-11).** (1) **Track modeling** — alternate tracks and swapping courses into/out of the career-building set to see coverage impact; `builds_to_career` is the seam this hangs off (a future `track_courses` join or per-track boolean override). (2) **Per-student elective-contribution view** — "if I take elective X, how does my path fill in?". (3) **Coverage cells for `builds_to_career=false` courses are RETAINED, not deleted** — the matrix read filters on the course join so excluded courses simply stop appearing; flipping a course's flag back to `true` later restores its cells with no rescoring. A future cleanup could prune them, but retention is cheaper + reversible. (4) **Exclusion scope decision (Ⓑ):** near-term only the 27 flagged courses map; chosen with the operator (16 Core + 11 high-enrollment Major Req), excluding optional Specialty electives + the unselected choose-one sides. (5) **The "held 0031/0032 block DB CHECKs" framing below now lapses** once `0033` applies (it carries `0032` in via the watermark) — the deferred prereq/intended-skills DB CHECKs can be bundled into a future migration without the held-migration blocker.
- **Migration journal cosmetic hash drift — intentionally NOT repaired (2026-06-11).** `__drizzle_migrations` hashes for `0003`/`0022`/`0023` no longer match their (later-edited) `.sql` files. Verified harmless: both `drizzle-kit migrate` and the programmatic migrator apply by a **created_at watermark** (only entries whose `when` exceeds the max applied `created_at` run), and all three sit below the watermark so they never re-run. Repairing the journal is risk-for-no-reward (a botched watermark would silently skip or re-run real migrations); if cosmetic tidiness is ever wanted, do a `drizzle-kit up` snapshot rebuild on a branch with a DB backup as its own task, not bundled into feature work. (Note: `drizzle/meta/` is gitignored — migrations apply once from the dev checkout against the single shared local Postgres, which both `dev` and the deploy worktree point at; the deploy worktree never runs `db:migrate`.)
- **Wiki band floor — RESOLVED 2026-06-14 (structured `evidence_bands` frontmatter, branch `feat/wiki-structured-band-floor`).** `search_wiki`/`read_wiki` no longer scrape `·claimed`/`·materials`/`·artifact` prose tokens — each course/competency page now carries a structured `evidence_bands: [...]` list, stamped deterministically in `update.ts` (mirrors `input_hash`, derived from the same `deriveEvidenceBand` data; the prose markers stay for humans). Read-time uses `resolvePageBands` = frontmatter-first with a prose-scrape fallback for un-backfilled pages. New lint `evidence-bands-missing` (warning) flags any course/competency page that has markers but no structured field. The pure helpers (`stampEvidenceBands`/`dedupeBands`/`readEvidenceBandsFrontmatter`/`resolvePageBands`) live in `lib/ai/wiki/evidence-band-markers.ts`. Spec [`2026-06-14-wiki-evidence-band-structured-design.md`](./superpowers/specs/2026-06-14-wiki-evidence-band-structured-design.md) · plan [`2026-06-14-wiki-evidence-band-structured.md`](./superpowers/plans/2026-06-14-wiki-evidence-band-structured.md). **Operator action — DONE 2026-06-14:** `pnpm wiki:backfill-bands` ran on deploy (35 pages stamped: 5 courses + 30 competencies), committed + pushed to `gc-curriculum-wiki` (`172a241`); `pnpm wiki:lint` reports 0 `evidence-bands-missing`. Merged + deployed to `main` (`eb5f28a`). Target-page coverageRollup still carries no band (unchanged, out of scope).
- **Quick-review one-click "Accept all & approve" — deferred (2026-06-10).** The capture-review triage + goal-first-landing redesigns shipped; the one open piece is a one-click "Accept all & approve" for the AI-confident competencies — shipped only as advisory "Looks right ✓"/reviewed marking, not an auto-approve (a deliberate-vs-frictionless approval decision left to the operator). Shipped detail in git + [`2026-06-10-capture-ux-redesigns-design.md`](./superpowers/specs/2026-06-10-capture-ux-redesigns-design.md).
- **Projects-first-class only populates on the v2 synthesis path (branch `projects-first-class-2026-06-08`).** `class_structure` / `major_projects` are extracted by `generateCaptureProfileV2` (gated `COURSECAPTURE_V2_INGESTION=1`); the legacy v1 path (`generateCaptureProfile`) leaves them `null` → the review panel + wiki show "not yet captured" (graceful). **(v1 path retired 2026-06-11 — all captures now go through v2, so the "v1 leaves them null" concern is moot.)** Minor a11y debt — the review panel's class-structure topic chips were click-to-edit but not keyboard-focusable — **FIXED 2026-06-11** (`ClassStructureSection`: read-mode chips now have `role="button"` + `tabIndex=0` + Enter/Space handler + focus ring).
- **Shared "working/running" indicator + relaunch-safe background jobs — DESIGN-PENDING (Roadmap #17).** Long AI actions need a live "working" signal and background fire-and-forget jobs need a "running" indicator + relaunch-safety. Addressed piecemeal on 3 surfaces (2026-06-05 live-testing pass: stress-test + coverage spinners, transcription/`/ask` fixes), but the general cross-cutting pattern (and the wiki watermark/reconcile) is the real fix — needs its own brainstorm → spec, not another piecemeal pass.
- **Real faculty auth.** Current Basic Auth gate is a stopgap. Options: magic-link sessions (same pattern as `/partners/*`), Clemson SSO/Shibboleth, OAuth via Clemson IdP. Revisit in deployment-planning phase.
- **Admin-route `?slug=` auth is query-string-based (cross-cutting hardening).** Every `/api/admin/*` route (`synthesis/[targetId]/run`, `resync-courses`, `partners`, `partners/import`, and the new `synthesis/targets/[targetId]/regenerate-aggregate`) authenticates with a `?slug=` query param via `isValidSlug`, behind the faculty Basic Auth middleware. An automated security review (2026-06-05) flagged this on the PC v1 `regenerate-aggregate` route: query-string secrets leak to access logs / Referer. It's a *secondary* gate (Basic Auth is primary, LAN-only) and matches the codebase-wide admin convention, so it's not a PC-v1 regression — but the proper fix is cross-cutting: move the slug off the query string to an `Authorization: Bearer` header (timing-safe compare against a server-side secret) or a real admin session across ALL admin routes + their callers (e.g. `AggregatePanel`), not one route in isolation. Bundle with "Real faculty auth" above. **Partial progress (branch `audit-remediation-2026-06-07`, 2026-06-07):** the three admin routes that were *missing* the slug second factor entirely (`mark-invited`, `v2-reset`, `v2-backfill`) now have it, so all admin routes are consistently slug-gated (regression test enforces it). **Server-side dual-accept LANDED 2026-06-11 (branch `chore/system-hardening`):** new `lib/auth/admin-auth.ts:checkAdminAuth(req,{slug?})` accepts `Authorization: Bearer <ADMIN_TOKEN>` (timing-safe via `authorizedForBearer`, fail-closed) OR the slug presented in that same Bearer header (so the secret leaves the query string with NO new env var — `isValidSlug` on the header value) OR the legacy `?slug=`/body slug (accepted + `console.warn`'d so callers can be confirmed migrated before removal). All 12 admin routes migrated to it; the F1 regression test now asserts `checkAdminAuth`; helper unit-tested; `ADMIN_TOKEN` documented in `.env.example` (optional). **Chosen approach: dual-accept transition (not hard cutover)** — see the AskUserQuestion decision. **REMAINING (the leak-closing client flip — deliberately not blind-cut here):** move the slug from the URL into an `Authorization: Bearer ${slug}` header and drop `?slug=` in the query-string client callers — `app/admin/synthesis/targets/[targetId]/AggregatePanel.tsx` (regenerate-aggregate), `app/courses/CourseRosterControls.tsx` (roster, 2 fetches), `app/courses/[code]/PrereqEdgesClient.tsx` (prereq-edges `base`, 3 defs + `&id=`/`&mode=` appends → switch to `?`). These work today via the slug fallback; removing the query removes that fallback for the converted caller, so each needs a live click-test (a mistake 401s one admin button — contained, not a full lockout). Once confirmed live, remove the legacy `?slug=`/body-slug fallback from `checkAdminAuth` and (optionally) provision a distinct `ADMIN_TOKEN`. (`intended-skills` query-slug route has no client caller; body-slug POST routes don't leak to logs/Referer the way query strings do, so they're lower priority.)
- **Intended-skills rough pass — deferred review findings (low, non-blocking).** From the 2026-06-05 adversarial review (1 confirmed HIGH fixed — the `mixed` basis so a cross-prereq *intended* value can no longer close a gap under a `measured` label; + 2 mediums fixed — no-catalog courses now skip the AI call, `droppedUnknown` surfaced): lows (reviewed 2026-06-11): (1) DB `CHECK (intended_k/u/d 0–5)` — **deferred:** Zod + the strict JSON schema enforce the range on every real write path, and the CHECK can't go through `drizzle-kit migrate` right now without also applying the deliberately-held `0031`/`0032` (demand-coverage seam) — bundle it when that seam is activated (data verified clean 2026-06-11). (2) `mode=one` 500 doesn't signal `spendRecorded` — kept (accepted pattern, matches prereq-edges). (3) redundant `getCourseByCode` round-trip in `mode=one` — kept (micro-perf; the clean fix restructures the seed loop for ~no gain).
- **Prerequisite edges — deferred review findings (low, non-blocking).** From the 2026-06-05 adversarial review (6 confirmed crit/high all fixed — incl. the **critical** `eq(retiredAt,null)`→`isNull` that had silently killed the gap engine's measured path, the confirmed-only cycle guard, and cross-course PATCH/DELETE scoping): lows (reviewed 2026-06-11): (1) TOCTOU in `addFacultyEdge` — **deferred:** single-admin LAN tool (concurrent edits can't realistically race), and a correct fix needs threading a serializable tx through the `wouldCreateCycle` read path — disproportionate to the risk. (2) DB `CHECK (focal != prereq)` + (3) index on `sub_competency_id` — **deferred:** both need a migration, which `drizzle-kit migrate` can't apply without also applying the held `0031`/`0032`; bundle when that seam is activated (data verified clean 2026-06-11 — 0 self-edges, so the CHECK applies cleanly). (4) `bulkCreateCourses` per-row loop — **FIXED 2026-06-11** (single batched insert). None blocks the rough-pass increment or program-wide viz.
- **Position Capture v1 — deferred review findings (low, non-blocking).** From the 2026-06-05 adversarial whole-branch review (9 confirmed crit/high all fixed; these low ones reviewed 2026-06-05): (1) ~~concurrent same-session POSTs surface as a generic 500~~ **FIXED** — the unique-constraint violation (`23505` / `uq_position_capture_messages_session_turn`) now returns an idempotent 409 instead of a confusing 500 (`app/api/partners/[token]/positions/[id]/chat/route.ts`); (2) ~~`Page1Section` two `<input type=file>` sharing one `ref`~~ **FIXED** — hoisted to a single always-mounted hidden input; (3) ~~no `completeness >= 'rated'` guard before reaching Page 6~~ **FIXED 2026-06-11** — the Page-6 interview/synthesis POST 409s unless completeness is `rated` or `interviewed` (the agent bundle relies on Page-5 `ratedSkills`). Full review JSON archived in the session transcript dir.
- **Industry Partner Input Plan 2** — position ratings table + project-rating heat map. Gap between Plan 1 and the already-shipped Plan 3 synthesis.
- **Cross-snapshot diff view.** Phase 2 carryover.
- **Provider-aware function-tier routing.** `lib/ai/function-settings.ts` maps `light/default/heavy` to OpenAI model names (`gpt-5.4-mini` / `gpt-5.4` / `gpt-5.5`); selecting `AI_PROVIDER=campus` currently sends those names through to the campus endpoint, which doesn't host them. To actually use GLM/DeepSeek/Qwen via the tier system, the resolver needs to be provider-aware (different tier→model map per provider, or per-function `(provider, model)` rows). Tactical wire-in works today by using `AI_PROVIDER=campus` + setting a per-function `customModel`, but that's a workaround. Spec the refactor once one campus model is in production use.
- **`FACULTY_BASIC_AUTH` in `.env.local` carries an inline `#` comment — strip it when scripting (2026-06-08).** The line is written `FACULTY_BASIC_AUTH=<token>  # <comment>`. `@next/env`/dotenv strips the inline comment, so the **live credential is only the token before the ` #`**. The dev server (launchd `com.gc.curriculum-tool` → `next dev`, which loads `.env.local` at boot) and any browser that has authed both agree on that stripped value — there is **no** server-vs-file mismatch. But naive shell extraction (`cut`/`grep`/`sed` without comment-stripping) yields the *whole* line and 401s on every faculty surface + headless `/api/*` call, while cached browser sessions keep working — which masquerades as an "env mismatch needing a restart" but is not (chased exactly this red herring on 2026-06-08; an unnecessary `launchctl kickstart` did not change anything). To script against faculty `/api/*` routes: parse the value as dotenv does — `v = afterEquals.replace(/\s+#.*$/, '').trim()` — then send `Authorization: Basic base64(v)`. If `.env.local` is *genuinely* edited, the dev server only picks it up on restart: `launchctl kickstart -k gui/$(id -u)/com.gc.curriculum-tool` (briefly drops the partner Funnel — do it at low traffic).
- **System-audit deferred mediums (2026-06-07, branch `audit-remediation-2026-06-07`).** Confirmed by the 8-area audit but consciously not fixed in the remediation pass (full list + rationale in [`docs/superpowers/audits/2026-06-07-system-audit.md`](./superpowers/audits/2026-06-07-system-audit.md)). The load-bearing ones: **(1) F11 broad cost-accounting rollout — DONE (per-route) 2026-06-11 (branch `chore/system-hardening`), with one scoped remainder.** Approach chosen: per-route shared guard (NOT centralized in `provider.complete`, which would double-count the ~6 routes that already record). Of the originally-listed ~10 routes, two (`explore/build-downstream`, `program/scaffolding`) make **no AI call** (pure DB/deterministic) and were correctly excluded. The remaining **7 paid routes now `checkDailyCap()` → 503** when the cap is hit (the actual overspend ceiling): `kuds/generate`, `parse-profile`, `explore/analyze`, `explore/what-if`, `explore/draft-custom`, `capture/chat`, `ask/chat`. The **5 non-streaming** of those also `recordSpend()` — cost surfaced from their helpers (`costUsdCents` added to `CompareResult`/`SimulateWhatIfResult`/`DraftCustomTargetResult`/`parseProfileFields`; `kuds/generate` used its existing telemetry). **Remaining (scoped follow-up):** the two **streaming** routes (`capture/chat`, `ask/chat`) are **gated but do NOT yet `recordSpend`** — the provider's final-event `telemetry.costUsdCents` isn't forwarded through `streamAuditAgent` / `runAuditAgent` / `streamCurriculumChat` to the route. They're protected (503 once the cap is hit from other routes' recorded spend), but their OWN per-turn spend under-counts the daily total until the forwarding lands. Forward cost through those three generators + record it in the two routes to close it. **(2) legacy `getProvider()` scorers — LIVE ONES MIGRATED 2026-06-11 (branch `chore/system-hardening`).** Of the ~15 bare-`getProvider()` call sites, only four were actually reachable; those now use `getProviderForFunction(id)` so `/settings` tiers take effect: `parse-profile-fields`, `extract-course-kud` (kuds/generate), `synthesize-target` (admin synthesis orchestrator) — three new functionIds added, all default tier to preserve the prior effective model — and the `analyze-materials` route now uses the existing `materials-analysis` id (declared tier **light** → a deliberate effective-model change from the old env-default gpt-5.4; bump in `/settings` if per-file quality drops). The two **confirmed-dead** chains were deleted (`kud-draft.ts`/`draft-outcomes.md`, `coverage-score.ts`/`score-coverage.md` + their tests). **Unreachable M-trial scorers — RESOLVED 2026-06-11 (item 13):** the 7 superseded ones were DELETED (modules + prompts + tests) — `extract-prereqs` + `prereq-suggest` (→ `prereq-edge-seed`), `gap-analyze` (→ `decompose-prereq-gap` + prereq engine), `kud-draft-course` (→ `extract-course-kud`), `scaffolding-eval` (→ deterministic `lib/program/scaffolding.ts`), `score-prior-coverage` + `analyze-course-gaps` (M-trial course-builder, gone). **Kept:** `evaluate-course-scaffolding` (Phase 1B Stage 2 build-ahead) — still on `getProvider()`, migrate when wired. `extract-text.ts`'s vision fallback deliberately stays on `getProvider()` — it needs a vision-capable model and must not be steerable to a non-vision tier via settings. **(3) wiki** — batch-page-dropped reconciliation + prompt-injection fencing **both ADDRESSED 2026-06-11 on branch `feat/wiki-trust-hardening`** (reconcile + `input_hash` watermark in `update.ts`; fencing in `wiki-update.md` — see Active arc). **index-stale-in-first-batch — FIXED 2026-06-11:** the index is forced into the LAST batch and handed a manifest (`substrate.affectedPages`) of every page in the run, so its navigation stays complete regardless of batch splits (`update.ts` c.1 + `wiki-update.md` index section). **(4) wiki private-repo assertion** — nothing in code enforces the gc-curriculum-wiki remote staying private (it currently is); add a startup check or push-time guard. **(5) scaffolding `coverage_only` mislabel — FIXED 2026-06-11:** added a `no_introduction` status (practiced/integrated but never introduced — distinct from brittle and coverage_only) in `lib/program/scaffolding.ts`, with a UI chip in `ScaffoldingStripClient` and a focused status test. **(6) low/cosmetic — ALL FIXED 2026-06-11:** daily-cap day key now LOCAL (matches `CURRENT_DATE`, so the cap resets at local midnight); spreadsheet base64 strip broadened to any `data:` MIME; `getSessionInstructor` uses a direct NOT NULL + LIMIT 1 query (no 50-row cap); evidence-ladder honors `source='materials'`. (Dead-prompt drift also resolved: `draft-outcomes.md` + `score-coverage.md` deleted with their dead chains; prompt count now 33, `kud-chat.md` correctly recorded as mounted.)

---

## What this file tracks

Update STATE.md as part of any commit that touches:

- **Routes** — anything added/removed/renamed under `app/**/page.tsx` or `app/api/**/route.ts`
- **Schema** — any new Drizzle migration in `drizzle/`, or any change to a table in `lib/db/schema.ts`
- **AI functions** — adding / removing / renaming an entry in `AI_FUNCTION_IDS`, or changing a default tier in `DEFAULT_TIERS` (`lib/ai/function-settings.ts`)
- **Env vars** — anything added/removed in `.env.example`
- **Deployment surface** — middleware behavior, what runs where, launchd plists
- **Plan / spec status** — when a plan ships, a spec is superseded, or a new plan/spec file lands in `docs/superpowers/{plans,specs}/`
- **"What's live"** — adding / removing / renaming a user-visible surface
- **Deferred work / debt decisions** — whenever you *consciously choose not to do* something (defer a fix, skip a finding, leave a known bug, hold an item for sign-off), add it to **Deferred / debt** in the same commit. This is the one trigger that leaves no code diff to trip over, so it relies entirely on this rule: a decision-not-to-change is invisible unless written down. An audit/review report counts only as the backing detail — its deferred items still need a one-line pointer in Deferred / debt, which is where maintainers look.

Trivial commits (typos, copy edits, single-line bugfixes, internal refactors that change nothing above) do **not** update this file.

---

## Conventions (project-specific)

- **Specs and plans** are created via `superpowers:brainstorming` and `superpowers:writing-plans`. One dated file per increment; never edit historical specs/plans (write a new one that supersedes).
- **Plans executed** via `superpowers:subagent-driven-development` (fresh subagent per task + spec/quality review).
- **`docs/`** is published to GitHub Pages from `main`. **Pages serves from the repo ROOT (`source.path = /`), not `/docs`** — so a doc at `docs/foo.html` is reached at `chiptoe-svg.github.io/gc-curriculum-tool/docs/foo.html` (the `/docs/` segment is required; a bare `…/foo.html` 404s). The source `.md` files are the source of truth, the `.html` siblings are styled exports.
- **`pnpm`** is the package manager. `pnpm dev` for solo dev (localhost); `pnpm dev:lan` for the launchd 0.0.0.0 binding.
- **Migrations** via `pnpm db:generate` + `pnpm db:migrate` (drizzle-kit). Never rename a generated migration file.

---

## Related

- [`CLAUDE.md`](../CLAUDE.md) — session bootstrap; KUD+ summary, doc map, codegraph protocol, pre-impl ritual.
- [`docs/superpowers/README.md`](./superpowers/README.md) — full doc index (specs, plans, vision, deep-dives, pilot writeups).
- [`docs/superpowers/running-locally.md`](./superpowers/running-locally.md) — local-Mac setup, launchd plists, Docling + omlx wiring.
